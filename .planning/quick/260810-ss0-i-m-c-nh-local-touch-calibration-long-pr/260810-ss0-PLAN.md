---
mode: quick
phase: quick
plan: 260810-ss0
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-touch.sh
  - tests/wrapper_production_check.sh
  - patches/onemix-touch.patch
  - README.md
autonomous: true
requirements: [GEST-01, GEST-02, GEST-03, CONF-01]
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "With both calibration environment variables unset, the canonical local-touch launch passes a 500 ms long-press duration and a 12 px touch slop to xfreerdp3 without prompting the user."
    - "The local X11 recognizer uses the same 500 ms and 12 px values whenever the existing rdpSettings fields are zero, including one-finger long-press/drag/tap-pair handling and two- or three-finger translation claim thresholds."
    - "The existing accepted ranges, calibration variable names, mouse-only behavior, local-only gesture ownership, and fixed pinch classifier constants remain unchanged; 600 ms and 8 px remain valid explicit overrides rather than defaults."
    - "The refreshed integrated quilt patch applies to a clean pinned source extraction, and the existing wrapper, dispatcher, classifier, and release-smoke checks pass."
  artifacts:
    - "scripts/launch-touch.sh contains 500 and 12 as the only unset-environment calibration defaults."
    - "patches/onemix-touch.patch carries synchronized X11 zero-value fallbacks, command-line help text, and dispatcher/classifier regressions."
    - "tests/wrapper_production_check.sh asserts the default wrapper arguments while retaining boundary and invalid-input coverage."
    - "README.md describes the same daily long-press and touch-slop defaults as the executable paths."
  key_links:
    - "FREERDP_TOUCH_LONG_PRESS_MS/FREERDP_TOUCH_SLOP_PX -> launch-touch.sh validation -> /touch-long-press and /touch-slop arguments -> existing FreeRDP settings -> local XI2 recognizer."
    - "Zero-valued FreeRDP_TouchLongPressDurationMs/FreeRDP_TouchLongPressSlopPx -> xf_input_touch_fallback and the two-/three-finger resolvers."
    - "patches/onemix-touch.patch -> fresh dpkg-source/quilt extraction -> TestXfInputDispatcher and build-release classifier smoke."
---

<objective>
Change the local-touch calibration defaults from 600 ms / 8 px to 500 ms / 12 px everywhere users or the X11 local-only recognizer receive an unset-value fallback.

Purpose: The launch wrapper, the packaged source, regressions, and current user documentation must agree on one calibration baseline while preserving the established range validation and gesture architecture.

Output: A refreshed integrated quilt patch, synchronized wrapper and README defaults, and strengthened coverage using the project’s existing regression and release-smoke paths.

Discovery level: 0. This is a bounded numeric-default synchronization using existing shell, FreeRDP settings, XI2 dispatcher, CTest, quilt, and Debian release patterns; it introduces no dependency, API, configuration surface, or architectural choice.

Trace completed before planning:

| Surface | Current 600 ms / 8 px fallback or reference | Planned treatment |
|---|---|---|
| Wrapper runtime | `scripts/launch-touch.sh` expands unset `FREERDP_TOUCH_LONG_PRESS_MS` to 600 and `FREERDP_TOUCH_SLOP_PX` to 8. | Change only those unset values to 500 and 12; retain validation and argument names. |
| X11 runtime | `xf_input.c` in the integrated patch uses zero-value fallbacks for one-finger begin, one-finger motion, short-tap pairing, two-finger pan, and three-finger pan. | Synchronize every setting-backed fallback: duration to 500 and slop/pan thresholds to 12. |
| CLI help | `client/common/cmdline.h` in the patch advertises defaults of 600 and 8. | Update the two help strings to 500 and 12. |
| Dispatcher regression | `TestXfInputDispatcher` deliberately leaves the calibration settings at zero, so it exercises the X11 fallback paths but does not currently prove their exact values. | Add behavioral assertions for 500 ms and 12 px through the production dispatcher. |
| Classifier regression and release smoke | `test_scroll_classifier.c` supplies 8 as its pan-slop model input; `build-release.sh` compiles and runs it before the smoke exit. | Model the user-facing pan slop as 12 and retain the independent fixed pinch deadband at 8. |
| Wrapper regression | `wrapper_production_check.sh` invokes normal mode but currently checks only that fallback mode is present. | Assert the exact default arguments while preserving the existing boundary and rejection cases. |
| Current documentation | `README.md` names 600 ms in the feature list and 600 ms / 8 px in the normal-launch section. | Update those current user-facing statements. |
| Historical planning evidence | Earlier phase plans, summaries, debug records, and the Phase 4 context describe the then-current defaults. | Leave these historical records unchanged; the current user directive supersedes only the old default literals. |

`PINCH_DEADBAND_PX` is a separate fixed pinch activation constant, not the user-facing touch-slop setting. It remains 8 per D-14.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@patches/onemix-touch.patch
@scripts/launch-touch.sh
@tests/wrapper_production_check.sh
@scripts/build-release.sh
@README.md

The current user request supersedes the numeric-default portion of historical D-13 and D-17: unset daily calibration is now 500 ms / 12 px. It does not supersede D-01/D-02 local-only gesture ownership, D-14’s fixed classifier constants, D-15’s two existing environment-variable names, D-16’s accepted ranges, or D-18–D-20’s wrapper and mouse-only contract.
</context>

<source_audit>
SOURCE | ID | Feature / constraint | Coverage | Status
--- | --- | --- | --- | ---
GOAL | user request | Default local-touch calibration becomes 500 ms long press and 12 px slop across wrapper, patch, regression, and documentation. | Tasks 1–2 | COVERED
REQ | GEST-01 | One-finger right-click threshold remains configurable within 500–700 ms. | Task 1 retains the parser/wrapper bounds and changes only the unset default. | COVERED
REQ | GEST-02 | Slop still controls the pre-threshold drag transition. | Task 1 proves the 12 px zero-value fallback through the production dispatcher. | COVERED
REQ | GEST-03 | Local-only two-finger scroll and three-finger middle-button translation retain their setting-backed claim threshold. | Tasks 1–2 update and exercise both pan fallback paths and the classifier model. | COVERED
REQ | CONF-01 | Canonical preset exposes existing calibration controls and mouse-only escape behavior. | Task 1 keeps names, validation, passthrough, and mouse-only semantics; Task 2 proves wrapper behavior. | COVERED
RESEARCH | project stack / STATE.md | Reuse Debian quilt, existing FreeRDP settings, the production XI2 dispatcher fixture, CTest, and the existing release smoke; add no dependency, daemon, setting, or parser. | Tasks 1–2 | COVERED
CONTEXT | D-01, D-02 | Native multitouch remains off and all touch remains in the client-owned local recognizer. | Task 1 modifies only existing local fallback values and keeps `+touch-pinch-wheel-fallback` / no `+multitouch`. | COVERED
CONTEXT | D-13, D-17 | The current user directive replaces the historical 600/8 default literals while preserving non-interactive launch behavior. | Tasks 1–2 | COVERED
CONTEXT | D-14 | Pinch/scroll classifier constants remain fixed and no classifier setting is exposed. | Task 2 retains `PINCH_DEADBAND_PX` at 8 and the 2.5 dominance ratio. | COVERED
CONTEXT | D-15, D-16 | Retain the two environment variable names and the 500–700 ms / 4–16 px validation ranges. | Task 1 retains validators and parser bounds; the wrapper regression keeps boundary and rejection coverage. | COVERED
CONTEXT | D-18, D-19, D-20 | The credential-free wrapper owns only the local preset and preserves `--mouse-only`. | Task 1 changes only default expansion values and uses the existing wrapper regression. | COVERED
</source_audit>

<dependency_graph>
Task 1 needs the current wrapper, integrated patch, and existing test fixtures. It creates the synchronized executable defaults and direct-dispatcher proof. Task 2 needs Task 1’s green workspace and refreshed patch, then updates the standalone classifier model and current documentation before proving the final tracked patch in a second clean extraction. The shared patch artifact makes these tasks sequential inside one Wave-1 atomic plan.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Set the canonical 500 ms / 12 px defaults through the wrapper and local XI2 fallback path</name>
  <files>scripts/launch-touch.sh, tests/wrapper_production_check.sh, patches/onemix-touch.patch (hunks for client/X11/xf_input.c, client/X11/test/TestXfInputDispatcher.c, and client/common/cmdline.h)</files>
  <precondition>The pinned descriptor `/home/hoang/freerdp-touch/src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc` and checksum-bound source archives exist, and the existing Debian build dependencies provide `dpkg-source`, `quilt`, CMake, CTest, a C compiler, and XInput2 headers.</precondition>
  <read_first>
    - `scripts/launch-touch.sh` and `tests/wrapper_production_check.sh` — retain the canonical validator, opaque argument forwarding, diagnostic behavior, and mouse-only bypass.
    - `patches/onemix-touch.patch` — locate every setting-backed zero fallback in `xf_input.c`, the existing local dispatcher fixture, and the two CLI help entries before editing.
    - `client/X11/xf_input.c`, `client/X11/test/TestXfInputDispatcher.c`, and `client/common/cmdline.h` in a private patched extraction — work from the actual source represented by the quilt patch rather than a copied model.
    - `client/common/cmdline.c` in that extraction — preserve the existing `500..700` and `4..16` parser conditions exactly.
  </read_first>
  <behavior>
    - RED: the existing normal-wrapper regression must require `/touch-long-press:500` and `/touch-slop:12` when both environment variables are absent; the unmodified wrapper fails that assertion.
    - RED: using the fixture’s existing zero-valued `rdpSettings`, the production dispatcher test must expect a 500 ms arm deadline, a 10 px one-finger update to remain below slop, a 13 px update to claim a left-button drag, a 10 px nearby second tap to reuse the first coordinate, and a 550 ms old tap anchor to be too old to reuse. These assertions fail against the former fallback values.
    - A coordinated two-finger or three-finger local translation below 12 px remains pending; after movement reaches the 12 px claim boundary it enters the existing scroll or middle-button-drag state. This exercises both `panDb` zero-value fallbacks without changing the pinch classifier.
    - Explicit values at 500, 600, and 700 ms plus 4, 8, and 16 px remain valid overrides. Invalid and mouse-only behavior remains covered by the existing wrapper fixture.
  </behavior>
  <action>
    Create a private writable extraction from the pinned DSC, copy the project patch into that extraction’s temporary Debian patch stack, append it only to the temporary `series`, apply the complete quilt stack, and configure an object directory with `BUILD_TESTING=ON`.

    Start RED in the existing tests. In `tests/wrapper_production_check.sh`, extend the normal-mode block to assert the two exact default command-line arguments in addition to its current fallback-mode assertion. Keep every existing valid-boundary loop, invalid-input case, diagnostic case, opaque-argument case, menu case, and `--mouse-only` case unchanged. In the private extraction’s `TestXfInputDispatcher.c`, extend the existing synthetic XI2-cookie fixture rather than adding a second executable or copied recognizer. Leave the settings’ two calibration fields unset and add isolated cases for the deadline, one-finger movement boundary, short-tap anchor distance and age, two-finger translation, and three-finger translation described in `<behavior>`. Record real `rdpInput.MouseEvent` output or the existing gesture fields at each branch boundary. Run the focused wrapper test and dispatcher CTest to establish failure before changing production values.

    Make the smallest value-only implementation change. In `scripts/launch-touch.sh`, set the two unset environment defaults to 500 and 12. Preserve `FREERDP_TOUCH_LONG_PRESS_MS`, `FREERDP_TOUCH_SLOP_PX`, both validation functions, the inclusive `500-700` and `4-16` checks, option order, argument passthrough, diagnostics, and mouse-only bypass per D-15, D-16, and D-18–D-20.

    In the private patched `xf_input.c`, update every zero-valued fallback that reads `FreeRDP_TouchLongPressDurationMs` to 500 and every zero-valued fallback that reads `FreeRDP_TouchLongPressSlopPx` to 12: the one-finger arm deadline, movement/drag decision, short-tap pair duration and distance, two-finger pan resolver, and three-finger pan resolver. This implements the current user directive that supersedes the numeric portion of D-13 and D-17 while retaining D-01/D-02 local-only routing. Retain the independent `PINCH_DEADBAND_PX` value of 8, `PINCH_DOMINANCE_RATIO` value of 2.5, wheel-step values, `+touch-pinch-wheel-fallback`, and no-`+multitouch` contract per D-14. Update only the two user-facing default strings in `client/common/cmdline.h`; keep the existing parser and settings interface unchanged.

    Add the changed source files to the active project quilt patch only where they are not already tracked, run `quilt refresh`, and copy the refreshed `onemix-touch.patch` back to the repository. Run the wrapper regression, assert the two parser range conditions are still present in the private source, then build and run the production dispatcher test to reach GREEN. Do not add an environment variable, CLI switch, setting field, configuration file, recognizer helper, native-touch route, or dependency.
  </action>
  <verify>
    <automated>bash /home/hoang/freerdp-touch/tests/wrapper_production_check.sh &amp;&amp; grep -Fq 'if (v &lt; 500 || v &gt; 700)' "$WORKDIR/client/common/cmdline.c" &amp;&amp; grep -Fq 'if (v &lt; 4 || v &gt; 16)' "$WORKDIR/client/common/cmdline.c" &amp;&amp; cmake --build "$WORKDIR/obj" --target TestXfInputDispatcher &amp;&amp; ctest --test-dir "$WORKDIR/obj" --output-on-failure -R '^TestXfInputDispatcher$'</automated>
  </verify>
  <done>The wrapper sends 500/12 when unset, all setting-backed X11 zero fallbacks use 500/12, CLI help reports the same values, the old explicit values remain valid overrides, and the expanded production-dispatcher test proves the one-, two-, and three-finger boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Align the classifier model and current documentation, then prove the shipping patch from a clean extraction</name>
  <files>README.md, patches/onemix-touch.patch (hunks for client/X11/test_scroll_classifier.c)</files>
  <precondition>Task 1 is green in its private patched workspace, and the source descriptor plus archive inputs under `/home/hoang/freerdp-touch/src/` remain readable.</precondition>
  <read_first>
    - `client/X11/test_scroll_classifier.c` in Task 1’s workspace — preserve its role as the release-smoke classifier model and distinguish its fixed pinch deadband from the setting-backed pan slop input.
    - `README.md` — update only current daily-default statements; retain the override example, accepted ranges, launch workflow, mouse-only guidance, and certificate-risk notice.
    - `scripts/build-release.sh` — preserve its fresh DSC extraction, full quilt application, classifier compilation, and `BUILD_RELEASE_SMOKE=1` no-publication behavior.
    - `patches/onemix-touch.patch` — retain every pre-existing hunk and Debian diff header while refreshing the final test change.
  </read_first>
  <behavior>
    - The standalone classifier model passes 12 as its pan-slop input: translations below that threshold remain pending, deliberate translation at or beyond the threshold claims scroll, and genuine stationary-midpoint pinch cases still claim pinch.
    - `PINCH_DEADBAND_PX` remains 8 and the 2.5 dominance ratio remains unchanged, proving that the requested touch-slop change does not retune the independent pinch classifier constants (D-14).
    - README’s feature list and normal daily-launch statement report 500 ms / 12 px; its valid ranges and existing environment variable names remain exactly as before.
    - A newly extracted, fully quilt-applied source tree passes the real XI2 dispatcher CTest, and the established release smoke compiles/runs the classifier before exiting without a package build or publication.
  </behavior>
  <action>
    In Task 1’s private source tree, revise the existing `test_scroll_classifier.c` cases so every modeled user-facing pan-slop argument is 12 rather than 8. Recalculate the translation cases and comments so sub-12 movement remains pending and a deterministic movement at or above the 12 px boundary claims scroll. Keep the fixed pinch deadband macro at 8, keep the 2.5 dominance ratio, and retain the true-pinch cases; this is a calibration-default synchronization, not a classifier retune.

    Update `README.md` at the one-finger gesture summary and the normal daily-launch defaults line from 600 ms / 8 px to 500 ms / 12 px. Leave the calibration override example and the accepted `500-700 ms` / `4-16 px` range statement intact. Leave historical phase plans, summaries, research, debug records, and the old Phase 4 decision record intact because they document the prior baseline rather than the current user-facing behavior.

    Refresh the active quilt patch and copy only the refreshed `onemix-touch.patch` to the repository. Create a second brand-new workspace from the pinned DSC, apply the complete Debian patch stack plus the refreshed project patch, configure `BUILD_TESTING=ON`, build and run `TestXfInputDispatcher`, then run the existing release smoke. Do not modify `scripts/build-release.sh`, package metadata, the menu, range validators, source pin, or release publication behavior.
  </action>
  <verify>
    <automated>git -C /home/hoang/freerdp-touch diff --check &amp;&amp; bash /home/hoang/freerdp-touch/tests/wrapper_production_check.sh &amp;&amp; cmake -S "$FRESH_WORKDIR" -B "$FRESH_WORKDIR/obj" -DBUILD_TESTING=ON &amp;&amp; cmake --build "$FRESH_WORKDIR/obj" --target TestXfInputDispatcher &amp;&amp; ctest --test-dir "$FRESH_WORKDIR/obj" --output-on-failure -R '^TestXfInputDispatcher$' &amp;&amp; BUILD_RELEASE_SMOKE=1 bash /home/hoang/freerdp-touch/scripts/build-release.sh</automated>
    <human-check>On the OneMix 3’s native X11 launch path, leave both calibration variables unset and use `menu` option 3. Confirm a stationary press reaches the right-click action at about half a second, ordinary slight finger jitter does not begin a drag, two-finger scrolling and three-finger middle-button drag retain their established behavior, and `FREERDP_TOUCH_LONG_PRESS_MS=600 FREERDP_TOUCH_SLOP_PX=8 menu` still launches as an explicit override.</human-check>
  </verify>
  <done>The tracked README, wrapper, CLI help, runtime fallbacks, dispatcher regression, and release-smoke classifier model agree on 500 ms / 12 px; the patch applies cleanly in a second pinned-source extraction and all existing relevant automated checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shell environment to launch wrapper | User-provided calibration strings become FreeRDP command-line arguments after the existing validator. |
| FreeRDP settings to local XI2 recognizer | Zero-valued settings select the source-level duration and movement thresholds that govern synthesized RDP mouse actions. |
| Private patched workspace to shipping quilt patch | Numeric source and regression changes become package source only through the tracked integrated patch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-ss0-01 | Tampering | `scripts/launch-touch.sh` calibration composition | medium | mitigate | Preserve digits-only, length, leading-zero, and inclusive range validation; extend the existing wrapper fixture to assert unset defaults and retain every valid/invalid boundary case. |
| T-ss0-02 | Tampering | `xf_input.c` zero-value setting fallbacks | medium | mitigate | Exercise exact 500/12 behavior through synthetic XI2 events at one-, two-, and three-finger boundaries in the production dispatcher fixture. |
| T-ss0-03 | Tampering | `patches/onemix-touch.patch` | medium | mitigate | Refresh via quilt, reapply after the complete Debian stack in a second clean extraction, run focused CTest, and execute the existing release smoke. |
| T-ss0-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install or new package is in scope; the plan reuses the existing Debian build toolchain. |
</threat_model>

<verification>
1. Run `tests/wrapper_production_check.sh`; unset-environment normal mode must log both 500/12 arguments, valid endpoint overrides including 600/8 must remain accepted, invalid values must still be rejected, and mouse-only must remain touch-option-free.
2. In Task 1’s patched workspace, run the production XI2 dispatcher CTest and assert both parser range checks are unchanged.
3. In a second clean DSC extraction, apply the final quilt patch, configure CTest, and run `TestXfInputDispatcher` through public `xf_input_handle_event` synthetic XI2 cookies.
4. Run `BUILD_RELEASE_SMOKE=1 scripts/build-release.sh`; it must compile and execute the refreshed classifier regression without building or publishing packages.
5. Perform the native-X11 manual check in Task 2 only after automated checks are green.
</verification>

<success_criteria>
- Unset wrapper invocation composes `+touch-pinch-wheel-fallback /touch-long-press:500 /touch-slop:12` with the existing forwarding behavior.
- Every settings-backed X11 fallback for duration or slop uses 500 or 12 as applicable; the independent pinch deadband remains 8.
- Accepted calibration ranges remain 500–700 ms and 4–16 px, and the two existing environment-variable names, CLI switches, parser fields, local-only routing, and mouse-only behavior are unchanged.
- Current CLI help and README accurately describe 500 ms / 12 px defaults.
- The one integrated patch applies through the full Debian quilt stack and passes the wrapper regression, dispatcher CTest, classifier smoke, and release smoke.
</success_criteria>

<output>
Create `.planning/quick/260810-ss0-i-m-c-nh-local-touch-calibration-long-pr/260810-ss0-SUMMARY.md` when done.
</output>
