---
mode: quick
phase: quick
plan: 260907-t28
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/menu
  - tests/menu_diagnostic_env_check.sh
  - tests/wrapper_production_check.sh
  - README.md
autonomous: true
requirements: [CONF-01]
user_setup:
  - service: local X11/XRandR display layout
    why: "The physical output identifiers are machine-specific and must be selected while the OneMix panel and external display are connected."
    env_vars:
      - name: FREERDP_ONEMIX_OUTPUT
        source: "The connected OneMix panel name shown by xrandr --query in the native startx session"
      - name: FREERDP_EXTERNAL_OUTPUT
        source: "The connected external-display name shown by xrandr --query in the native startx session"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "With two configured connected outputs, menu option 3 starts the existing native-X11 path with the OneMix panel marked primary, the external display positioned as secondary, and xfreerdp3 launched in multi-monitor mode."
    - "The launcher never relies on a fixed connector name: machine-specific output names are supplied by the user, checked against the target X server, and rejected clearly if absent, duplicated, or disconnected."
    - "Normal, diagnostic, and mouse-only launches retain the current TTY-to-startx-to-wrapper flow, OneMix rotation/touch transform, local-only touch preset, and credential handling."
    - "A user can verify the XRandR primary marker and two-display layout, then return to the OneMix-only launch by removing the external-output configuration."
  artifacts:
    - "scripts/menu configures named XRandR outputs in its private xinitrc and forwards /multimon only for a configured external display."
    - "tests/menu_diagnostic_env_check.sh and tests/wrapper_production_check.sh exercise the generated xinitrc with deterministic two-monitor XRandR fixtures."
    - "README.md documents output identification, persistent or one-shot configuration, physical verification, and the OneMix-only rollback."
  key_links:
    - "FREERDP_ONEMIX_OUTPUT/FREERDP_EXTERNAL_OUTPUT -> generated private xinitrc -> quoted xrandr layout commands -> xrandr --listmonitors confirmation."
    - "Configured external output -> opaque /multimon argument in menu -> scripts/launch-touch.sh passthrough -> installed /usr/bin/xfreerdp3."
    - "menu --mouse-only and FREERDP_TOUCH_DIAG=1 menu -> same display-layout path -> existing wrapper mode handling."
---

<objective>
Make the existing TTY/startx launcher run FreeRDP across the OneMix panel and one external monitor, with the OneMix panel as the XRandR primary display and the external display as its secondary neighbor.

Purpose: Reuse the established direct `xfreerdp3` launch path and native X11 display controls instead of adding a window manager, display daemon, or a second launcher.

Output: A connector-name-independent dual-monitor branch in `scripts/menu`, deterministic launcher regressions, and current operator guidance for setup, checking, and rollback.

Discovery level: 1. The existing menu, wrapper, X11 gate, documentation, and two launcher fixtures were inspected. The installed `xfreerdp3 /help` confirms `/multimon` and `/monitors` support; this work uses the existing `/f` fullscreen session with `/multimon` and adds no dependency or external integration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@scripts/menu
@scripts/launch-touch.sh
@scripts/check-x11-session.sh
@tests/menu_diagnostic_env_check.sh
@tests/wrapper_production_check.sh
@README.md

<interfaces>
- `scripts/menu` owns the private xinitrc, XRandR/XInput setup, non-secret FreeRDP session arguments, and the TTY password prompt.
- `scripts/launch-touch.sh` consumes only `--mouse-only`; every other argument is forwarded unchanged to `/usr/bin/xfreerdp3` after the existing native-X11 gate and local-touch preset composition.
- `/multimon` is valid in the existing fullscreen FreeRDP invocation. Do not use a static `/monitors:` list because monitor IDs are runtime-specific, and do not use `+span` because this task requires separate monitor geometry.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature / constraint | Coverage | Status
--- | --- | --- | --- | ---
GOAL | quick task | Run xfreerdp3 on the OneMix primary display plus one external secondary display, without assumed connector names, with verification and rollback. | Tasks 1–2 | COVERED
REQ | CONF-01 | The documented preset remains a local-only-touch launch with an explicit mouse-only path. | Task 1 preserves wrapper ownership and all three menu modes; Task 2 documents the result. | COVERED
RESEARCH | project stack + installed CLI help | Prefer native X11/XRandR and FreeRDP `/multimon`; add no package, daemon, window manager, or alternate client. | Task 1 | COVERED
CONTEXT | D-01, D-02 | Native multitouch stays disabled and the established local gesture behavior is not retuned. | Task 1 changes only XRandR/session arguments and leaves wrapper touch arguments intact. | COVERED
CONTEXT | D-03, D-05, D-06 | Diagnostic mode remains one opt-in wrapper path with its existing stderr/file behavior. | Task 1 proves the dual-display branch under diagnostic mode without changing diagnostic handling. | COVERED
CONTEXT | D-18, D-19, D-20 | The wrapper remains credential-free, owns only the touch preset, forwards session arguments, and retains `--mouse-only`. | Task 1 supplies `/multimon` as an opaque menu session argument and preserves the wrapper contract. | COVERED
CONTEXT | D-22, D-23, D-24 | Preserve the TTY password prompt, private startx xinitrc, display/touch setup, direct wrapper invocation, and normal/diagnostic/mouse-only menu flows. | Task 1 extends the existing xinitrc rather than changing its launch topology; Task 2 documents the same menu entry point. | COVERED
CONTEXT | D-25 | Keep the existing accepted certificate policy unchanged. | Task 1 retains the existing connection argument; Task 2 does not alter the security-warning or package-rollback guidance. | COVERED
</source_audit>

<dependency_graph>
Task 1 needs the current menu, wrapper, and generated-xinitrc fixtures. It creates a tested display-layout branch that feeds `/multimon` through the already-tested wrapper. Task 2 needs that behavior to be green so operator instructions name the exact environment variables, checks, and rollback. Both tasks share the menu contract and therefore execute sequentially inside this one Wave-1 plan.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route a named two-monitor XRandR layout through the existing menu and direct FreeRDP launch</name>
  <files>scripts/menu, tests/menu_diagnostic_env_check.sh, tests/wrapper_production_check.sh</files>
  <read_first>
    - `scripts/menu` — preserve its optional `--mouse-only` parser, password prompt, private xinitrc lifecycle, `XDG_SESSION_TYPE=x11`, existing panel mode/rotation, XInput transformation, wrapper call, and opaque connection arguments.
    - `scripts/launch-touch.sh` — preserve its one-flag interface and opaque FreeRDP-argument passthrough per D-18, D-19, and D-20.
    - `tests/menu_diagnostic_env_check.sh` and `tests/wrapper_production_check.sh` — extend their real-menu/private-xinitrc fixtures rather than creating a parallel launcher model.
    - `scripts/check-x11-session.sh` — the wrapper continues to enforce the native-X11 gate after menu display setup per D-22.
  </read_first>
  <behavior>
    - RED: a deterministic fixture with distinct connected panel and external output names requires the generated xinitrc to mark the panel primary, retain its 1600x2560 left rotation, place the external output to its right, and pass one `/multimon` argument to the wrapper. The current launcher cannot satisfy these assertions.
    - The normal, `FREERDP_TOUCH_DIAG=1`, and `--mouse-only` menu paths all use the same validated two-monitor setup and wrapper invocation; diagnostic propagation and mouse-only handling retain their current outcomes.
    - A missing primary name, identical primary/secondary names, or a name absent from the `xrandr --query` fixture stops before the wrapper process and prints the target X server's available outputs with a corrective error.
    - With `FREERDP_EXTERNAL_OUTPUT` unset but a valid OneMix output configured, the launcher applies the existing panel rotation/primary setup and omits `/multimon`; this is the runtime rollback path.
  </behavior>
  <action>
    Start RED by extending both existing launcher fixtures. Give their `xrandr` mocks deterministic `--query` output containing a named connected OneMix panel and a named connected external display; have layout invocations and `--listmonitors` calls record arguments. Supply `FREERDP_ONEMIX_OUTPUT` and `FREERDP_EXTERNAL_OUTPUT` in every menu execution that represents the two-monitor path. Assert the actual generated xinitrc invokes the wrapper once with `/multimon`, applies the existing panel mode and left rotation plus `--primary`, applies the external output with `--auto` and `--right-of` the panel, and retains the current normal, diagnostic, and mouse-only wrapper observations. Add invalid/missing/duplicate-output fixture cases that prove no wrapper call occurs. Run both scripts before changing the launcher so the new expectations fail.

    In `scripts/menu`, keep the direct TTY password prompt, private `mktemp` xinitrc, native-X11 export, existing XInput coordinate transformation, wrapper path, FreeRDP host/user/fullscreen/smart-sizing/scale/clipboard/audio/microphone/drive arguments, and existing certificate argument unchanged per D-22, D-23, D-24, and D-25. In the generated xinitrc, consume inherited `FREERDP_ONEMIX_OUTPUT` as the required panel name and `FREERDP_EXTERNAL_OUTPUT` as the optional external name. Never assign a connector-name fallback and never evaluate or source a user-controlled file. Quote every value supplied to `xrandr`; reject an unset required panel, equal names, and names that are not connected according to `xrandr --query`. For these errors, show the connected-output listing from the active private X server before exiting so the operator can identify the correct names.

    Configure the named panel with its existing 1600x2560/left-orientation behavior and `--primary`. When a validated external name is present, enable it with `--auto`, place it `--right-of` the OneMix panel, print `xrandr --listmonitors` for an operator-visible primary/secondary check, and append exactly one `/multimon` to the menu-owned FreeRDP session arguments before the unchanged wrapper call. When the external name is absent, retain the OneMix-only setup and do not add `/multimon`. Keep the existing wrapper touch options, `FREERDP_TOUCH_DIAG` propagation, and `--mouse-only` argument behavior unmodified per D-01, D-02, D-03, D-18, D-19, and D-20. Do not add Openbox, wmctrl, a display daemon, a second X server, a static monitor-ID list, or `+span`; the current direct launch and XRandR layout supply the required behavior.

    Reach GREEN by running both real-menu fixture scripts and shell syntax checking `scripts/menu`.
  </action>
  <verify>
    <automated>bash /home/hoang/freerdp-touch/tests/menu_diagnostic_env_check.sh &amp;&amp; bash /home/hoang/freerdp-touch/tests/wrapper_production_check.sh &amp;&amp; bash -n /home/hoang/freerdp-touch/scripts/menu</automated>
  </verify>
  <done>The actual menu-generated xinitrc validates named connected outputs, makes the OneMix panel primary, lays out the external display as secondary, and sends `/multimon` through the existing wrapper in every configured menu mode; fixture checks also prove clear rejection and OneMix-only fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document connector discovery, two-display verification, and the OneMix-only rollback</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — retain the launch modes, calibration section, existing package rollback, accepted certificate-risk notice, and explicit `menu --mouse-only` stock-launch guidance.
    - `scripts/menu` after Task 1 — document only the exact variable contract and layout behavior that the tested launcher implements.
  </read_first>
  <action>
    Add a focused dual-monitor subsection in README's existing Launch material. Explain that the configuration runs only through the current TTY `menu` option-3 path and depends on the native-X11 session already enforced by the wrapper. Show how to use `xrandr --query` with both displays connected to select the panel's connected name for `FREERDP_ONEMIX_OUTPUT` and the external display's connected name for `FREERDP_EXTERNAL_OUTPUT`; use placeholders rather than example hardware connector names. Provide both a one-shot environment invocation and an optional `~/.profile` export pair so normal `menu`, diagnostic `FREERDP_TOUCH_DIAG=1 menu`, and `menu --mouse-only` retain the established entry points after one-time configuration.

    State the exact resulting layout: the OneMix panel keeps its existing rotation and becomes XRandR primary; the external display uses its preferred mode to the panel's right; the fullscreen FreeRDP session receives `/multimon` so the two X11 monitor geometries remain separate. Include an on-device check using the `xrandr --listmonitors` output printed by the launch path, with the OneMix line carrying the primary marker and the external line present as the secondary display, followed by a remote-session check that both physical screens are active and pointer movement crosses in the configured direction.

    Document rollback without package changes: exit the RDP session so its private startx server stops, remove or unset `FREERDP_EXTERNAL_OUTPUT`, retain the validated OneMix output variable, and launch `menu` to return to the OneMix-only path without `/multimon`. Explain how to remove the optional profile export for persistence. Preserve all existing package rollback instructions, touch calibration guidance, local-only touch behavior, and D-25 security disclosure verbatim.
  </action>
  <verify>
    <automated>grep -Fq 'FREERDP_ONEMIX_OUTPUT' /home/hoang/freerdp-touch/README.md &amp;&amp; grep -Fq 'FREERDP_EXTERNAL_OUTPUT' /home/hoang/freerdp-touch/README.md &amp;&amp; grep -Fq 'xrandr --listmonitors' /home/hoang/freerdp-touch/README.md &amp;&amp; bash /home/hoang/freerdp-touch/tests/menu_diagnostic_env_check.sh &amp;&amp; bash /home/hoang/freerdp-touch/tests/wrapper_production_check.sh</automated>
    <human-check>On the OneMix with the external display connected, set the two names identified by the native-X11 `xrandr --query` output and run `menu`, then choose option 3. Confirm the printed monitor list marks the OneMix panel primary, the remote desktop occupies both physical displays with the external display on the documented side, and touch works on the OneMix panel. Exit the session, unset the external-output variable, rerun `menu`, and confirm the session returns to the OneMix-only layout without changing the installed package.</human-check>
  </verify>
  <done>README gives a machine-independent setup command, an optional persistent configuration, observable XRandR and remote-session checks, and a tested-path rollback to OneMix-only operation while leaving unrelated launch, touch, package, and security documentation intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User shell environment to generated xinitrc | Machine-specific display identifiers cross from user environment into XRandR command arguments. |
| Generated xinitrc to XRandR server | The private X server accepts display layout mutations before FreeRDP starts. |
| Menu session arguments to wrapper/client | The menu-owned `/multimon` argument passes through the credential-free wrapper to the installed FreeRDP client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-t28-01 | Tampering | `FREERDP_ONEMIX_OUTPUT` and `FREERDP_EXTERNAL_OUTPUT` consumption in `scripts/menu` | medium | mitigate | Treat names as opaque quoted values, do not source configuration files, reject absent/equal/disconnected values against the target `xrandr --query` output, and cover failures with real-xinitrc fixtures. |
| T-t28-02 | Denial of Service | Private XRandR layout before client launch | medium | mitigate | Fail before wrapper invocation when a requested display is unavailable, print the active X server's discoverable outputs, preserve the OneMix-only branch, and document removal of the external variable as rollback. |
| T-t28-03 | Tampering | Session-mode argument composition | medium | mitigate | Exercise normal, diagnostic, and mouse-only menu paths through the same generated xinitrc and assert one `/multimon` passthrough without changing wrapper-owned touch or credential behavior. |
| T-t28-04 | Spoofing | Existing RDP certificate policy | high | accept | Preserve the owner-accepted D-25 policy unchanged; this display-layout task neither changes certificate handling nor claims to resolve that accepted exposure. |
| T-t28-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install or new package is in scope; the plan uses installed XRandR, shell, and FreeRDP capabilities. |
</threat_model>

<verification>
1. Run both real-menu fixture scripts and shell syntax checking; they must exercise a named two-monitor xinitrc, all menu modes, input validation, and OneMix-only fallback without a physical X server.
2. Inspect the generated test logs to confirm the panel `--primary`, left rotation, external `--right-of`, and `/multimon` argument are all present in the same path.
3. On the target device, verify `xrandr --listmonitors` reports the OneMix as primary and the external display as secondary before assessing the remote desktop across both screens.
4. Unset the external name and relaunch through menu to verify the documented OneMix-only rollback; package state must remain unchanged.
</verification>

<success_criteria>
- The launcher accepts no assumed connector name: it requires the OneMix name, optionally accepts a distinct external name, and validates both in the private native-X11 session.
- With both names configured, XRandR makes OneMix primary, preserves its existing rotation/touch setup, places the external display to the right, and `xfreerdp3` receives `/multimon` through the unchanged wrapper.
- Normal, diagnostic, and mouse-only menu flows remain on the existing TTY/startx/direct-FreeRDP path with native multitouch still disabled.
- README tells the operator how to identify monitor names, configure and verify two displays, and reverse only the display layout without modifying packages.
</success_criteria>

<output>
Create `.planning/quick/260907-t28-thi-t-l-p-c-u-h-nh-ch-y-xfreerdp3-tr-n-h/260907-t28-SUMMARY.md` when done.
</output>
