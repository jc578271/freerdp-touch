---
phase: quick
plan: 260907-t28
subsystem: launch-documentation
tags: [xrandr, x11, freerdp, multimon, shell, documentation]

# Dependency graph
requires:
  - phase: quick-260907-t28-task-1
    provides: Named-output validation, XRandR layout, and /multimon passthrough in scripts/menu
provides:
  - Machine-independent dual-monitor setup and verification guidance
  - OneMix-only display-layout rollback guidance without package changes
affects: [launch operations, native-x11 verification, manual OneMix UAT]

# Actuals (#2632)
actuals:
  tokens: 4918
  tasks: 2
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Document machine-specific XRandR connector names through environment variables rather than fixed connector assumptions
    - Keep dual-monitor rollback separate from package rollback

key-files:
  created:
    - .planning/quick/260907-t28-thi-t-l-p-c-u-h-nh-ch-y-xfreerdp3-tr-n-h/260907-t28-SUMMARY.md
  modified:
    - README.md

key-decisions:
  - "Use placeholders for FREERDP_ONEMIX_OUTPUT and FREERDP_EXTERNAL_OUTPUT; connector names remain machine-specific."
  - "Keep persistent configuration optional in ~/.profile and retain the existing one-shot menu, diagnostic, and mouse-only entry points."
  - "Document removal of FREERDP_EXTERNAL_OUTPUT as the no-package-change rollback to the OneMix-only path."

patterns-established:
  - "README operator instructions mirror the tested scripts/menu contract: required OneMix output, optional distinct connected external output, and /multimon only when configured."

requirements-completed: [CONF-01]

coverage:
  - id: D1
    description: "README documents connector discovery, one-shot and persistent dual-monitor configuration, and the tested menu modes."
    requirement: CONF-01
    verification:
      - kind: other
        ref: "grep README.md for FREERDP_ONEMIX_OUTPUT, FREERDP_EXTERNAL_OUTPUT, and xrandr --listmonitors"
        status: pass
      - kind: integration
        ref: "bash tests/menu_diagnostic_env_check.sh"
        status: pass
      - kind: integration
        ref: "bash tests/wrapper_production_check.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "README documents XRandR primary/secondary verification, remote-session pointer crossing, and OneMix-only rollback."
    requirement: CONF-01
    verification:
      - kind: other
        ref: "bash -n scripts/menu && git diff --check -- README.md"
        status: pass
    human_judgment: false
  - id: D3
    description: "Physical native-X11, RDP, and touchscreen verification on the OneMix with an external display."
    verification: []
    human_judgment: true
    rationale: "Physical Xorg/RDP/touch UAT was not performed. The inspected interactive session is Wayland, and launching menu requires a live password-bearing RDP session. This remains a pending manual check, not a passed result."

# Metrics
duration: 3min
completed: 2026-09-07
status: complete
---

# Phase quick Plan 260907-t28: Dual-monitor launch documentation Summary

**README guidance now matches the named XRandR dual-monitor launcher and documents reversible OneMix-only operation.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-09-07T14:50:25Z
- **Completed:** 2026-09-07T14:52:57Z
- **Tasks:** 2
- **Files modified:** 1 implementation file

## Accomplishments

- Documented connector discovery through `xrandr --query` without assuming hardware-specific output names.
- Added one-shot and optional `~/.profile` configuration for normal, diagnostic, and mouse-only `menu` launches.
- Documented the exact XRandR primary/secondary layout, `/multimon` behavior, observable monitor-list check, remote-session check, and OneMix-only rollback.
- Preserved the existing package rollback, touch calibration, local-only touch, and accepted certificate-risk documentation.

## Task Commits

1. **Task 1: Route a named two-monitor XRandR layout through the existing menu and direct FreeRDP launch** - `a295c5f` (test), `4abcb52` (feat)
2. **Task 2: Document connector discovery, two-display verification, and the OneMix-only rollback** - `953afb3` (docs)

**Plan metadata:** Not committed; the main orchestrator owns planning-artifact commits.

## Files Created/Modified

- `README.md` - Dual-monitor connector discovery, configuration, verification, and OneMix-only rollback instructions.
- `.planning/quick/260907-t28-thi-t-l-p-c-u-h-nh-ch-y-xfreerdp3-tr-n-h/260907-t28-SUMMARY.md` - Execution summary and coverage record.

## Verification

- `grep` assertions for both output variables and `xrandr --listmonitors`: passed.
- `bash tests/menu_diagnostic_env_check.sh`: passed.
- `bash tests/wrapper_production_check.sh`: passed.
- `bash -n scripts/menu`: passed.
- `git diff --check -- README.md`: passed.

## Physical UAT Status

Physical native-X11, RDP, and touchscreen UAT was not performed. The inspected interactive session is Wayland, and launching `menu` requires a live password-bearing RDP session. The `xrandr --listmonitors` primary-marker check, two-screen remote-session check, pointer-crossing check, and touch check remain pending manual verification; none is recorded as passed.

## Decisions Made

- Connector names are documented as placeholders and must come from the connected-output lines of the operator's own native-X11 `xrandr --query` output.
- Removing or unsetting only `FREERDP_EXTERNAL_OUTPUT` is the documented display-layout rollback; package rollback remains a separate procedure.

## Deviations from Plan

None - Task 2 followed the plan exactly. Task 1 was already complete in commits `a295c5f` and `4abcb52` and was not rerun.

## Issues Encountered

None requiring code or documentation changes. Physical UAT is intentionally pending because this environment did not provide the required native-X11 desktop and live password-bearing RDP session.

## User Setup Required

Manual verification requires the OneMix panel and external display connected in a native X11 session, the two output names identified with `xrandr --query`, and a live RDP session launched through `menu` option 3.

## Next Phase Readiness

The operator documentation and automated launcher regressions are complete. Before treating the dual-monitor behavior as physically verified, run the pending native-X11/RDP/touch UAT and confirm the documented OneMix-only rollback.

---
*Plan: quick-260907-t28*
*Completed: 2026-09-07*

## Self-Check: PASSED

- Summary file exists at the required path.
- Commits `a295c5f`, `4abcb52`, and `953afb3` exist in git history.
- Task 2 commit `953afb3` contains only `README.md`.
