---
phase: 04-diagnostics-packaging-launch-configuration
plan: 260810-e0n
type: execute
wave: 1
depends_on: []
files_modified:
  - src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc
  - src/freerdp3_3.15.0+dfsg.orig.tar.xz
  - src/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz
  - scripts/build-release.sh
  - tests/build_release_signal_check.sh
  - .gitignore
  - rdp-debug-gestures.log
  - rdp-debug-native.log
autonomous: true
requirements: [PACK-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh checkout contains exactly the three pinned Debian source-package inputs under src/, while extracted worktrees and Debian binary outputs remain outside Git tracking."
    - "The release pipeline derives the base version and validates source metadata from the one local src/*.dsc descriptor, then passes that same descriptor to dpkg-source for fresh extraction without consulting configured APT source metadata."
    - "BUILD_RELEASE_SMOKE=1 proves the local-descriptor extraction, patch application, version assertion, and classifier stages without invoking the Debian package build or publishing dist/."
    - "Current diagnostic logs are absent from the Git index but remain on disk, and the broad diagnostic-log ignore rule prevents matching future logs from being added accidentally."
  artifacts:
    - path: src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc
      provides: "Tracked signed Debian source descriptor for the pinned FreeRDP package"
      contains: "Version: 3.15.0+dfsg-2.1+deb13u3"
    - path: src/freerdp3_3.15.0+dfsg.orig.tar.xz
      provides: "Tracked upstream source archive referenced by the descriptor"
    - path: src/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz
      provides: "Tracked Debian packaging archive referenced by the descriptor"
    - path: scripts/build-release.sh
      provides: "Local-DSC release validation, extraction, and pre-build smoke switch"
      contains: "BUILD_RELEASE_SMOKE"
    - path: tests/build_release_signal_check.sh
      provides: "Fixture regression for local DSC metadata parsing and smoke termination before package build"
      contains: "dpkg-parsecontrol"
    - path: .gitignore
      provides: "Narrow source-input exceptions and broad diagnostic-log exclusion"
      contains: "rdp-debug*.log"
  key_links:
    - from: scripts/build-release.sh
      to: src/*.dsc
      via: "The single discovered descriptor is parsed for Source, Version, and Build-Depends, then passed unchanged to dpkg-source -x."
      pattern: "dpkg-parsecontrol.*DSC|dpkg-source -x.*DSC"
    - from: src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc
      to: src/freerdp3_3.15.0+dfsg.orig.tar.xz
      via: "dpkg-source resolves and checksum-validates the adjacent archive named in the descriptor."
      pattern: "dpkg-source -x"
    - from: .gitignore
      to: Git index
      via: "Three later negation rules permit only the pinned inputs; the diagnostic wildcard and cached removals keep diagnostic logs out of subsequent commits."
      pattern: "!src/|rdp-debug\\*\\.log"
---

<objective>
Make the exact pinned Debian source package part of the repository's fresh-clone inputs, make the release script use that local descriptor as its sole source-version authority, and remove diagnostic logs from future Git tracking without deleting the developer's local copies.

Purpose: Reproducible builds must not rely on an ignored build workspace or whichever source version happens to be available through the local APT configuration. Diagnostic traces can contain sensitive session data and need forward protection in the working tree and index.
Output: Three tracked source-package inputs in `src/`, a local-DSC release smoke path with fixture coverage, narrow ignore exceptions, and cached-only removals for the two tracked diagnostic logs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-RESEARCH.md
@.gitignore
@scripts/build-release.sh
@tests/build_release_signal_check.sh
@build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc
@build/freerdp3_3.15.0+dfsg.orig.tar.xz
@build/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz
</context>

## Source Audit

| Source | ID | Scoped item | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | quick 260810-e0n | Fresh-clone release builds use the exact three pinned source-package inputs rather than ignored workspace inputs or mutable APT source resolution. | 1 | COVERED | The tracer copies the authoritative bytes and wires the descriptor through the release path. |
| REQ | PACK-01 | Reproducibly build from pinned `freerdp3-x11 3.15.0+dfsg-2.1+deb13u3` source using the quilt release path. | 1 | COVERED | Base version is read from the tracked descriptor; package suffix, quilt application, classifier, and normal package closure remain intact. |
| RESEARCH | Phase 04 packaging | Extract a clean tree with `dpkg-source`, preserve Debian's quilt stack, and keep shell orchestration limited to clean local inputs and selected outputs. | 1 | COVERED | The release script continues to use `dpkg-source -x`, `quilt`, and the existing explicit package closure. |
| CONTEXT | user task constraints | Keep source bytes exact, leave generated artifacts untracked, remove tracked diagnostic logs without history rewrite, and avoid touch/package/launcher behavior changes. | 1, 2 | COVERED | Task 1 handles source/script boundaries; Task 2 handles index hygiene and byte/index checks. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one local DSC the release pipeline's end-to-end source authority</name>
  <precondition>The local build environment resolves `dpkg-source`, `dpkg-parsecontrol`, `dpkg-parsechangelog`, `quilt`, `cc`, and `flock`; this task does not install or upgrade build dependencies.</precondition>
  <reversibility rating="costly">Tracking the pinned archives adds durable binary blobs to repository history; the user explicitly requires this reproducibility boundary, and changing it later needs a coordinated repository-retention decision.</reversibility>
  <files>src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc, src/freerdp3_3.15.0+dfsg.orig.tar.xz, src/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz, scripts/build-release.sh, tests/build_release_signal_check.sh</files>
  <read_first>
    - scripts/build-release.sh — preserve its lock, cleanup, quilt, classifier, expected local suffix, explicit four-package closure, checksum, and atomic publication behavior while replacing only the source-authority stage.
    - tests/build_release_signal_check.sh — extend its existing isolated shell-fixture pattern rather than adding a second release-script harness.
    - build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc — the local `Source`, `Version`, `Build-Depends`, and archive references define the descriptor contract.
    - .planning/phases/04-diagnostics-packaging-launch-configuration/04-RESEARCH.md — PACK-01 requires clean `dpkg-source` extraction and preservation of the Debian quilt stack.
  </read_first>
  <behavior>
    - A fixture with one valid descriptor under `src/` observes `dpkg-parsecontrol` requests for `Source`, `Version`, and `Build-Depends`, and observes `dpkg-source -x` receiving that same descriptor path.
    - The parsed base version produces the unchanged `+onemix1` expected changelog/package version for the pinned descriptor.
    - With `BUILD_RELEASE_SMOKE=1`, the fixture reaches fresh extraction, quilt application, changelog assertion, and classifier success, exits successfully before the package-build shim can run, and leaves no published bundle.
    - The fixture's failing sentinel for the former repository-source lookup is never reached.
  </behavior>
  <action>
    Start RED by adapting `tests/build_release_signal_check.sh` before changing the release script. Replace its source-metadata shim with a `dpkg-parsecontrol` shim that records and returns values for `Source`, `Version`, and `Build-Depends`; make the fixture source directory `src/` and create the descriptor plus both companion archive placeholders there. Retain all three existing TERM/publication cases. Add one synchronous smoke case whose package-build shim writes a failure marker if invoked, then assert the parser field calls, exact descriptor handoff to the extraction shim, successful smoke exit, absent build marker, and absent new dist bundle. Provide a failure sentinel for the obsolete repository-source command so this regression fails if that call returns.

    Create `src/` and copy the three listed Debian source-package inputs from `build/` with `cp --preserve=mode,timestamps`; use `cmp -s` immediately after each copy. Leave the ignored `build/` workspace, extracted trees, and existing binary outputs in place; do not copy any extracted source tree or `.deb` into `src/`.

    Move `REPO_ROOT` resolution ahead of source validation in `scripts/build-release.sh`. Discover exactly one readable `src/*.dsc` using Bash array/glob handling that rejects zero or multiple descriptors. Parse that descriptor with `dpkg-parsecontrol` for `Source`, `Version`, and `Build-Depends`; fail before extraction unless the source is exactly `freerdp3` and the parsed version and dependency declaration are nonempty. Use the parsed base version to form the existing expected `+onemix1` version, and pass the identical descriptor variable to `dpkg-source -x`. Remove the cache-based source-version resolution entirely; no configured APT source metadata may affect the release result.

    Add a `BUILD_RELEASE_SMOKE=1` branch immediately after the classifier passes and before the package-build stage. It must print an unambiguous successful smoke message and exit through the existing EXIT cleanup path, without creating a bundle, swapping `dist`, or changing normal invocations that omit the variable. Preserve the touch source, quilt patch contents, package metadata/suffix, launcher behavior, package selection, and publication logic. The active-reference search identifies only this script and its shell fixture; do not modify historical planning records or unrelated user documentation.
  </action>
  <verify>
    <automated>set -euo pipefail
cd /home/hoang/freerdp-touch
bash tests/build_release_signal_check.sh
smoke_output=$(mktemp)
trap 'rm -f "$smoke_output"' EXIT
BUILD_RELEASE_SMOKE=1 ./scripts/build-release.sh &gt;"$smoke_output" 2&gt;&amp;1
grep -Fq 'Local source pin-check OK' "$smoke_output"
grep -Fq 'Fresh extraction OK.' "$smoke_output"
grep -Fq 'Patch applied OK.' "$smoke_output"
grep -Fq 'Classifier check: OK.' "$smoke_output"
grep -Fq 'Pre-build smoke completed' "$smoke_output"
git diff --check</automated>
  </verify>
  <done>The three byte-identical Debian source inputs exist under `src/`. The release script selects exactly one local descriptor, validates its required metadata, derives the base version locally, and uses that descriptor for extraction. Its smoke switch exercises all pre-build release stages and returns before the full package build; the existing shell fixture proves the descriptor/metadata handoff, smoke boundary, and retained signal-safety cases.</done>
</task>

<task type="auto">
  <name>Task 2: Track only the pinned inputs and remove diagnostic traces from the index</name>
  <files>.gitignore, rdp-debug-gestures.log, rdp-debug-native.log</files>
  <read_first>
    - .gitignore — retain generic exclusions for `build/`, Debian artifacts, release outputs, and the existing launch helper.
    - Git index entries for rdp-debug-gestures.log and rdp-debug-native.log — remove only index ownership and never inspect or print their diagnostic content.
    - src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc — verify the descriptor remains the same bytes copied in Task 1 before staging it.
  </read_first>
  <action>
    Keep the generic Debian artifact exclusions in `.gitignore`, then add later root-relative negation rules for exactly `src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc`, `src/freerdp3_3.15.0+dfsg.orig.tar.xz`, and `src/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz`. Replace the narrow diagnostic-log rule with the broad `rdp-debug*.log` rule so the current base log and named diagnostic variants remain excluded without allowing other generated Debian artifacts.

    Run cached-only removal for `rdp-debug-gestures.log` and `rdp-debug-native.log`, then assert both regular files still exist locally. Do not read their contents, delete them from disk, or rewrite Git history. Explicitly stage the `.gitignore` update and the three source inputs after the negation rules are present, so the final commit contains precisely those `src/` artifacts while the two diagnostic paths are recorded as index deletions.

    Run the migration checks after staging: compare each `src/` file with its original `build/` counterpart byte-for-byte; prove the three inputs are unignored and tracked; prove the diagnostic wildcard matches both existing variants and a hypothetical later variant; prove the two current logs are no longer index entries while still present locally; and prove no path below `build/` or ending in `.deb` is tracked. Re-run the release fixture so its local descriptor and smoke checks remain green after the Git-hygiene changes.
  </action>
  <verify>
    <automated>set -euo pipefail
cd /home/hoang/freerdp-touch
for name in \
  freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc \
  freerdp3_3.15.0+dfsg.orig.tar.xz \
  freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz
do
  cmp -s "build/$name" "src/$name"
  if git check-ignore -q --no-index "src/$name"; then
    printf 'FAIL: intended source input is ignored: %s\n' "$name" >&amp;2
    exit 1
  fi
done
for log in rdp-debug-gestures.log rdp-debug-native.log rdp-debug-future.log
do
  git check-ignore -q --no-index "$log"
done
for log in rdp-debug-gestures.log rdp-debug-native.log
do
  test -f "$log"
  if git ls-files --error-unmatch -- "$log" &gt;/dev/null 2&gt;&amp;1; then
    printf 'FAIL: diagnostic log remains tracked: %s\n' "$log" >&amp;2
    exit 1
  fi
done
python3 -c 'import subprocess; expected={"src/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc", "src/freerdp3_3.15.0+dfsg.orig.tar.xz", "src/freerdp3_3.15.0+dfsg-2.1+deb13u3.debian.tar.xz"}; tracked=set(subprocess.check_output(["git", "ls-files"], text=True).splitlines()); assert expected <= tracked, sorted(expected-tracked); assert not {"rdp-debug-gestures.log", "rdp-debug-native.log"} &amp; tracked; assert not any(path.startswith("build/") or path.endswith(".deb") for path in tracked)'
bash tests/build_release_signal_check.sh
git diff --cached --check
git diff --check</automated>
  </verify>
  <done>The exact three source-package inputs are trackable and staged under `src/`, with no extracted workspace or Debian package binary entering the index. Both previously tracked diagnostic logs remain present on disk but are absent from the index, and the broad ignore rule protects matching diagnostic files created later. Byte comparison, Git status/ignore checks, the local DSC metadata simulation, the pre-build smoke case, and release signal regression all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tracked `src/` inputs → release pipeline | Repository-controlled Debian source inputs determine what code `dpkg-source` extracts and packages. |
| Local source descriptor → shell metadata handling | The release script consumes `Source`, `Version`, and `Build-Depends` from a file rather than an APT source-cache response. |
| Diagnostic files → Git index | Touch/session logs can contain sensitive operational data when an index operation accidentally captures them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260810-e0n-01 | Tampering | `src/*.dsc` and adjacent source archives | medium | mitigate | Require one readable descriptor, validate its expected source name and nonempty metadata, and hand that exact path to `dpkg-source`, which validates descriptor-referenced inputs during extraction. |
| T-260810-e0n-02 | Tampering | `scripts/build-release.sh` source selection | medium | mitigate | Remove dependency on mutable APT source metadata; derive the base version from the repository-tracked descriptor and fixture-test the parser/extraction handoff. |
| T-260810-e0n-03 | Information Disclosure | `rdp-debug-gestures.log` and `rdp-debug-native.log` | high | mitigate | Remove both paths from the current index without deleting local files and use the broad diagnostic wildcard to prevent recurrence. Historical blobs are deliberately left untouched under the stated no-history-rewrite boundary. |
| T-260810-e0n-04 | Repudiation | Source provenance and release smoke evidence | low | accept | Git tracks the exact input filenames and the descriptor carries archive checksums; the focused fixture records the local parse/extraction contract without introducing a separate provenance service. |
</threat_model>

<verification>
- `cmp -s` proves the three new inputs match the current authoritative source-package files byte-for-byte before the source copies are staged.
- The release fixture exercises all existing signal-publication cases plus local descriptor metadata parsing and a smoke run whose build shim must remain unused.
- A real `BUILD_RELEASE_SMOKE=1 ./scripts/build-release.sh` reaches extraction, patch application, version assertion, and classifier success but does not start package construction or dist publication.
- Git checks confirm the source inputs are unignored/tracked, diagnostic logs are ignored/untracked/present locally, and no extracted `build/` path or `.deb` appears in the index.
</verification>

<success_criteria>
1. A repository clone has all source artifacts needed for `dpkg-source -x` beside one tracked FreeRDP descriptor, with their current bytes preserved exactly.
2. `scripts/build-release.sh` does not rely on APT source resolution for the package version; it uses a validated local descriptor for parsing and extraction while retaining the current `+onemix1` release version and package closure.
3. The smoke option gives a fast, non-publishing pre-build release check, and fixture coverage fails if the full package-build boundary is crossed.
4. The current diagnostic traces no longer appear in new commits, remain on the developer machine, and matching future traces are ignored.
5. No touch implementation, quilt patch, Debian package version, launcher, extracted workspace, or generated Debian output changes as part of this quick task.
</success_criteria>

<output>
Create `.planning/quick/260810-e0n-create-src-containing-the-three-pinned-d/260810-e0n-SUMMARY.md` when done.
</output>
