---
phase: 03-gestures-session-stability
plan: 260808-956
subsystem: gesture-state-machine
tags: [gestures, scroll, pan, touch, local-only]
requires: ["260807-oz9"]
provides: ["two-finger-scroll", "three-finger-pan"]
affects: [GEST-04]
tech-stack:
  added: []
  patterns: [state-machine, scoped-gate, centroid-tracking]
key-files:
  created: []
  modified:
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
    - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h
decisions:
  - "Two-finger pan reassigned from middle-button PAN to wheel scroll (scrollActive)"
  - "Three-finger pan uses three-finger centroid for middle-button drag (midActive)"
  - "Third finger during two-finger PENDING upgrades to three-finger pan; during CLAIMED aborts"
  - "SCROLL_WHEEL_STEP_PX = 40px per detent mirrors PINCH_WHEEL_STEP_PX"
  - "force_cancel owns all new state: scrollActive, threeFingerPending, midFingerC"
  - "MidActive paths (update/end/force_cancel) extended to three-finger centroid"
metrics:
  duration: 511
  completed_date: "2026-08-08"
status: complete
actuals:
  tokens: 15000
  tasks: 2
  commits: 1
---

# Phase 03 Plan 260808-956: Two-Finger Scroll, Three-Finger Middle-Button Pan Summary

Reassigned local-only gesture fallback: two-finger pan synthesizes mouse-wheel scroll detents, three-finger pan performs middle-button press-and-drag via three-finger centroid. Pinch and one-finger behavior preserved.

## Tasks Executed

### Task 1: Reassign two-finger pan to wheel scroll; add three-finger middle-button pan

Changed the PAN claim branch in `xf_input_two_finger_resolve` from `midActive`/`PTR_FLAGS_BUTTON3` to `scrollActive` (wheel detents). Added `xf_input_three_finger_resolve` helper that claims `midActive` at the three-finger centroid. Added upgrade path in the PENDING gate: a third finger arriving while two-finger is still PENDING (before pinch/scroll claimed) promotes to three-finger pan instead of aborting. Extended `midActive` update/end blocks for three-finger centroid tracking. Extended `xf_touch_force_cancel` to clear and quarantine `scrollActive`, `threeFingerPending`, and `midFingerC`. Pinch, one-finger tap/drag/long-press paths untouched.

**Files:** `build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c`, `build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h`

**Build:** `[100%] Built target xfreerdp` with no new errors. Binary exists at `obj-x86_64-linux-gnu/client/X11/xfreerdp3`. `run-rdp.sh` syntax ok. `/help` lists `+touch-pinch-wheel-fallback`.

### Task 2: Static verification of the contact-count branches

All 7 static checks pass:

1. `xf_input_three_finger_resolve` is defined: present
2. `three-finger: pending upgrade` log: present
3. `pan: claimed two-finger scroll` log: present
4. `pan: claimed three-finger middle-button drag` log: present
5. `scrollActive` referenced in source: present
6. `midFingerC` referenced in source: present
7. Old `pan: claimed middle-button drag` log: absent

## Verification

- Build: passes (cmake build reaches 100%, binary exists, run-rdp.sh syntax ok)
- Static: all 7 grep checks pass; two-finger resolve no longer references BUTTON3; three-finger resolve does
- Hardware UAT: deferred (agent cannot run)

## Deviations from Plan

### Note: Commit handling

The source files live under the gitignored `build/` directory per the project's `.gitignore`. The initial task commit (`0a626f5`) force-added these gitignored files -- this was removed with `git reset --mixed ff670ce`. Source edits remain intact in the gitignored build tree. Per project convention, implementation is recorded as `build-tree`.

## Known Stubs

None -- all gesture state paths are wired end to end.

## Threat Flags

None -- no new network endpoints, auth paths, file access, or schema changes. All new gesture states route through the existing `xf_touch_force_cancel` release-and-quarantine seam per threat model mitigations T-956-01 and T-956-02.

## Self-Check: PASSED

- [x] `build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c` exists with edits intact
- [x] `build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h` exists with edits intact
- [x] Binary at `obj-x86_64-linux-gnu/client/X11/xfreerdp3` is executable
- [x] All 7 static checks print OK
- [x] Build reaches 100% with no new errors
