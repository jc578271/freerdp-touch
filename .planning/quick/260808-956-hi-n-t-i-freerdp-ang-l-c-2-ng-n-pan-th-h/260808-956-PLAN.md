---
phase: 03-gestures-session-stability
plan: 260808-956
type: execute
wave: 1
depends_on: []
files_modified:
  - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
  - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h
autonomous: true
requirements: [GEST-04]
estimate:
  tokens: 52000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Two-finger midpoint translation (pan) emits mouse-wheel scroll detents, never presses the middle button.
    - Three-finger midpoint translation presses and holds the middle button (BUTTON3) and drags the remote pointer with the three-finger centroid.
    - Two-finger pinch (distance change) still emits Ctrl+wheel zoom unchanged.
    - One-finger tap/drag/long-press behavior is unchanged.
    - A third finger arriving while a two-finger gesture is still PENDING upgrades to three-finger pan rather than aborting; a finger arriving after a gesture is CLAIMED (pinch, scroll, or midActive) still aborts via xf_touch_force_cancel.
    - Every new gesture state is released and quarantined by xf_touch_force_cancel (FocusOut / lifecycle / abort) so no button or scroll state sticks.
  artifacts:
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c (two-finger pan converted to wheel scroll; new three-finger middle-button pan; gate, resolve, update, end, force_cancel updated)
    - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h (new state fields)
  key_links:
    - xf_input_two_finger_resolve PAN claim branch -> scrollActive (was midActive/BUTTON3)
    - new xf_input_three_finger_resolve -> midActive (BUTTON3 down at 3-finger centroid)
    - xf_touch_force_cancel -> releases scrollActive + threeFingerPending + midFingerC quarantine
---

<objective>
Reassign the local-only fallback gesture layer so that two-finger pan synthesizes mouse-wheel scrolling and three-finger pan performs the existing middle-button-hold-plus-pointer-drag. Pinch and one-finger behavior are preserved.

Purpose: The user changed the intended mapping — two fingers should scroll (lăn chuột), three fingers should hold the wheel and drag. This keeps the gesture state machine in xf_input.c and reuses the existing middle-button pan code path, now triggered by three fingers instead of two.
Output: Patched xf_input.c and xfreerdp.h that build cleanly and pass static checks for the new contact-count branches.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260807-oz9-t-i-kh-ng-mu-n-mutitouch-native-n-a-t-i-/260807-oz9-SUMMARY.md

Source of truth (read these before editing):
- build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c  — xf_input_two_finger_resolve (lines ~858-931), the pinch/pan/PENDING gate (lines ~943-989), the main switch TouchBegin/Update/End (lines ~991-1239), xf_touch_force_cancel (lines ~1293-1414), xf_input_init state-zero block (lines ~300-341).
- build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h  — WITH_XI gesture state block (lines ~318-341).

Key facts already verified in code:
- Wheel detent pattern: `freerdp_client_send_wheel_event(&xfc->common, PTR_FLAGS_WHEEL | 0x78)` up; `PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78)` down. One detent = 0x78 (120).
- Middle-button pan: `PTR_FLAGS_DOWN | PTR_FLAGS_BUTTON3` to claim, `PTR_FLAGS_MOVE` to drag, `PTR_FLAGS_BUTTON3` to release.
- Pan deadband: read from `FreeRDP_TouchLongPressSlopPx` (default 8) in xf_input_two_finger_resolve — reuse for both 2-finger scroll and 3-finger pan claim thresholds.
- `twoStartMidX/Y` holds the gesture baseline midpoint (set at two-finger Begin); reuse as the three-finger centroid baseline at upgrade time.
- `pinchFingerA/B` and their Ax/Ay/Bx/By store the first two fingers and are reused for scroll and 3-finger pan tracking.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reassign two-finger pan to wheel scroll; add three-finger middle-button pan</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c, build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h</files>
  <behavior>
    - Two fingers move together (midpoint translation past pan deadband, distance stable): wheel scroll emits, no BUTTON3 event. Vertical midpoint delta accumulates into up/down wheel detents.
    - Two fingers change distance (pinch): Ctrl+wheel zoom — unchanged from today.
    - Third finger lands while the two-finger gesture is still PENDING (before pinch/scroll claimed): state upgrades to three-finger pan; first movement presses middle button and drags pointer by three-finger centroid.
    - Third finger lands after a gesture is CLAIMED (pinchActive, scrollActive, or midActive): xf_touch_force_cancel fires and all participating IDs quarantine (existing abort behavior preserved).
    - Fourth finger in any active/pending multi-finger state: abort via force_cancel.
    - Releasing the first of the two scroll fingers ends the scroll (no button release); releasing any of the three midActive fingers releases BUTTON3; the remaining fingers quarantine.
    - xf_touch_force_cancel (lifecycle abort) releases scrollActive, threeFingerPending, midActive, and quarantines midFingerC alongside A/B.
  </behavior>
  <action>
    Add near the PINCH_* defines a `#define SCROLL_WHEEL_STEP_PX 40` (midpoint pixels per wheel detent), mirroring PINCH_WHEEL_STEP_PX.

    **xfreerdp.h (WITH_XI block, after the existing two-finger fields):** add three-finger pan state and two-finger scroll state:
    `BOOL threeFingerPending;` `int midFingerC;` `INT32 midFingerCx, midFingerCy;` `BOOL scrollActive;` `INT32 scrollLastY;` `INT32 scrollAccum;`
    Initialize all of these to FALSE/0 in the xf_input_init state-zero block (right after the existing `twoStartMidY = 0;` line).

    **xf_input_two_finger_resolve — PAN claim branch (the `if (panDelta >= panDb)` block):** replace the middle-button claim with a scroll claim. Instead of setting midActive and sending `PTR_FLAGS_DOWN | PTR_FLAGS_BUTTON3`, set `xfc->scrollActive = TRUE`, `xfc->scrollLastY = currentMidY`, `xfc->scrollAccum = 0`. Emit NO button event at claim time (the deadband absorbed the initial movement; subsequent updates produce detents). Change the log to `"pan: claimed two-finger scroll d=%.1f mid=(%d,%d)"`. Leave the PINCH claim branch untouched.

    **New helper xf_input_three_finger_resolve(xfc, touchId, x, y):** mirror xf_input_two_finger_resolve's finger-update preamble, but also update `midFingerCx/Cy` when `touchId == xfc->midFingerC`. Compute the three-finger centroid `midX=(Ax+Bx+Cx)/3`, `midY=(Ay+By+Cy)/3`. Read the same pan deadband (FreeRDP_TouchLongPressSlopPx, default 8). If `hypot(midX - twoStartMidX, midY - twoStartMidY) >= panDb`, claim: set `threeFingerPending=FALSE`, `midActive=TRUE`, `midLastX=midX`, `midLastY=midY`, send `PTR_FLAGS_DOWN | PTR_FLAGS_BUTTON3` at the centroid, log `"pan: claimed three-finger middle-button drag d=%.1f mid=(%d,%d)"`. Otherwise stay pending. Return 0. This helper reuses midActive for the active 3-finger pan, so the existing midActive drag/release code path applies once claimed.

    **PENDING gate (top of xf_input_touch_fallback, the `if (pinchActive || midActive || twoFingerPending)` block):** widen the condition to `|| xfc->scrollActive || xfc->threeFingerPending`. In its XI_TouchBegin case, BEFORE the existing third-finger abort, insert the upgrade path: `if (xfc->twoFingerPending)` (only reached when not yet pinch/scroll — resolve clears twoFingerPending when claiming) then record `midFingerC=touchId`, `midFingerCx=x`, `midFingerCy=y`, set the three-finger centroid baseline `twoStartMidX=(Ax+Bx+Cx)/3`, `twoStartMidY=(Ay+By+Cy)/3`, clear `twoFingerPending=FALSE`, set `threeFingerPending=TRUE`, log `"three-finger: pending upgrade C=%d"`, return 0. The existing abort code after this branch now only fires for claimed states and 4th fingers — extend its quarantine list from `{fingerA, fingerB, touchId}` to `{fingerA, fingerB, fingerC, touchId}` (include `xfc->midFingerC`), skipping zeros. In the gate's XI_TouchUpdate case, keep only the existing `if (pinchActive) { pinch_update; return 0; }` early return; let scrollActive, threeFingerPending, midActive, twoFingerPending fall through to the main switch. In the gate's XI_TouchEnd case, keep only the existing `if (pinchActive && ...)` pinch_cleanup early return; let everything else fall through.

    **Main switch XI_TouchUpdate:** insert, in order, at the top (before the existing twoFingerPending check):
      1. Three-finger PENDING resolve: `if (xfc->threeFingerPending) return xf_input_three_finger_resolve(xfc, touchId, x, y);`
      2. Active 2-finger scroll: `if (xfc->scrollActive)` — update the moving finger's stored position for A or B (reject any other touchId with `return 0`), compute `midY=(Ay+By)/2`, `deltaY = midY - xfc->scrollLastY`, `xfc->scrollAccum += deltaY`, then emit wheel detents in a `while (abs(scrollAccum) >= SCROLL_WHEEL_STEP_PX)` loop: positive scrollAccum → `PTR_FLAGS_WHEEL | 0x78` (up), decrement by step; negative → `PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78)` (down), increment by step. Set `scrollLastY = midY`, return 0.
    Then the existing `twoFingerPending` and `midActive` blocks follow. In the existing midActive block, extend finger tracking to three fingers: add an `else if (touchId == xfc->midFingerC) { midFingerCx=x; midFingerCy=y; }` before the `else return 0;`, and change the midpoint to the three-finger centroid `(Ax+Bx+Cx)/3, (Ay+By+Cy)/3`. Keep the `PTR_FLAGS_MOVE` + midLast update unchanged.

    **Main switch XI_TouchEnd:** add three new blocks BEFORE the existing twoFingerPending block, in this order:
      1. Three-finger PENDING lift: `if (xfc->threeFingerPending)` — if the lifted id is A, B, or C, clear `threeFingerPending=FALSE`, `midFingerC=0`, `midFingerCx=0`, `midFingerCy=0`, `pinchFingerA=0`, `pinchFingerB=0`, `twoStartMidX=0`, `twoStartMidY=0`, then quarantine the two remaining IDs among {A,B,C minus lifted} into quarantinedFingers and arm recoveryGate. Return 0.
      2. Active 2-finger scroll lift: `if (xfc->scrollActive)` — if lifted id is A or B, clear `scrollActive=FALSE`, `scrollLastY=0`, `scrollAccum=0`, `pinchFingerA=0`, `pinchFingerB=0`, `twoStartMidX=0`, `twoStartMidY=0`, quarantine the one remaining finger, log `"scroll: released id=%d"`. NO button release. Return 0.
      3. (existing twoFingerPending and midActive blocks follow.)
    In the existing midActive lift block, add `midFingerC=0; midFingerCx=0; midFingerCy=0;` to the clear section and quarantine the two remaining fingers among {A,B,C minus lifted} instead of just one.

    **xf_touch_force_cancel:** add `const int savedMidFingerC = xfc->midFingerC;` and `const BOOL hadScroll = xfc->scrollActive;` `const BOOL hadThree = xfc->threeFingerPending;` to the saved-flags preamble. No button release is needed for scrollActive (it only sends wheel events) — but DO clear it in the clear-state section: add `xfc->scrollActive=FALSE; xfc->scrollLastY=0; xfc->scrollAccum=0; xfc->threeFingerPending=FALSE; xfc->midFingerC=0; xfc->midFingerCx=0; xfc->midFingerCy=0;`. Widen the quarantine condition `if (hadPinch || hadPending || hadMid)` to also include `|| hadScroll || hadThree`, and inside it append `savedMidFingerC` to quarantinedFingers when non-zero (with the existing dup guard pattern). Update the force_cancel diagnostic log to include `scroll=%d three=%d`.

    Do NOT touch the pinch claim/update/cleanup path, the one-finger long-press/click/drag path, register_input_events, xf_input_touch_remote, cmdline.c, or any settings slot — those are unchanged by this reassignment.
  </action>
  <verify>
    <automated>cd /home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg && cmake -B obj-x86_64-linux-gnu -S . && cmake --build obj-x86_64-linux-gnu --target xfreerdp && test -x obj-x86_64-linux-gnu/client/X11/xfreerdp3 && bash -n /home/hoang/freerdp-touch/run-rdp.sh && obj-x86_64-linux-gnu/client/X11/xfreerdp3 /help 2>&1 | grep -q 'touch-pinch-wheel-fallback'</automated>
  </verify>
  <done>
    Build reaches `[100%] Built target xfreerdp` with no errors; xfreerdp3 binary exists and is executable; run-rdp.sh syntax checks; `/help` still lists `+touch-pinch-wheel-fallback`. Two-finger pan no longer references BUTTON3 in its claim path; three-finger pan does. force_cancel clears and quarantines the new state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Static verification of the contact-count branches</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c</files>
  <action>
    After Task 1 builds, run targeted static checks on the built source to confirm the non-trivial contact-count branches are present and the old two-finger middle-button claim is gone. This is the smallest runnable verification for the 2-vs-3 finger branch, since hardware touch UAT cannot run on the agent.

    Confirm (each must be non-zero / present):
    - `xf_input_three_finger_resolve` is defined exactly once (grep -c the function signature).
    - A `three-finger: pending upgrade` log string exists (the upgrade path).
    - A `pan: claimed two-finger scroll` log string exists (2-finger scroll claim).
    - A `pan: claimed three-finger middle-button drag` log string exists (3-finger pan claim).
    - `scrollActive` is referenced in xf_touch_force_cancel's clear section (lifecycle releases scroll).
    - `midFingerC` is referenced in xf_touch_force_cancel's quarantine section.
    - The two-finger PAN claim branch no longer contains `PTR_FLAGS_DOWN | PTR_FLAGS_BUTTON3` combined with `midActive` — the only `PTR_FLAGS_DOWN | PTR_FLAGS_BUTTON3` occurrences now belong to the three-finger resolve and (if any remain) none belong to the two-finger resolve. Concretely: the string `pan: claimed middle-button drag` (the OLD log) is absent.

    If any check fails, fix the source and rebuild before marking done. Do not edit run-rdp.sh or any setting — this task only validates the source.
  </action>
  <verify>
    <automated>cd /home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg/client/X11 && test "$(grep -c 'xf_input_three_finger_resolve' xf_input.c)" -ge 1 && grep -q 'three-finger: pending upgrade' xf_input.c && grep -q 'pan: claimed two-finger scroll' xf_input.c && grep -q 'pan: claimed three-finger middle-button drag' xf_input.c && grep -q 'scrollActive' xf_input.c && grep -q 'midFingerC' xf_input.c && ! grep -q 'pan: claimed middle-button drag' xf_input.c && echo OK</automated>
  </verify>
  <done>
    All seven static checks pass and print OK: three-finger resolve defined, both new claim logs present, upgrade log present, force_cancel references scrollActive and midFingerC, and the old two-finger middle-button claim log is gone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local X11 touch -> RDP input | XI2 touch events (untrusted physical input) cross into synthesized RDP mouse/wheel/keyboard events. No new network surface added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-956-01 | Tampering | gesture state machine (xf_input_touch_fallback) | medium | mitigate | All new multi-finger states (scrollActive, threeFingerPending, midActive 3-finger) route through the single xf_touch_force_cancel seam for release + quarantine; 4th-finger and post-claim 3rd-finger still abort. |
| T-956-02 | DoS | stuck wheel/button on lifecycle abort | high | mitigate | force_cancel clears scrollActive (no button to release) and midActive (releases BUTTON3) and quarantines midFingerC alongside A/B; the quarantine condition is widened to include hadScroll/hadThree. |
| T-956-03 | EoP | no new trust boundary | low | accept | Reuses existing RDP button/wheel send helpers; no new parsing, auth, or network surface. |
</threat_model>

<verification>
- Build: `cmake --build` reaches 100% with the new state fields and helpers compiled in.
- Static: the 2-finger resolve no longer claims a middle button; the 3-finger resolve does; force_cancel owns all new state.
- Hardware UAT (deferred, agent cannot run): on the OneMix 3 native X11 session, two-finger drag scrolls the remote window; three-finger drag pans with the middle button held; pinch still zooms; one-finger tap/drag/long-press unchanged; third finger during pending upgrades, during claimed aborts.
</verification>

<success_criteria>
- xfreerdp3 builds and the binary exists.
- Two-finger pan = wheel scroll; three-finger pan = middle-button drag; pinch and one-finger unchanged.
- All new gesture state is released and quarantined by xf_touch_force_cancel.
- Static checks for the contact-count branches pass.
</success_criteria>

<output>
Create `.planning/quick/260808-956-hi-n-t-i-freerdp-ang-l-c-2-ng-n-pan-th-h/260808-956-SUMMARY.md` when done.
</output>