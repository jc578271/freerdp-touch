---
phase: 01-environment-gate-build-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-x11-session.sh
  - scripts/build-baseline.sh
  - .gitignore
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
estimate:
  tokens: 42000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/check-x11-session.sh exits 1 on the current Wayland session and prints a remediation mentioning GNOME on Xorg"
    - "scripts/check-x11-session.sh exits 0 only when XDG_SESSION_TYPE=x11 AND WAYLAND_DISPLAY is empty AND pgrep Xorg succeeds AND pgrep Xwayland is empty"
    - "scripts/check-x11-session.sh prints exactly one stdout line on success and writes all errors to stderr"
    - "scripts/build-baseline.sh invokes scripts/check-x11-session.sh at its top before any other work"
    - "scripts/build-baseline.sh hard-fails if apt-cache showsrc freerdp3 resolves to a version other than 3.15.0+dfsg-2.1+deb13u3"
    - "scripts/build-baseline.sh installs only freerdp3-x11_*_amd64.deb and never globs freerdp3-*_amd64.deb"
    - "scripts/build-baseline.sh records sha256sum of the built freerdp3-x11 .deb"
    - "scripts/build-baseline.sh prompts for the launch command and Windows target as explicit inputs and does not read shell history"
    - "scripts/build-baseline.sh writes one generated Markdown file to .planning/phases/01-environment-gate-build-baseline/baseline-report.md"
    - "scripts/build-baseline.sh runs a sanitization pass that replaces credentials, usernames, hostnames, and IP addresses before writing the report"
    - ".gitignore contains build/ and *.deb"
    - "D-12: the unmodified baseline tree fetched into build/ is not reused for patch development; Phase 1 only proves the unmodified build, and later phases re-unpack a clean tree from this verified source rather than resetting the baseline tree"
  artifacts:
    - scripts/check-x11-session.sh
    - scripts/build-baseline.sh
    - .gitignore
  key_links:
    - "scripts/build-baseline.sh calls scripts/check-x11-session.sh at its top (D-02 gate re-run)"
    - "scripts/build-baseline.sh writes .planning/phases/01-environment-gate-build-baseline/baseline-report.md (D-07 handoff artifact)"
  prohibitions:
    - statement: "The gate must not gain a silent force, override, or backdoor path that bypasses the native-X11 check."
      status: unverified
      flagged: true
    - statement: "The baseline capture must not scrape shell history for the launch command or Windows target, and must not leave placeholders in the report."
      status: unverified
      flagged: true
    - statement: "The baseline report must not be committed with credentials, usernames, hostnames, IP addresses, or certificates."
      status: unverified
      flagged: true
    - statement: "Source acquisition must not silently accept a version other than 3.15.0+dfsg-2.1+deb13u3."
      status: unverified
      flagged: true
  backstop_truths:
    - statement: "The generated baseline-report.md is complete with before/during/after package versions, artifact path and SHA-256, install and rollback command results, and explicit pass/fail notes for both windowed and fullscreen modes."
      verification: backstop
    - statement: "The developer built the unmodified Debian .deb, installed freerdp3-x11, smoke-tested windowed and fullscreen against the Windows target, rolled back to stock, and verified the stock version."
      verification: backstop
---

<objective>
Create the X11 environment gate, the .gitignore, and the full build-baseline orchestrator script that encodes the baseline-capture + build/install/rollback + sanitization pipeline. This plan delivers the capability; the on-device execution proof is Plan 02.

Purpose: Establish the fail-closed native-X11 gate (BASE-01) and the shell pipeline that captures machine facts, builds the unmodified Debian source, installs/smoke-tests/rolls-back, and writes one sanitized Markdown report (BASE-02, BASE-03). The gate is verifiable right now on the live Wayland session (it hard-fails by design); the full pipeline executes in Plan 02 once the developer switches to GNOME on Xorg.

Output: scripts/check-x11-session.sh, scripts/build-baseline.sh, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md
@.planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md
@.planning/phases/01-environment-gate-build-baseline/01-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: X11 gate script + .gitignore</name>
  <files>scripts/check-x11-session.sh, .gitignore</files>
  <read_first>
    - .planning/phases/01-environment-gate-build-baseline/01-PATTERNS.md (Pattern 1 gate excerpt lines 29-64; .gitignore excerpt lines 198-213; shared POSIX-shell header lines 217-226; structured error idiom lines 228-242)
    - .planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md (Pattern 1 lines 170-211; Pitfall 1 lines 294-299; discriminator finding line 463; live session-state evidence lines 440-461)
    - .planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-10)
  </read_first>
  <action>
    Create scripts/check-x11-session.sh per D-01: a POSIX sh script (shebang #!/bin/sh, set -eu, no -o pipefail) that implements the four-signal discriminator from RESEARCH.md Pattern 1. The gate checks, in order: XDG_SESSION_TYPE equals x11; WAYLAND_DISPLAY is empty; pgrep -x Xorg succeeds; pgrep -x Xwayland is empty. Use the fail() helper pattern from PATTERNS.md lines 228-242: every error goes to stderr as "ERROR: native X11 not proven (detected: <state>).\n       <remedy>" then exit 1. Each failure names the detected session and prints the concrete remediation: log out and select "GNOME on Xorg" at the GDM login screen (the gnome-xorg.desktop session exists per RESEARCH.md line 432). On success print exactly one stdout line: "OK: native X11 session verified (display <DISPLAY>)." per D-04. The gate writes no pass-marker file and accepts no override flag (D-01, D-02). Do not use xdpyinfo vendor string as a discriminator (RESEARCH.md line 463 confirms it says "X.Org Foundation" under both Xorg and XWayland).

    Create .gitignore at repo root (none exists today, verified RESEARCH.md line 376). Add one line per pattern: build/ (the workspace, D-10), *.deb, *.dsc, *.orig.tar.xz, *.debian.tar.xz, *.build, *.buildinfo, *.changes. Do not ignore .planning/ or scripts/.

    Both decisions are reversible (scripts are new files; .gitignore is a new static config).
  </action>
  <verify>
    <automated>cd /home/hoang/freerdp-touch && sh scripts/check-x11-session.sh; echo "exit=$?"; test "$?" -ne 0 || { echo "FAIL: gate should exit nonzero on Wayland"; exit 1; }; sh scripts/check-x11-session.sh 2>&1 1>/dev/null | grep -q "GNOME on Xorg" || { echo "FAIL: remediation missing"; exit 1; }; grep -q '^build/$' .gitignore && grep -q '^\*\.deb$' .gitignore || { echo "FAIL: .gitignore missing patterns"; exit 1; }; sh -n scripts/check-x11-session.sh</automated>
  </verify>
  <done>
    scripts/check-x11-session.sh exits 1 on the current Wayland session, prints a remediation mentioning "GNOME on Xorg" to stderr, passes sh -n syntax check, and prints exactly one stdout line on success. .gitignore exists with build/ and *.deb entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: build-baseline.sh orchestrator (gate re-run + pin-check + fetch + build + capture + install/smoke/rollback + sanitize + report)</name>
  <files>scripts/build-baseline.sh</files>
  <read_first>
    - scripts/check-x11-session.sh (the gate from Task 1 — build-baseline.sh calls it)
    - .planning/phases/01-environment-gate-build-baseline/01-PATTERNS.md (Pattern 2 pin-check lines 85-97; Pattern 3 build/install/rollback lines 99-117; canonical command sequence lines 119-130; idiom extraction lines 132-146; report data-source commands lines 154-185; sanitization lines 257-263)
    - .planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md (Pattern 2 lines 213-230; Pattern 3 lines 232-256; baseline capture commands lines 336-367; anti-patterns lines 258-265; pitfalls 2-6 lines 301-331; assumptions A1/A2/A3 lines 391-397)
    - .planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md (D-02, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-13, D-14, D-15, D-16)
  </read_first>
  <action>
    Create scripts/build-baseline.sh as a POSIX sh script (#!/bin/sh, set -eu). Structure it as a sequential pipeline with these stages, each printing a stage header to stderr:

    1. GATE RE-RUN (D-02): invoke scripts/check-x11-session.sh as a subprocess at the very top; set -eu propagates its nonzero exit. Do not source it (keep variable scope clean).
    2. PIN-CHECK (D-11): set PINNED="3.15.0+dfsg-2.1+deb13u3"; resolved=$(apt-cache showsrc freerdp3 | awk '/^Version:/{print $2; exit}'); if resolved != PINNED, print "ERROR: apt source freerdp3 resolves to <resolved>, expected <PINNED>." to stderr and exit 1. No automatic fallback.
    3. FETCH (D-09, D-10): mkdir -p build; cd build; apt source freerdp3 (unpacks into build/freerdp3-3.15.0+dfsg/). If apt source fails, print the deb-src remediation (uncomment deb-src in /etc/apt/sources.list) per Pitfall 6.
    4. BUILD-DEP: sudo apt build-dep freerdp3 (pulls dpkg-dev, libxi-dev, cmake, build-essential transitively per RESEARCH.md line 88).
    5. BUILD (D-13): cd freerdp3-3.15.0+dfsg; dpkg-buildpackage -us -uc -b -j"$(nproc)"; cd .. (binary-only build). Use dpkg-buildpackage, not debuild (devscripts not installed, RESEARCH.md line 99).
    6. CHECKSUM (D-16): DEB=$(ls freerdp3-x11_*_amd64.deb | head -1); SHA=$(sha256sum "$DEB" | awk '{print $1}'). Record both for the report.
    7. CAPTURE MACHINE FACTS (D-06 auto portion): collect into shell variables the outputs of: dpkg -l freerdp3-x11 (installed version, the "before" version), apt-cache showsrc freerdp3 version, xdpyinfo display name + vendor + version, pgrep -ax Xorg, pgrep -ax Xwayland, xprop -root _NET_SUPPORTING_WM_CHECK then xprop -id <wm> _NET_WM_NAME (WM name), xinput list, xinput list --long, xrandr --query active output + rotation, xprop -root RESOURCE_MANAGER piped through tr ';' '\n' | grep -i dpi. These run under native X11 (the gate already confirmed it).
    8. EXPLICIT INPUTS (D-06): prompt the developer for the launch command and the Windows target description via read to stdin. Do not read shell history, do not leave placeholders. Store as LAUNCH_CMD and WIN_TARGET.
    9. INSTALL (D-13): sudo apt install "./$DEB" — install ONLY the freerdp3-x11_*_amd64.deb file, never glob freerdp3-*_amd64.deb (Pitfall 4: would also replace freerdp3-wayland). Use apt install ./ not dpkg -i (RESEARCH.md line 386). Record the "during" version via xfreerdp3 --version or dpkg -l freerdp3-x11.
    10. SMOKE TEST (D-14, D-15): print instructions for the developer to run the windowed launch then the fullscreen launch (/f flag) against the provided target, and prompt for pass/fail confirmation of each: desktop reached, keyboard+mouse work, clean disconnect. Store WINDOWED_PASS and FULLSCREEN_PASS as the human's yes/no answers. This stage is interactive — it pauses for human input.
    11. ROLLBACK (D-13): sudo apt install --reinstall freerdp3-x11. If apt refuses (same-version), fall back to sudo apt install freerdp3-x11/trixie per RESEARCH.md line 256 / Assumption A1. Record the "after" version.
    12. VERIFY STOCK: xfreerdp3 --version (or dpkg -l freerdp3-x11) — confirm it reports the stock 3.15.0+dfsg-2.1+deb13u3, not the locally-built one.
    13. SANITIZE (D-08): before writing the report, run a sanitization pass over LAUNCH_CMD and WIN_TARGET and all captured strings: replace any user@host or bare IP with USER@HOST-IP, replace /p:<password> with /p:***, remove any hostname/domain/certificate content. Preserve package versions, session/WM details, display rotation/scale, touchscreen model/capabilities.
    14. REPORT (D-05, D-07, D-16): write one Markdown file to .planning/phases/01-environment-gate-build-baseline/baseline-report.md with sections: Session (type, display, Xorg/Xwayland process evidence), Window Manager, Installed Package (before version), Source Version (apt-cache showsrc), Touch Device (xinput identity + XITouchClass/XIDirectTouch + max touches), Display (xrandr rotation/scale, DPI), Build (artifact path, SHA-256, dpkg-buildpackage result), Install (during version, apt install result), Smoke Test (windowed pass/fail, fullscreen pass/fail, D-14/D-15 confirmation), Rollback (after version, apt reinstall result, stock verification), Launch Command (sanitized), Windows Target (sanitized). No screenshots. The report is the handoff artifact for Phase 2.

    The script is a single file with all 14 stages. Stages 1-9 and 11-14 are automated; stage 10 is interactive (human-verify). The script is not run in this plan — Plan 02 runs it on the X11 session. This task only creates and syntax-checks the script.
  </action>
  <verify>
    <automated>cd /home/hoang/freerdp-touch && sh -n scripts/build-baseline.sh && grep -q 'check-x11-session.sh' scripts/build-baseline.sh && grep -q '3.15.0+dfsg-2.1+deb13u3' scripts/build-baseline.sh && grep -q 'apt source freerdp3' scripts/build-baseline.sh && grep -q 'dpkg-buildpackage' scripts/build-baseline.sh && grep -q 'sha256sum' scripts/build-baseline.sh && grep -q 'apt install --reinstall freerdp3-x11' scripts/build-baseline.sh && grep -q 'freerdp3-x11_\*_amd64.deb' scripts/build-baseline.sh && ! grep -q 'freerdp3-\*_amd64.deb' scripts/build-baseline.sh && grep -q 'baseline-report.md' scripts/build-baseline.sh && grep -qi 'sanitiz' scripts/build-baseline.sh && grep -qi 'read ' scripts/build-baseline.sh</automated>
  </verify>
  <done>
    scripts/build-baseline.sh passes sh -n, calls the gate at its top, pins 3.15.0+dfsg-2.1+deb13u3, uses apt source + dpkg-buildpackage + sha256sum + apt install --reinstall, installs only freerdp3-x11_*_amd64.deb (not the freerdp3-* glob), writes baseline-report.md, runs a sanitization pass, and prompts for explicit inputs via read.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell → apt/dpkg (sudo) | The build/install/rollback stages invoke sudo apt; a tampered source or wrong-version package crosses here. |
| Capture process → committed report | Machine facts + explicit launch/target inputs flow into baseline-report.md, which is committed to git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | baseline-report.md | high | mitigate | Sanitization pass (D-08) replaces credentials, usernames, hostnames, IPs with placeholders before writing; Plan 02 reviews git diff before commit. |
| T-01-02 | Tampering | scripts/check-x11-session.sh | high | mitigate | Four-signal AND check (XDG_SESSION_TYPE, WAYLAND_DISPLAY, pgrep Xorg, pgrep Xwayland); any miss exits 1; no override flag (D-01). |
| T-01-03 | Tampering | scripts/build-baseline.sh pin-check | medium | mitigate | Hard-fail if apt-cache showsrc version != 3.15.0+dfsg-2.1+deb13u3 (D-11); no automatic fallback. |
| T-01-04 | Elevation | sudo apt install of built .deb | low | accept | apt verifies the .dsc signature from the Debian repo; pin-check rejects version drift; the .deb is built from the distro's own debian/rules. |
</threat_model>

<verification>
- scripts/check-x11-session.sh exits 1 on the current Wayland session and names "GNOME on Xorg" in the remediation.
- scripts/check-x11-session.sh passes sh -n and uses the four-signal discriminator (no xdpyinfo vendor reliance).
- .gitignore contains build/ and *.deb (and the other Debian build byproducts).
- scripts/build-baseline.sh passes sh -n and contains all load-bearing identifiers: the gate call, the pinned version, apt source, dpkg-buildpackage, sha256sum, apt install --reinstall, freerdp3-x11_*_amd64.deb (not the - glob), baseline-report.md, a sanitization pass, and a read prompt for explicit inputs.
</verification>

<success_criteria>
- The gate hard-fails on Wayland with the correct remediation (proven live now).
- The orchestrator script is syntactically valid and encodes the full 14-stage pipeline per D-01 through D-16.
- .gitignore excludes the build workspace and all Debian build byproducts.
- No FreeRDP source files are touched (Phase 1 boundary).
</success_criteria>

<output>
Create .planning/phases/01-environment-gate-build-baseline/01-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

| Artifact | Path | Purpose |
|----------|------|---------|
| X11 gate script | scripts/check-x11-session.sh | Fail-closed native-X11 session check; re-run by every Phase 1 entry point (D-01, D-02). |
| Build/baseline orchestrator | scripts/build-baseline.sh | 14-stage pipeline: gate → pin-check → fetch → build-dep → build → checksum → capture → explicit inputs → install → smoke → rollback → verify → sanitize → report. |
| Git ignore file | .gitignore | Excludes build/ workspace and *.deb / *.dsc / *.orig.tar.xz / *.debian.tar.xz / *.build / *.buildinfo / *.changes (D-10). |
| Sanitized baseline report | .planning/phases/01-environment-gate-build-baseline/baseline-report.md | The committed handoff artifact for Phase 2 (produced by Plan 02 execution, not this plan). |

**Env vars / CLI args referenced:** XDG_SESSION_TYPE, WAYLAND_DISPLAY, DISPLAY (read by the gate); PINNED (internal script constant = 3.15.0+dfsg-2.1+deb13u3); LAUNCH_CMD, WIN_TARGET, WINDOWED_PASS, FULLSCREEN_PASS (internal script variables from interactive prompts).