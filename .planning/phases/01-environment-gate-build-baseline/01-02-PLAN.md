---
phase: 01-environment-gate-build-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-environment-gate-build-baseline/baseline-report.md
autonomous: false
requirements:
  - BASE-02
  - BASE-03
estimate:
  tokens: 24000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/check-x11-session.sh exits 0 under the GNOME on Xorg session selected at the GDM login screen"
    - "scripts/build-baseline.sh runs to completion on the native-X11 session and produces build/freerdp3-x11_*_amd64.deb"
    - "The committed baseline-report.md records the before (stock), during (locally built), and after (stock restored) freerdp3-x11 versions"
    - "The committed baseline-report.md records the built artifact path and its SHA-256 checksum"
    - "The committed baseline-report.md records explicit pass/fail notes for both windowed and fullscreen smoke-test modes"
    - "The committed baseline-report.md contains the touchscreen identity and capabilities captured under native X11 (not xwayland-touch)"
    - "git diff of the committed baseline-report.md contains no IP addresses, hostnames, usernames, or /p:password values"
  artifacts:
    - .planning/phases/01-environment-gate-build-baseline/baseline-report.md
  key_links:
    - "baseline-report.md is the handoff artifact consumed by Phase 2 planning (D-07)"
  prohibitions:
    - statement: "The committed report must not contain credentials, usernames, hostnames, IP addresses, or certificates."
      status: unverified
      flagged: true
    - statement: "The on-device cycle must not be skipped or replaced with documentation-only rollback (D-13 requires the full build/install/smoke/rollback proof)."
      status: unverified
      flagged: true
  backstop_truths:
    - statement: "The windowed smoke test reached the Windows desktop, confirmed ordinary keyboard and mouse input, and disconnected cleanly."
      verification: backstop
    - statement: "The fullscreen smoke test reached the Windows desktop, confirmed ordinary keyboard and mouse input, and disconnected cleanly."
      verification: backstop
    - statement: "The report is complete with all required sections and no placeholders."
      verification: backstop
---

<objective>
Execute the build-baseline orchestrator on a verified native-X11 session, complete the full on-device build/install/smoke/rollback proof cycle (D-13), and commit the sanitized baseline report as the Phase 2 handoff artifact.

Purpose: Prove BASE-02 (captured baseline) and BASE-03 (build, install, launch, roll back) with real evidence. The gate hard-fails on the current Wayland session, so this plan is a human-verified checkpoint: the developer switches to GNOME on Xorg, runs the script, provides the launch command and Windows target, confirms the windowed and fullscreen smoke tests, and the locally-built package is rolled back to stock. Documentation-only rollback does not pass this plan (D-13).

Output: .planning/phases/01-environment-gate-build-baseline/baseline-report.md (committed, sanitized).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md
@.planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md
@.planning/phases/01-environment-gate-build-baseline/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the full build/install/smoke/rollback cycle on GNOME on Xorg</name>
  <precondition>The developer has logged out of the Wayland session and selected "GNOME on Xorg" at the GDM login screen (the gate hard-fails otherwise); scripts/build-baseline.sh exists from Plan 01.</precondition>
  <files>.planning/phases/01-environment-gate-build-baseline/baseline-report.md</files>
  <read_first>
    - scripts/check-x11-session.sh (the gate — confirm it now exits 0 on the Xorg session)
    - scripts/build-baseline.sh (the orchestrator from Plan 01 — review the 14 stages before running)
    - .planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md (D-06, D-13, D-14, D-15, D-16)
    - .planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md (assumptions A1/A2/A3 lines 391-397; open questions lines 399-409; rollback fallback line 256)
  </read_first>
  <what-built>
    Plan 01 produced the X11 gate and the build-baseline orchestrator script (14-stage pipeline). The orchestrator has not yet been executed end-to-end because the live session is Wayland and the gate correctly hard-fails there.
  </what-built>
  <how-to-verify>
    This task is the on-device execution of D-13. Steps for the developer:

    1. Log out of the current GNOME Wayland session. At the GDM login screen, select the "GNOME on Xorg" session (/usr/share/xsessions/gnome-xorg.desktop exists per RESEARCH.md line 432). Log in.
    2. Open a terminal in the repo root. Confirm the gate now passes: `sh scripts/check-x11-session.sh` — expect exit 0 and one line "OK: native X11 session verified (display :0)." If it still fails, do not proceed; the session switch did not take.
    3. Run the orchestrator: `sh scripts/build-baseline.sh`. It will: re-run the gate, pin-check apt source, fetch the source into build/, install build-deps via sudo, build the unmodified .deb via dpkg-buildpackage, checksum it, capture machine facts, then PROMPT for the launch command and the Windows target (have both ready — do not type a password inline; use /from-stdin or a prompt if the client requires credentials). Then it installs freerdp3-x11_*.deb, PAUSES for the smoke test, prompts for windowed pass/fail and fullscreen pass/fail, reinstalls stock, and verifies the stock version.
    4. At the smoke-test pause, run BOTH launches against the provided Windows target:
       - Windowed: `xfreerdp3 /v:<target> /u:<user>` — confirm the Windows desktop is reached, ordinary keyboard and mouse input work, and the session disconnects cleanly (D-14, D-15).
       - Fullscreen: `xfreerdp3 /v:<target> /u:<user> /f` — confirm the same three criteria.
       Answer the script's pass/fail prompts honestly; a "no" is recorded as failure and does not block this checkpoint but is reported as-is.
    5. Let the script finish: it reinstalls the stock freerdp3-x11 via `apt install --reinstall` (or the =trixie fallback per RESEARCH.md line 256 / A1 if apt refuses same-version reinstall), verifies the stock version, sanitizes the captured data, and writes .planning/phases/01-environment-gate-build-baseline/baseline-report.md.
    6. After the script exits, confirm the rollback took: `xfreerdp3 --version` reports the stock 3.15.0 (n/a), not the locally-built one. Confirm `dpkg -l freerdp3-x11` shows the stock 3.15.0+dfsg-2.1+deb13u3.

    Type "approved" with the captured before/during/after versions, or describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved" and paste the before/during/after freerdp3-x11 versions, or describe any failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Sanitization review and commit of baseline-report.md</name>
  <files>.planning/phases/01-environment-gate-build-baseline/baseline-report.md</files>
  <read_first>
    - .planning/phases/01-environment-gate-build-baseline/baseline-report.md (the report produced by Task 1)
    - .planning/phases/01-environment-gate-build-baseline/01-CONTEXT.md (D-07, D-08)
    - .planning/phases/01-environment-gate-build-baseline/01-RESEARCH.md (Pitfall 5 lines 321-325; sanitization section lines 322-325)
  </read_first>
  <action>
    Review the generated baseline-report.md for the D-08 sanitization contract before committing. Run automated checks for identity-bearing leakage: grep for IPv4 patterns (grep -E '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+'), /p: followed by non-asterisk characters (credentials), and any @domain.tld patterns. If any are found, edit the report to replace them with USER@HOST-IP and /p:*** placeholders per RESEARCH.md Pitfall 5. Confirm the report preserves technical data (package versions, session/WM, rotation/scale, touchscreen model/caps, artifact path, SHA-256) and contains no __TODO__ or placeholder gaps in the technical sections. Confirm the report has before/during/after versions, the artifact path + SHA-256, install and rollback command results, and explicit windowed + fullscreen pass/fail notes (D-16). Then git add and commit the report (D-07). Do not commit anything from build/ (it is git-ignored). This is a reversible commit (Markdown evidence file).
  </action>
  <verify>
    <automated>cd /home/hoang/freerdp-touch && test -f .planning/phases/01-environment-gate-build-baseline/baseline-report.md && grep -qi 'before\|stock' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && grep -qi 'sha-256\|sha256' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && grep -qi 'fullscreen' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && grep -qi 'windowed' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && ! grep -qE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && ! grep -qE '/p:[^*]' .planning/phases/01-environment-gate-build-baseline/baseline-report.md && git status --porcelain .planning/phases/01-environment-gate-build-baseline/baseline-report.md | grep -q .</automated>
  </verify>
  <done>
    baseline-report.md exists, contains before/during/after version evidence, a SHA-256 checksum, windowed and fullscreen pass/fail notes, no IPv4 addresses, no /p:<non-asterisk> credentials, and is staged/committed to git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native-X11 session → capture tools | xinput/xrandr/xprop run under the real Xorg session; results are valid only here (the gate enforces it). |
| Generated report → git commit | The sanitized report crosses from the developer's machine into version-controlled history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | committed baseline-report.md | high | mitigate | Task 2 automated grep for IPv4 and /p:credential patterns plus manual review of git diff before commit (D-08); replace with USER@HOST-IP and /p:***. |
| T-01-06 | Repudiation | smoke-test pass/fail notes | low | accept | The report records the developer's honest yes/no answers; no cryptographic proof of the smoke test is required (D-16 explicitly excludes screenshots). |
| T-01-07 | Tampering | rollback to stock | medium | mitigate | Task 1 step 6 confirms xfreerdp3 --version and dpkg -l report stock after apt install --reinstall; the =trixie fallback covers the same-version refusal case (A1). |
</threat_model>

<verification>
- The gate exits 0 under GNOME on Xorg (the session switch took).
- scripts/build-baseline.sh completed: build/freerdp3-x11_*_amd64.deb was built, checksummed, installed, smoke-tested in both modes, and the stock package was reinstalled.
- baseline-report.md is committed with before/during/after versions, artifact path + SHA-256, both smoke-test pass/fail notes, and the native-X11 touchscreen identity (not xwayland-touch).
- git diff of the commit shows no IP addresses, hostnames, usernames, or credentials.
- xfreerdp3 --version reports stock after rollback.
</verification>

<success_criteria>
- BASE-02 proven: the committed report captures installed version, WM, native-X11 touchscreen identity/capabilities, rotation/scale, sanitized launch command, and Windows target.
- BASE-03 proven: the developer built the unmodified Debian source, installed freerdp3-x11, smoke-tested windowed and fullscreen, rolled back to stock, and verified the stock version — the full D-13 cycle, not documentation-only.
- The committed report is sanitized (D-08) and serves as the Phase 2 handoff artifact (D-07).
</success_criteria>

<output>
Create .planning/phases/01-environment-gate-build-baseline/01-02-SUMMARY.md when done.
</output>

## Artifacts this phase produces

| Artifact | Path | Purpose |
|----------|------|---------|
| Sanitized baseline report | .planning/phases/01-environment-gate-build-baseline/baseline-report.md | The committed Phase 2 handoff artifact: before/during/after versions, artifact path + SHA-256, smoke-test pass/fail, native-X11 touchscreen identity/caps, rotation/scale, sanitized launch command + Windows target (D-07, D-16). |

**Transient (git-ignored, not committed):** build/freerdp3-3.15.0+dfsg/ source tree, build/freerdp3-x11_*_amd64.deb and other build byproducts — all excluded by .gitignore from Plan 01.

**No new env vars or CLI args introduced by this plan** — it executes scripts/build-baseline.sh (from Plan 01) which reads XDG_SESSION_TYPE, WAYLAND_DISPLAY, DISPLAY and prompts for LAUNCH_CMD / WIN_TARGET interactively.