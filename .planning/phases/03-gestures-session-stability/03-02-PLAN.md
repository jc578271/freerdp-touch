---
phase: 03-gestures-session-stability
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
autonomous: true
requirements: [GEST-03, GEST-04]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - In native pinch mode (TouchPinchWheelFallback false/unset), a two-finger pinch forwards both contacts as native RDPEI multitouch and emits no local wheel shortcut (D-05, GEST-03).
    - In native mode the exclusivity guard ensures no Ctrl key and no wheel event are synthesized during a two-finger gesture (D-05).
    - In fallback pinch mode (TouchPinchWheelFallback set at startup), the second finger immediately claims the gesture away from native RDPEI so native pinch and local Ctrl+wheel output can never overlap (D-06, GEST-04).
    - On fallback claim, the first contact's already-emitted native DOWN is cancelled via rdpei->TouchCancel so Windows does not keep a dangling contact (D-06, Pitfall 3).
    - Fallback wheel output waits until deliberate scale movement crosses a small activation deadband before the first detent (D-06, D-08).
    - Before the first fallback wheel tick, the hidden remote pointer is moved to the current pinch midpoint so applications that zoom around the cursor target the touched content (D-11).
    - Fingers moving apart zoom in (Ctrl+wheel-up) and fingers moving together zoom out (Ctrl+wheel-down) using fixed standard wheel detents with no speed-based acceleration (D-09, D-10).
    - When pinch direction reverses, the partial wheel-step accumulator is discarded and a fresh complete detent is required before emitting the opposite direction, so small reversals do not chatter (D-12, Pitfall 6).
    - A fallback pinch ends as soon as contact count drops below two: Ctrl is released, wheel output stops, and the remaining finger is ignored until every pinch contact has lifted (D-13).
    - A third finger aborts fallback pinch: Ctrl is released (via the force-cancel seam from plan 01), output stops, the gesture resets, and all involved fingers must lift before new touch input (D-14).
    - The startup-selected native/fallback mode is read from settings and never switched mid-session (D-16; the mode latch is evaluated at gesture claim time from the setting, not toggled).
  artifacts:
    - Native pinch exclusivity guard + mode-latch branch in xf_input_touch_remote (no new file, no new settings field)
    - Fallback pinch claiming + Ctrl press + activation deadband + midpoint pointer + fixed-detent accumulator + reversal hysteresis + end-on-first-lift + third-finger abort, all inline in xf_input_touch_remote
    - No new settings rows, no new xfContext fields (all declared in plan 01), no new files
  key_links:
    - Second-finger claim reads FreeRDP_TouchPinchWheelFallback to branch native vs fallback (D-16 mode latch)
    - rdpei->TouchCancel on the first contact at fallback claim moment (Pitfall 3 — cancel before Ctrl press so no native/fallback overlap)
    - freerdp_input_send_keyboard_event(KBD_FLAGS_DOWN, RDP_SCANCODE_LCONTROL) at claim; release routes through xf_touch_force_cancel's idempotent ctrlHeld guard (D-06, D-13, D-14)
    - Wheel constants copied verbatim from xf_client.c:1048-1061: PTR_FLAGS_WHEEL | 0x78 (up) and PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78) (down)
    - xf_touch_force_cancel (extended in plan 01) releases Ctrl + clears pinchActive/pinchClaimed/pinchAccum on every abort path (D-14, D-15)
  prohibitions:
    - statement: MUST NOT emit both native RDPEI pinch contacts and local Ctrl+wheel for the same two-finger gesture — fallback claims exclusively at the second finger (D-06)
      category: safety
      status: resolved
      verification: judgment
    - statement: MUST NOT leave the Ctrl modifier stuck after any pinch abort (third finger, focus loss, disconnect) — every release routes through the idempotent ctrlHeld guard in xf_touch_force_cancel
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Implement the pinch gesture in both modes: native RDPEI passthrough (already working from Phase 2, Phase 3 adds only the D-04 long-press cancel + the exclusivity guard + the mode latch) and the fallback Ctrl+wheel synthesizer (the new logic: exclusive claiming, activation deadband, midpoint targeting, fixed detents, reversal hysteresis, end/interrupt cleanup).

Purpose: GEST-03 is mostly already satisfied by the Phase 2 two-contact passthrough; Phase 3 adds the startup mode latch so native mode is a deliberate no-wheel passthrough. GEST-04 (fallback) is the substantial new logic — it synthesizes Ctrl+wheel for Windows apps that are not touch-aware, with midpoint targeting so the zoom centers on the touched content and reversal hysteresis so hesitation does not jitter zoom. All cleanup routes through the force-cancel seam extended in plan 01.

Output: Native pinch exclusivity guard + fallback pinch state machine inline in xf_input_touch_remote, reading the mode from the settings field declared in plan 01. No new files, no new settings, no new xfContext fields.
</objective>

<execution_context>
@.planning/phases/03-gestures-session-stability/03-CONTEXT.md
@.planning/phases/03-gestures-session-stability/03-RESEARCH.md
@.planning/phases/03-gestures-session-stability/03-PATTERNS.md
@.planning/phases/03-gestures-session-stability/03-01-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-rdpei-touch-lifecycle/02-02-SUMMARY.md

@build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native pinch passthrough exclusivity guard + mode latch (GEST-03)</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c</files>
  <read_first>
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c lines 646-830 (xf_input_touch_remote — the long-press state machine from plan 01 is now here before the switch; the pinch guard goes alongside it)
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c lines 416-470 (xf_input_detect_pinch — local-only, NOT reused; confirm it only rescales the local framebuffer via PubSub and sends no remote input)
    - The plan 01 additions in xf_input.c (lpArmed/lpFrozen branch + xf_touch_force_cancel extension) — confirm the candidate state machine and the Ctrl-release guard are in place before adding pinch
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_client.c lines 1048-1061 (wheel constants — read verbatim now, needed in task 2)
  </read_first>
  <action>
    Per D-04, D-05, D-16 and the native-mode contract.

    1. In xf_input_touch_remote, at the second-finger arrival (XI_TouchBegin when one finger is already active and rdpei is non-NULL): first cancel any armed long-press candidate (lpArmed=FALSE) per D-04 — this was partially handled in plan 01 but confirm it fires for the second finger specifically and does not emit a right-click. Then read the mode: `BOOL wheelFallback = freerdp_settings_get_bool(settings, FreeRDP_TouchPinchWheelFallback);` where settings = xfc->common.context.settings.

    2. Native mode (!wheelFallback): set pinchActive=FALSE, pinchClaimed=FALSE (native passthrough — no claiming). Both contacts forward through the existing switch to freerdp_client_handle_touch as normal (DOWN for the second finger, MOTION for updates). The exclusivity guard: ensure NO Ctrl key event and NO wheel event are synthesized on this path. No new code beyond the branch and the field clears — the passthrough already works. This satisfies GEST-03.

    3. Do NOT toggle MultiTouchInput/MultiTouchGestures mid-gesture (RESEARCH Anti-Pattern). Stay on the remote path; native mode simply does not claim.

    4. Two fingers translating together without separation change in native mode produce native RDPEI motion for both contacts — this is D-05 passthrough including native two-finger drag/scroll handled by Windows; Phase 3 adds no separate two-finger drag-to-wheel gesture (CONTEXT.md deferred).
  </action>
  <verify>
    <automated>cd build/freerdp3-3.15.0+dfsg && cmake --build obj-x86_64-linux-gnu -j$(nproc) 2>&1 | tail -3 && grep -c 'TouchPinchWheelFallback\|pinchActive\|pinchClaimed' client/X11/xf_input.c</automated>
    <human-check>On native X11, launch with /multitouch (no /touch-pinch-wheel-fallback), two-finger pinch in a touch-aware Windows app (e.g. Maps) — confirm native pinch zoom works and no wheel event fires.</human-check>
  </verify>
  <done>Native mode branch reads FreeRDP_TouchPinchWheelFallback=false and forwards both contacts as RDPEI passthrough with no Ctrl and no wheel synthesis. Build succeeds. The D-04 second-finger long-press cancel is confirmed in place.</done>
</task>

<task type="auto">
  <name>Task 2: Fallback pinch Ctrl+wheel synthesizer with claiming, deadband, midpoint, detents, reversal hysteresis, end/interrupt (GEST-04)</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c</files>
  <read_first>
    - The plan 01 xf_touch_force_cancel extension (confirm ctrlHeld idempotent release + pinchActive/pinchClaimed/pinchAccum/pinchDir/pinchMidpointDone clears are at the top before the contact loop)
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_client.c lines 1048-1061 (wheel constants: PTR_FLAGS_WHEEL | 0x78 for up/zoom-in; PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78) for down/zoom-out — copy verbatim, do not re-derive)
    - build/freerdp3-3.15.0+dfsg/include/freerdp/input.h lines 31-46, 104 (KBD_FLAGS_DOWN/RELEASE, PTR_FLAGS_*, freerdp_input_send_keyboard_event)
    - build/freerdp3-3.15.0+dfsg/include/freerdp/scancode.h line 71 (RDP_SCANCODE_LCONTROL)
    - build/freerdp3-3.15.0+dfsg/client/common/client.c lines 1590-1635, 1652 (freerdp_client_send_wheel_event, freerdp_client_send_button_event)
    - build/freerdp3-3.15.0+dfsg/include/freerdp/client/rdpei.h lines 44-45, 92-95 (TouchCancel signature; SuspendTouch/ResumeTouch as fallback if the frame builder complains on-device — Open Question 2)
  </read_first>
  <action>
    Per D-06..D-14, Patterns F/G/H/I. All state fields (pinchActive, pinchClaimed, pinchFirstDist, pinchLastDist, pinchAccum, pinchDir, pinchMidpointDone, ctrlHeld, pinchFingerA, pinchFingerB, pinchWheelActive) were declared in plan 01's xfreerdp.h additions and zeroed in xf_input_init.

    1. Claiming (D-06): when wheelFallback is true and the second finger arrives, set pinchClaimed=TRUE, pinchActive=TRUE, pinchFingerA=<first touchId>, pinchFingerB=<second touchId>. Cancel the first contact's already-emitted native DOWN via rdpei->TouchCancel(rdpei, pinchFingerA, <first x>, <first y>, &dummy) so Windows does not keep a dangling contact (Pitfall 3). Suppress native forwarding for BOTH contacts from this point (do not call freerdp_client_handle_touch for either finger while pinchActive). Press Ctrl: freerdp_input_send_keyboard_event(input, KBD_FLAGS_DOWN, RDP_SCANCODE_LCONTROL); set ctrlHeld=TRUE.

    2. Baseline + deadband (D-08): record pinchFirstDist = inter-finger distance (sqrt(pow(dx,2)+pow(dy,2)) between the two current adjusted coordinates) and pinchLastDist = pinchFirstDist. Define a small activation deadband (device-calibrated, use a fixed initial value of ~8px — exact value is planner/device discretion per CONTEXT.md; store as a #define constant, not a new setting). Until |currentDist - pinchFirstDist| exceeds the deadband, emit nothing (D-06 no local action for pure translation).

    3. Midpoint targeting (D-11): before the first wheel tick, compute the midpoint mx=(xA+xB)/2, my=(yA+yB)/2 of the two current adjusted coordinates, run xf_event_adjust_coordinates on it, and call freerdp_client_send_button_event(&xfc->common, FALSE, PTR_FLAGS_MOVE, mx, my) to move the hidden remote pointer. Set pinchMidpointDone=TRUE so it only happens once per gesture.

    4. Wheel detents (D-09, D-10): accumulate signed distance delta = currentDist - pinchLastDist into pinchAccum. Define a fixed wheel-step distance (device-calibrated #define, e.g. 40px change in inter-finger distance per detent — planner discretion). When |pinchAccum| >= wheelStep: if pinchAccum > 0 emit zoom-in: freerdp_client_send_wheel_event(&xfc->common, PTR_FLAGS_WHEEL | 0x78) (fingers apart); if pinchAccum < 0 emit zoom-out: freerdp_client_send_wheel_event(&xfc->common, PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78)) (fingers together). Subtract the consumed full detent from pinchAccum (keep the remainder, do not discard sub-step precision). Set pinchDir to the current direction. Update pinchLastDist = currentDist.

    5. Reversal hysteresis (D-12, Pitfall 6): when the sign of (currentDist - pinchLastDist) differs from pinchDir (direction reversed), discard the partial pinchAccum (set to 0) and require a fresh full wheelStep in the new direction before emitting the opposite direction. Update pinchDir to the new sign only after a full detent is emitted.

    6. End on first lift (D-13): on XI_TouchEnd when pinchActive and the lifting finger is pinchFingerA or pinchFingerB: release Ctrl via the force-cancel seam's idempotent guard (set ctrlHeld then call xf_touch_force_cancel(xfc), OR directly: if ctrlHeld, freerdp_input_send_keyboard_event(KBD_FLAGS_RELEASE, RDP_SCANCODE_LCONTROL), ctrlHeld=FALSE), set pinchActive=FALSE, pinchWheelActive=FALSE. Ignore the remaining finger until every pinch contact has lifted — do NOT resume it as a one-finger tap, drag, or long-press (set a flag or rely on the recovery gate: the remaining finger's TouchEnd will be consumed; new touches after both lift work normally).

    7. Third-finger abort (D-14): on a third XI_TouchBegin while pinchActive: call xf_touch_force_cancel(xfc) which releases Ctrl (idempotent), clears pinchActive/pinchClaimed/pinchAccum, and arms the recovery gate so all three fingers must lift before new input. Do not synthesize any click or wheel on abort.

    8. Channel-loss fallback (Open Question 2): if on-device testing shows rdpei->TouchCancel on the first contact during fallback claim confuses the RDPEI frame builder, wrap the suppression in rdpei->SuspendTouch(rdpei) before cancelling and rdpei->ResumeTouch(rdpei) after claiming instead. Leave a WLog_DBG note at the branch so the on-device UAT can confirm which path is taken. Prefer the TouchCancel path; only switch if the UAT shows a frame error.
  </action>
  <verify>
    <automated>cd build/freerdp3-3.15.0+dfsg && cmake --build obj-x86_64-linux-gnu -j$(nproc) 2>&1 | tail -3 && grep -c 'freerdp_client_send_wheel_event\|freerdp_input_send_keyboard_event.*LCONTROL\|rdpei->TouchCancel\|pinchClaimed\|pinchAccum\|pinchMidpointDone\|PTR_FLAGS_WHEEL' client/X11/xf_input.c</automated>
    <human-check>On native X11, launch with /multitouch /touch-pinch-wheel-fallback, two-finger pinch apart in a non-touch-aware Windows app (e.g. Explorer/Notepad) — confirm Ctrl+wheel zoom in occurs at the pinch midpoint, pinch together zooms out, hesitation/reversal does not chatter, lifting one finger releases Ctrl (next left-click is a plain click not Ctrl+click), and a third finger aborts cleanly with all three needing to lift before new touch works.</human-check>
  </verify>
  <done>Fallback pinch: second finger claims and cancels first native contact; Ctrl pressed; activation deadband suppresses tiny movement; midpoint pointer moves before first tick; fixed detents emit zoom-in/out with no acceleration; reversal discards partial accumulator; first-finger lift releases Ctrl and ignores remaining finger; third finger aborts via force-cancel. Build succeeds. On-device: zoom is controlled, no native/fallback overlap, Ctrl never stuck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local X11 socket -> xfreerdp3 | Two-finger touch coordinates arrive from the already-trusted X11 socket; Phase 2 content-bounds gate already applied before the pinch branch. |
| xfreerdp3 -> RDP input channel | Synthetic Ctrl key + wheel detents + midpoint pointer go through already-audited FreeRDP shared senders; no new channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | Native pinch and Ctrl+wheel both fire (overlap) -> zoom oscillation | high | mitigate | D-06 exclusive claim at second finger: cancel first native contact via TouchCancel before pressing Ctrl; suppress both native forwardings while pinchActive. |
| T-03-06 | Tampering/Elevation | Ctrl stuck after pinch abort (third finger, focus loss, disconnect) | high | mitigate | Every abort routes through xf_touch_force_cancel (extended in plan 01) which releases Ctrl idempotently via the ctrlHeld guard; third-finger abort calls force-cancel directly. |
| T-03-07 | Tampering | Stale pinch accumulator after reconnect -> spurious zoom on new session | medium | mitigate | xf_post_disconnect calls xf_touch_force_cancel which clears pinchAccum/pinchActive (plan 01); reconnect never re-arms (D-16); recovery gate requires all-fingers-up. |
</threat_model>

<verification>
- Build: cmake --build succeeds with zero errors in xf_input.c.
- Source gates: freerdp_client_send_wheel_event, freerdp_input_send_keyboard_event(LCONTROL), rdpei->TouchCancel, pinchClaimed/pinchAccum/pinchMidpointDone present.
- Exclusivity gate: in fallback mode, no freerdp_client_handle_touch call occurs for either pinch finger while pinchActive (grep the suppression branch).
- Ctrl-release gate: ctrlHeld release is present in both the first-lift path and routes through force-cancel for the third-finger/lifecycle paths.
- Wheel-constant gate: the zoom-in value is PTR_FLAGS_WHEEL | 0x78 and zoom-out is PTR_FLAGS_WHEEL | PTR_FLAGS_WHEEL_NEGATIVE | (0x100 - 0x78), copied verbatim from xf_client.c:1048-1061.
- On-device UAT (human, end-of-phase): native pinch zooms a touch-aware app with no wheel; fallback pinch zooms a non-touch-aware app at midpoint with no chatter; one-finger lift releases Ctrl; third finger aborts cleanly.
</verification>

<success_criteria>
- GEST-03: native pinch mode forwards native multitouch with no local wheel shortcut.
- GEST-04: fallback pinch mode emits Ctrl+wheel, never also native pinch, releases Ctrl on end/interrupt, with midpoint targeting and reversal hysteresis.
- Build is green; on-device pinch in both modes behaves per the decisions.
</success_criteria>

<output>
Create .planning/phases/03-gestures-session-stability/03-02-SUMMARY.md when done.
</output>