---
phase: 03-gestures-session-stability
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
autonomous: false
requirements: [STAB-01, STAB-02]
estimate:
  tokens: 38000
  raw_tokens: 19000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - Touch remains usable without crashes, stale contacts, or duplicate events after switching between windowed/fullscreen states and after losing and regaining focus, with all Phase 3 gesture state (long-press candidate, pinch accumulators, held Ctrl) cleared on every existing lifecycle hook (D-15, STAB-01).
    - An RDPEI channel loss mid-session (rdpei becomes NULL while a gesture is active) is handled without a crash by routing through the extended xf_touch_force_cancel before further gesture work, rather than dereferencing a NULL vtable (D-15 channel-loss, STAB-01).
    - Disconnecting during an active touch or gesture does not crash the client, because xf_post_disconnect calls xf_touch_force_cancel which clears all gesture state and releases Ctrl before channel teardown (D-15, STAB-02).
    - After automatic reconnect, touch works again: the startup-selected pinch mode is preserved (it is a setting, D-16), no interrupted gesture is resumed or replayed, all per-gesture state starts clean, and the recovery gate requires all-fingers-up before the first new touch (D-16, STAB-02).
    - No spurious right-click or wheel burst fires immediately after reconnect with no user input (D-16, Pitfall 5).
  artifacts:
    - Channel-loss inline guard in xf_input_touch_remote (if rdpei is NULL and any gesture state is active, call xf_touch_force_cancel before proceeding)
    - No new files, no new settings, no new xfContext fields (all from plan 01)
    - 03-UAT.md on-device test record covering long-press, native pinch, fallback pinch, every lifecycle hook, and reconnect
  key_links:
    - Channel-loss guard runs at the top of the gesture path before any rdpei-> vtable dereference (D-15)
    - xf_post_disconnect -> xf_touch_force_cancel clears gesture state before channel teardown (already wired in plan 01 + Phase 2)
    - client_auto_reconnect_ex preserves settings (pinch mode survives) but per-gesture state does not survive (cleared by force-cancel on the prior disconnect) (D-16)
    - Recovery gate (Phase 2) requires all-fingers-up before any new touch on the new session (D-16)
  prohibitions:
    - statement: MUST NOT replay or resume an interrupted gesture after reconnect — all per-gesture state is cleared by force-cancel on disconnect and reconnect never re-arms (D-16)
      category: safety
      status: resolved
      verification: judgment
    - statement: MUST NOT crash when the RDPEI channel drops mid-session while a gesture is active — the channel-loss guard routes through force-cancel before any vtable dereference
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Close the two stability gaps not covered by the plan 01 force-cancel extension and plan 02 pinch logic — an inline channel-loss guard for mid-session RDPEI loss — and run the comprehensive on-device UAT that verifies all six Phase 3 requirements hold across lifecycle interrupts and reconnect.

Purpose: STAB-01 and STAB-02 are largely satisfied by the plan 01 force-cancel extension (all 5 lifecycle hooks + post_disconnect already call it, and it now clears gesture state + releases Ctrl). The remaining gap is an RDPEI channel drop mid-session (rdpei becomes NULL while a gesture is armed) which the existing hooks do not catch because no lifecycle event fires — this plan adds a small inline guard. The rest is the on-device UAT that proves the gestures and the cleanup hold on real hardware, which is the only authoritative verification for touchscreen + Windows RDP behavior.

Output: A channel-loss inline guard in xf_input_touch_remote and a completed 03-UAT.md record. No new files, settings, or state fields.
</objective>

<execution_context>
@.planning/phases/03-gestures-session-stability/03-CONTEXT.md
@.planning/phases/03-gestures-session-stability/03-RESEARCH.md
@.planning/phases/03-gestures-session-stability/03-01-PLAN.md
@.planning/phases/03-gestures-session-stability/03-02-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-rdpei-touch-lifecycle/02-UAT.md

@build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
@build/freerdp3-3.15.0+dfsg/client/X11/xf_client.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Channel-loss inline guard + reconnect no-replay assertion + build</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c</files>
  <read_first>
    - The plan 01 + plan 02 additions in xf_input.c (confirm the gesture path dereferences rdpei in the timer callback and the pinch claiming; the guard must run before any of those)
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c lines 646-830 (xf_input_touch_remote — where the guard goes)
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_client.c lines 1460-1473, 1667 (post_disconnect calls force_cancel before channel teardown; auto_reconnect_ex preserves process state)
    - build/freerdp3-3.15.0+dfsg/include/freerdp/client/rdpei.h lines 92-95 (SuspendTouch/ResumeTouch — reference for the channel-loss path)
  </read_first>
  <action>
    Per D-15 (channel loss), D-16 (reconnect no-replay).

    1. Channel-loss guard: at the top of xf_input_touch_remote (before the long-press/pinch state machine added in plans 01/02 dereferences rdpei), add: if the gesture state indicates an active gesture (lpArmed || lpFrozen || pinchActive || pinchClaimed || ctrlHeld) AND xfc->common.rdpei is NULL, call xf_touch_force_cancel(xfc) to release Ctrl, clear all gesture state, and arm the recovery gate. This catches an RDPEI channel drop that fires no X11 lifecycle event. Do NOT add a new hook call site — this is an inline check in the existing dispatch, matching the "do not add new call sites" constraint (D-15 extends the one function; the guard is a defensive check, not a new hook). After the guard, the existing Phase 2 fallback latch (rdpei==NULL -> mouse fallback) handles ordinary (non-gesture) input as before.

    2. Reconnect no-replay: confirm (do not add code unless a gap is found) that client_auto_reconnect_ex at xf_client.c:1667 does not re-arm lpArmed/pinchActive. Since post_disconnect (called on the full disconnect before reconnect retries) calls xf_touch_force_cancel which clears all gesture state, and the recovery gate requires all-fingers-up, no replay path exists. If a grep finds any code that sets lpArmed or pinchActive outside the TouchBegin path, add an explicit reset in the reconnect continuation — otherwise no change. Add a WLog_DBG in the reconnect continuation path noting gesture state was cleared (diagnostics only, DEBUG-gated, not a product behavior and not a Phase 4 DIAG block).

    3. Build: cmake --build obj-x86_64-linux-gnu -j$(nproc). Confirm zero errors.
  </action>
  <verify>
    <automated>cd build/freerdp3-3.15.0+dfsg && cmake --build obj-x86_64-linux-gnu -j$(nproc) 2>&1 | tail -3 && grep -c 'rdpei.*NULL\|xf_touch_force_cancel' client/X11/xf_input.c | head -1 && grep -n 'lpArmed\|pinchActive\|pinchClaimed' client/X11/xf_input.c | grep -v 'TouchBegin\|init\|force_cancel\|=' | head -5</automated>
  </verify>
  <done>Channel-loss guard present at the top of xf_input_touch_remote: if any gesture state is active and rdpei is NULL, force_cancel runs before any vtable dereference. No lpArmed/pinchActive assignment exists outside the TouchBegin path (reconnect cannot re-arm). Build succeeds. No new hook call sites added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-of-phase on-device UAT — all 6 requirements across lifecycle + reconnect</name>
  <what-built>
    Phase 3 gesture state machine (long-press right-click with slop deadband, native pinch passthrough, fallback Ctrl+wheel pinch with midpoint + reversal hysteresis), extended xf_touch_force_cancel clearing all gesture state + idempotent Ctrl release on every lifecycle hook, channel-loss guard, and reconnect-safe reset. Built as a patched freerdp3-x11 .deb on the OneMix 3 native X11 session.
  </what-built>
  <how-to-verify>
    On the OneMix 3, log into the GNOME-on-Xorg session (gnome-xorg.desktop). Launch the patched xfreerdp3 against a Windows target with WLOG_LEVEL=DEBUG and /multitouch. Run each scenario and record pass/fail in .planning/phases/03-gestures-session-stability/03-UAT.md (mirror the 02-UAT.md format):

    GEST-01 (long-press right-click): hold one finger still ~700ms on the Windows desktop — confirm exactly one right-click context menu opens at the touch point with no preceding left-click drag. Hold for the configured /touch-long-press:500 minimum and /touch-long-press:700 maximum — confirm each fires.

    GEST-02 (slop cancel + drag): start a hold, then move beyond /touch-slop before the threshold — confirm no right-click fires and an ordinary native drag continues. Confirm a sub-slop jitter (the device's natural 1-3px) does NOT cancel the hold (the root-cause fix).

    GEST-03 (native pinch): launch WITHOUT /touch-pinch-wheel-fallback; two-finger pinch in a touch-aware Windows app — confirm native pinch zoom and no wheel event.

    GEST-04 (fallback pinch): launch WITH /touch-pinch-wheel-fallback; two-finger pinch apart/together in a non-touch-aware app — confirm Ctrl+wheel zoom at the pinch midpoint, reversal does not chatter, one-finger lift releases Ctrl (next left-click is a plain click), third finger aborts cleanly.

    STAB-01 (lifecycle stability): with a finger down and a long-press armed AND with a two-finger fallback pinch active, trigger each interruption — (a) toggle fullscreen (Ctrl+Alt+Enter), (b) focus loss / FocusOut, (c) minimize / UnmapNotify, (d) resize / ConfigureNotify — and confirm no crash, no stale contacts, no stuck Ctrl (left-click is plain after), and fresh touches work after all fingers lift (recovery gate). Confirm the WLog_DBG output shows force_cancel clearing gesture state on each.

    STAB-02 (disconnect/reconnect): with an active long-press or pinch, disconnect the session — confirm no crash. Reconnect (auto-reconnect or manual) — confirm no spurious right-click/wheel fires on reconnect, the pinch mode is preserved, and touch works again after all fingers lift.

    Also: confirm the two temporary /* DIAG: */ blocks from Phase 2 were NOT built upon and are still absent from the quilt patch (STATE.md cleanup note).
  </how-to-verify>
  <resume-signal>Type "approved" with the 03-UAT.md results, or describe any failing scenario with the WLog_DBG excerpt so it can be fixed before packaging.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RDPEI channel state -> xfreerdp3 | A mid-session channel drop changes rdpei from non-NULL to NULL without an X11 lifecycle event; the inline guard treats this as untrusted and routes through force-cancel. |
| reconnect -> xfreerdp3 | Reconnect preserves process/settings state; per-gesture state must not survive (D-16). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | RDPEI channel loss mid-gesture -> NULL vtable dereference / crash | medium | mitigate | Inline guard at top of xf_input_touch_remote: if gesture state active and rdpei NULL, call xf_touch_force_cancel before any vtable use. |
| T-03-09 | Tampering | Reconnect replays interrupted gesture -> spurious right-click/wheel on new session | medium | mitigate | post_disconnect -> xf_touch_force_cancel clears all per-gesture state; reconnect never re-arms; recovery gate requires all-fingers-up; startup mode is a setting (D-16). |
| T-03-10 | Tampering/Elevation | Ctrl stuck after channel loss (no lifecycle event fires) | high | mitigate | The channel-loss guard routes through xf_touch_force_cancel which releases Ctrl idempotently via ctrlHeld (plan 01). |
</threat_model>

<verification>
- Build: cmake --build succeeds with zero errors.
- Channel-loss guard gate: present at the top of xf_input_touch_remote before any rdpei-> dereference.
- Reconnect no-replay gate: no lpArmed/pinchActive assignment outside the TouchBegin path (grep).
- No-new-hooks gate: no new xf_touch_force_cancel call sites added in xf_event.c or xf_client.c (the guard is inline).
- End-of-phase UAT (blocking human): all six requirement scenarios pass on the OneMix 3 native X11 session, recorded in 03-UAT.md.
</verification>

<success_criteria>
- STAB-01: touch usable without crashes, stale contacts, or duplicate events after windowed/fullscreen switch and focus loss/regain, with all gesture state cleared.
- STAB-02: disconnect/reconnect during an active gesture does not crash, no gesture is replayed, and touch works after reconnection.
- Channel-loss guard prevents a NULL-vtable crash.
- 03-UAT.md records all scenarios as pass.
</success_criteria>

<output>
Create .planning/phases/03-gestures-session-stability/03-03-SUMMARY.md and 03-UAT.md when done.
</output>