---
phase: 04-diagnostics-packaging-launch-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-touch.sh
  - /usr/local/bin/menu
autonomous: false
requirements: [CONF-01]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "scripts/launch-touch.sh composes +touch-pinch-wheel-fallback /touch-long-press:<ms> /touch-slop:<px> and omits +multitouch (D-01, D-15)"
    - "Default launch uses 600ms long-press and 8px slop with no prompt (D-13, D-17)"
    - "FREERDP_TOUCH_LONG_PRESS_MS accepts only unsigned decimal in 500-700; FREERDP_TOUCH_SLOP_PX accepts only unsigned decimal in 4-16; invalid values fail with a clear message before FreeRDP is invoked (D-16)"
    - "--mouse-only mode omits all touch activation/calibration options, bypasses calibration validation entirely (no touch options are composed), and preserves connection/session arguments unchanged (D-20)"
    - "FREERDP_TOUCH_DIAG=1 creates one private log via mktemp under ${XDG_STATE_HOME:-$HOME/.local/state}/freerdp-touch/ with umask 077 set before mkdir, directory permissions 0700, file permissions 0600, tees combined stderr+stdout, and exits with the FreeRDP client status via PIPESTATUS[0] (D-05, D-06, D-21)"
    - "Normal (non-diagnostic) launch execs the real client directly with no tee pipeline (D-05)"
    - "The wrapper never echoes $@, never uses eval, never sets WLOG_LEVEL=DEBUG, and never stores or persists passwords (D-06, D-19)"
    - "/usr/local/bin/menu option 3 delegates the FreeRDP invocation to the wrapper, preserves the TTY password prompt and startx/rotation flow, uses a private temp xinitrc (umask 077 + mktemp) with reliable cleanup, and contains no WLOG_LEVEL=DEBUG, no build-tree binary path, no touch options (D-18, D-22, D-23)"
    - { statement: "The wrapper does not expose classifier constants, dominance ratio, activation thresholds, or wheel-step constants as settings or flags (D-14)", verification: backstop }
  artifacts:
    - path: "scripts/launch-touch.sh"
      provides: "Credential-free launch wrapper with touch preset, calibration validation, mouse-only escape, and diagnostic tee"
      contains: "xfreerdp3"
    - path: "/usr/local/bin/menu"
      provides: "Updated menu option 3 that delegates to the wrapper instead of embedding a credential-bearing command"
  key_links:
    - from: "/usr/local/bin/menu"
      to: "scripts/launch-touch.sh"
      via: "menu option 3 calls the wrapper with connection/session arguments including the literal /p:$PASS handoff from the private external menu (D-19, D-23)"
      pattern: "launch-touch.sh"
    - from: "scripts/launch-touch.sh"
      to: "/usr/bin/xfreerdp3"
      via: "wrapper execs or tees the installed client with composed touch args plus forwarded $@"
      pattern: "xfreerdp3"
  prohibitions:
    - requirement_id: CONF-01
      category: privacy
      statement: "MUST NOT store or persist server passwords in committed artifacts, diagnostic logs, or the wrapper — the wrapper passes credentials as opaque arguments without logging them; the menu preserves the interactive TTY password prompt per D-23 with private temp file hygiene (umask 077 + mktemp + cleanup)"
    - requirement_id: CONF-01
      category: safety
      statement: "MUST NOT reintroduce +multitouch or native RDPEI touch forwarding in the launch preset or menu adapter (D-01, D-02)"
    - requirement_id: CONF-01
      category: values
      statement: "MUST NOT expose classifier constants, dominance ratio, activation thresholds, or wheel-step constants as user-facing settings or CLI flags (D-14)"
---

<objective>
Create the canonical credential-free launch wrapper that owns only the local-touch preset, and update the live menu to delegate to it instead of carrying a credential-bearing command.

Purpose: This is the launch-configuration tracer — it proves the wrapper composes the frozen touch options correctly, validates calibration, supports mouse-only and diagnostic modes, and delegates cleanly from the menu without credentials.
Output: scripts/launch-touch.sh, updated /usr/local/bin/menu
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-RESEARCH.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-PATTERNS.md
@scripts/check-x11-session.sh
@build/freerdp3-3.15.0+dfsg/client/common/cmdline.h
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create scripts/launch-touch.sh — credential-free wrapper with touch preset, calibration validation, mouse-only, and diagnostic tee</name>
  <files>scripts/launch-touch.sh</files>
  <read_first>
    - scripts/check-x11-session.sh (reuse the fail-closed native-X11 gate — call it, do not duplicate session detection)
    - scripts/build-baseline.sh (reuse set -eu, stderr progress, and fail-fast conventions)
    - 04-RESEARCH.md Pattern 3 "One wrapper, three modes" and Code Examples "Existing CLI values" and "Diagnostic wrapper pipeline"
    - 04-PATTERNS.md "scripts/launch-touch.sh" section — required command composition, validation policy, and pipeline pattern
    - build/freerdp3-3.15.0+dfsg/client/common/cmdline.h lines 497-502 (the three existing touch switches: touch-long-press, touch-pinch-wheel-fallback, touch-slop)
  </read_first>
  <action>
Create `scripts/launch-touch.sh` as a Bash script (Bash is required for arrays and PIPESTATUS[0] — Pitfall 5). The wrapper owns ONLY the local-touch preset; all FreeRDP connection/session arguments pass through unchanged.

Structure:
1. Shebang `#!/usr/bin/env bash`, `set -eu`.
2. Resolve the sibling gate script source-relative: compute `script_dir` from `BASH_SOURCE[0]` (not a relative or PATH lookup) so the gate resolves correctly regardless of the caller's working directory. The gate path is `"$script_dir/check-x11-session.sh"`.
3. Parse `--mouse-only` from `$@`: iterate all args, set a `mouse_only=1` flag when `--mouse-only` is found, and build a `fwd_args` array of all remaining arguments. This preserves argument boundaries and removes only the `--mouse-only` token. Never use eval (Pitfall 6, ASVS V5).
4. Compose touch args as a Bash array (D-01, D-15):
   - Mouse-only: set `touch_args=()` (omit all touch options — D-20) and skip calibration validation entirely since no touch options are composed. Irrelevant or invalid calibration env vars must NOT cause rejection in mouse-only mode.
   - Non-mouse-only: validate calibration overrides (D-15, D-16) BEFORE composing touch args:
     - `FREERDP_TOUCH_LONG_PRESS_MS`: default 600. Accept only unsigned decimal digits. Range 500-700. Reject non-digits or out-of-range with a clear stderr message and exit 1 before invoking FreeRDP.
     - `FREERDP_TOUCH_SLOP_PX`: default 8. Accept only unsigned decimal digits. Range 4-16. Reject non-digits or out-of-range with a clear stderr message and exit 1.
     - Validation must use a digits-only check (e.g., `case "$val" in *[!0-9]*|'') reject;; esac`) and arithmetic comparison (`[ "$val" -ge 500 ] && [ "$val" -le 700 ]`).
   - Non-mouse-only touch_args: `("+touch-pinch-wheel-fallback" "/touch-long-press:${lp_ms}" "/touch-slop:${slop_px}")`
   - Never include `+multitouch` (D-01, Pitfall 3).
5. Run the native-X11 gate: `"$script_dir/check-x11-session.sh"`. Fail closed if not native X11.
6. Normal mode (FREERDP_TOUCH_DIAG unset or not "1"): `exec /usr/bin/xfreerdp3 "${touch_args[@]}" "${fwd_args[@]}"` — direct exec, no tee, no subshell (D-05).
7. Diagnostic mode (FREERDP_TOUCH_DIAG=1):
   - Set `umask 077` BEFORE creating any directory or file (D-06).
   - Create log directory: `log_dir="${XDG_STATE_HOME:-$HOME/.local/state}/freerdp-touch"`; `mkdir -p "$log_dir"`; then explicitly `chmod 700 "$log_dir"` (umask alone does not narrow a pre-existing directory with wider permissions — chmod 700 is mandatory after mkdir -p).
   - Create log via mktemp inside the state directory: `log_path="$(mktemp "$log_dir/touch-XXXXXX.log")"` — not a predictable filename (D-05, D-06); then explicitly `chmod 600 "$log_path"`.
   - Verify directory permissions are 0700 and file permissions are 0600 (both via explicit chmod, not umask assumption).
   - Run with tee: `/usr/bin/xfreerdp3 "${touch_args[@]}" "${fwd_args[@]}" 2>&1 | tee "$log_path"`; `exit "${PIPESTATUS[0]}"` (Pitfall 5: preserve client exit status, not tee's).
   - Do NOT echo the constructed command, do NOT set WLOG_LEVEL=DEBUG, do NOT enable shell xtrace (D-05, D-06, Pitfall 6).
8. The wrapper must NOT read, store, prompt for, or echo passwords or connection credentials (D-19). FreeRDP's existing prompt handles authentication.

Make the script executable: `chmod +x scripts/launch-touch.sh`.

Do NOT add a production test hook or test-specific env var to the wrapper. The test approach uses a temporary rewritten copy with mocked gate and client paths: create a temp directory, copy the wrapper there, create a mock check-x11-session.sh that exits 0, create a mock xfreerdp3 that echoes its args to stderr and exits with a controlled status, then sed the wrapper copy to replace /usr/bin/xfreerdp3 with the mock path. Run the rewritten copy with various inputs and assert all modes and forwarding behavior.
  </action>
  <verify>
    <automated>bash -n scripts/launch-touch.sh && bash -c '
td=$(mktemp -d) || exit 1
trap "rm -rf $td" EXIT
cp scripts/launch-touch.sh "$td/launch-touch.sh" || exit 1
chmod +x "$td/launch-touch.sh"
echo '\''#!/usr/bin/env bash'\'' > "$td/check-x11-session.sh"
echo '\''exit 0'\'' >> "$td/check-x11-session.sh"
chmod +x "$td/check-x11-session.sh"
echo '\''#!/usr/bin/env bash'\'' > "$td/xfreerdp3"
echo '\''for a in "$@"; do printf "%s\n" "$a"; done'\'' >> "$td/xfreerdp3"
echo '\''exit ${MOCK_EXIT:-0}'\'' >> "$td/xfreerdp3"
chmod +x "$td/xfreerdp3"
sed -i "s|/usr/bin/xfreerdp3|$td/xfreerdp3|g" "$td/launch-touch.sh"
export XDG_STATE_HOME="$td/state"
fails=0
COMPLEX=("/v:my host" "/u:user" "" "/f")
# (a) Normal: 3 touch + 4 forwarded = 7 lines, args preserve boundaries
out=$("$td/launch-touch.sh" "${COMPLEX[@]}" 2>/dev/null)
[ "$(printf "%s\n" "$out" | wc -l)" -eq 7 ] || { echo FAIL:a-lines; fails=1; }
printf "%s\n" "$out" | grep -qx "+touch-pinch-wheel-fallback" || { echo FAIL:a1; fails=1; }
printf "%s\n" "$out" | grep -qx "/touch-long-press:600" || { echo FAIL:a2; fails=1; }
printf "%s\n" "$out" | grep -qx "/touch-slop:8" || { echo FAIL:a3; fails=1; }
printf "%s\n" "$out" | grep -qx "/v:my host" || { echo FAIL:a4; fails=1; }
printf "%s\n" "$out" | grep -qx "/u:user" || { echo FAIL:a5; fails=1; }
[ "$(printf "%s\n" "$out" | grep -c "^$")" -eq 1 ] || { echo FAIL:a6-empty; fails=1; }
printf "%s\n" "$out" | grep -qx "/f" || { echo FAIL:a7; fails=1; }
# (b) Valid overrides appear
out=$(FREERDP_TOUCH_LONG_PRESS_MS=650 FREERDP_TOUCH_SLOP_PX=10 "$td/launch-touch.sh" "${COMPLEX[@]}" 2>/dev/null)
printf "%s\n" "$out" | grep -qx "/touch-long-press:650" || { echo FAIL:b1; fails=1; }
printf "%s\n" "$out" | grep -qx "/touch-slop:10" || { echo FAIL:b2; fails=1; }
# (b2) Diagnostic mode forwards same touch + complex args through tee in order (boundaries preserved)
MOCK_EXIT=0 FREERDP_TOUCH_DIAG=1 FREERDP_TOUCH_LONG_PRESS_MS=650 FREERDP_TOUCH_SLOP_PX=10 "$td/launch-touch.sh" "${COMPLEX[@]}" >"$td/diag.out" 2>/dev/null
printf "%s\n%s\n%s\n%s\n%s\n%s\n%s\n" "+touch-pinch-wheel-fallback" "/touch-long-press:650" "/touch-slop:10" "/v:my host" "/u:user" "" "/f" > "$td/diag.expected"
diff "$td/diag.expected" "$td/diag.out" || { echo FAIL:b2-order; fails=1; }
# (c) Non-integer rejected before FreeRDP invoked
FREERDP_TOUCH_LONG_PRESS_MS=abc "$td/launch-touch.sh" "${COMPLEX[@]}" 2>&1 | grep -qi "invalid\|error\|reject" || { echo FAIL:c; fails=1; }
# (d) Out-of-range rejected
FREERDP_TOUCH_LONG_PRESS_MS=999 "$td/launch-touch.sh" "${COMPLEX[@]}" 2>&1 | grep -qi "range\|error" || { echo FAIL:d; fails=1; }
# (e) Mouse-only: no touch args, --mouse-only removed, args preserved, validation bypassed
out=$(FREERDP_TOUCH_LONG_PRESS_MS=abc "$td/launch-touch.sh" --mouse-only "${COMPLEX[@]}" 2>/dev/null)
[ "$(printf "%s\n" "$out" | wc -l)" -eq 4 ] || { echo FAIL:e-lines; fails=1; }
! printf "%s\n" "$out" | grep -qx "+touch-pinch-wheel-fallback" || { echo FAIL:e1; fails=1; }
! printf "%s\n" "$out" | grep -qx "/touch-long-press:.*" || { echo FAIL:e2; fails=1; }
! printf "%s\n" "$out" | grep -qx "/touch-slop:.*" || { echo FAIL:e3; fails=1; }
! printf "%s\n" "$out" | grep -qx -- "--mouse-only" || { echo FAIL:e4; fails=1; }
printf "%s\n" "$out" | grep -qx "/v:my host" || { echo FAIL:e5; fails=1; }
[ "$(printf "%s\n" "$out" | grep -c "^$")" -eq 1 ] || { echo FAIL:e6-empty; fails=1; }
printf "%s\n" "$out" | grep -qx "/f" || { echo FAIL:e7; fails=1; }
FREERDP_TOUCH_LONG_PRESS_MS=abc "$td/launch-touch.sh" --mouse-only "${COMPLEX[@]}" >/dev/null 2>&1 || { echo FAIL:e8-reject; fails=1; }
# (f) Diagnostic: log 0600, dir 0700, pre-existing 0755 dir becomes 0700
mkdir -p "$td/state/freerdp-touch"
chmod 755 "$td/state/freerdp-touch"
FREERDP_TOUCH_DIAG=1 "$td/launch-touch.sh" "${COMPLEX[@]}" >/dev/null 2>&1
[ "$(stat -c %a "$td/state/freerdp-touch")" = "700" ] || { echo FAIL:f1-dir; fails=1; }
logf=$(find "$td/state/freerdp-touch" -name "*.log" | head -1)
[ -n "$logf" ] || { echo FAIL:f2-log; fails=1; }
[ "$(stat -c %a "$logf")" = "600" ] || { echo FAIL:f3-mode; fails=1; }
# (g) Diagnostic exit status preserved
MOCK_EXIT=42 FREERDP_TOUCH_DIAG=1 "$td/launch-touch.sh" "${COMPLEX[@]}" >/dev/null 2>&1; [ $? -eq 42 ] || { echo FAIL:g; fails=1; }
# (h) Normal exit status preserved
MOCK_EXIT=7 "$td/launch-touch.sh" "${COMPLEX[@]}" >/dev/null 2>&1; [ $? -eq 7 ] || { echo FAIL:h; fails=1; }
# (i) No +multitouch
out=$("$td/launch-touch.sh" "${COMPLEX[@]}" 2>/dev/null)
! printf "%s\n" "$out" | grep -qx "+multitouch" || { echo FAIL:i; fails=1; }
# (j) Credential static checks on wrapper source
! grep -qE "^set -[a-z]*x" scripts/launch-touch.sh || { echo FAIL:j1-xtrace; fails=1; }
! grep -qw "eval" scripts/launch-touch.sh || { echo FAIL:j2-eval; fails=1; }
! grep -F '\''$PASS'\'' scripts/launch-touch.sh || { echo FAIL:j3-pass; fails=1; }
! grep -qE '\''(echo|printf).*\$@'\'' scripts/launch-touch.sh || { echo FAIL:j4-dump; fails=1; }
! grep -qEi "(echo|printf).*password" scripts/launch-touch.sh || { echo FAIL:j5-pw; fails=1; }
! grep -qEi "(echo|printf).*credential" scripts/launch-touch.sh || { echo FAIL:j6-cred; fails=1; }
exit $fails
'</automated>
  </verify>
  <done>
scripts/launch-touch.sh exists, is valid Bash, resolves the gate script source-relative via BASH_SOURCE, and passes all dry-run cases via a temp rewritten copy with mocked gate and client using delimiter-safe indexed (printf %s, one arg per line) output: default 600/8 composes exactly +touch-pinch-wheel-fallback /touch-long-press:600 /touch-slop:8 (7 output lines = 3 touch + 4 forwarded); valid overrides compose custom values; non-integer and out-of-range values are rejected before FreeRDP invocation; --mouse-only removes only --mouse-only, omits all touch args (4 output lines = 4 forwarded, no touch options), bypasses calibration validation entirely (invalid calibration env vars do NOT cause rejection in mouse-only mode), and preserves all opaque connection/session argument boundaries (spaces and empty values distinguishable); FREERDP_TOUCH_DIAG=1 creates a private log via mktemp under the XDG state directory with umask 077, explicit chmod 700 "$log_dir" after mkdir -p (pre-existing 0755 directory becomes 0700), explicit chmod 600 "$log_path" after mktemp, and preserves PIPESTATUS[0] (client exit status) in both normal and diagnostic modes. Isolated temporary XDG_STATE_HOME used in tests, never the real user state directory. No +multitouch in any mode. Credential static checks pass: no set -x, no eval, no $PASS reference, no echo/printf of $@, no echo/printf containing password or credential.
  </done>
  <acceptance_criteria>
- bash -n scripts/launch-touch.sh passes (valid Bash syntax)
- Normal mode with complex args (spaces, empty value): exactly 3 touch options + forwarded args = correct line count, all args preserve order and boundaries (empty arg produces an empty line, space-containing arg preserves the space)
- Valid calibration overrides (650/10) appear correctly in composed output, in both normal and diagnostic modes (diagnostic forwards the same touch + complex args through tee in exact order with boundaries preserved)
- Non-integer calibration value is rejected before FreeRDP is invoked
- Out-of-range calibration value is rejected before FreeRDP is invoked
- Mouse-only mode: --mouse-only is removed from forwarded args, no touch options appear, all opaque connection/session args preserve boundaries, invalid calibration env vars do NOT cause rejection
- Diagnostic mode: log created via mktemp with mode 0600, directory mode 0700, pre-existing mode-0755 directory becomes 0700 (explicit chmod, not umask assumption)
- Both normal and diagnostic client exit statuses propagate (MOCK_EXIT preserved)
- No +multitouch in any mode
- Credential static checks on wrapper source: no set -x, no eval, no $PASS, no echo/printf of $@, no echo/printf containing password or credential
- Tests use an isolated temporary XDG_STATE_HOME, never the real user state directory
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update /usr/local/bin/menu option 3 — concrete TTY/startx-preserving handoff to the wrapper</name>
  <files>/usr/local/bin/menu</files>
  <read_first>
    - /usr/local/bin/menu (current live menu — option 3 captures password via read -s, writes /tmp/.xinitrc-rdp with rotation + full FreeRDP command, uses build-tree binary, sets WLOG_LEVEL=DEBUG)
    - scripts/launch-touch.sh (the wrapper created in Task 1 — menu delegates the FreeRDP invocation to this)
    - 04-CONTEXT.md D-22, D-23 (canonical operating flow: TTY → startx → rotation → wrapper → installed xfreerdp3; menu changes minimal, preserve password-entry UX, file hygiene)
    - 04-RESEARCH.md Open Questions 1 and 2 (both RESOLVED — menu preserves TTY/startx/rotation, delegates FreeRDP invocation to wrapper)
    - scripts/check-x11-session.sh (the native-X11 gate — the xinitrc must set XDG_SESSION_TYPE=x11 so the gate passes in the startx session)
  </read_first>
  <precondition>The /usr/local/bin/menu file must be writable (use sudo if needed). The wrapper scripts/launch-touch.sh must exist and be executable from Task 1.</precondition>
  <action>
Update /usr/local/bin/menu option 3 to delegate the FreeRDP invocation to the wrapper while preserving the current TTY/startx/rotation interaction and startup flow per D-22 and D-23. This is an external file (not committed to the repo) — it is machine-specific by design.

Required changes to option 3 — preserve the current UX, apply minimum file hygiene (D-23):

1. Preserve the TTY password prompt: keep `read -s -p "RDP Password: " PASS` and the `echo` that follows it. The password-entry UX is preserved per D-23. The wrapper does not own or persist credentials (D-19) — it receives `/p:$PASS` as an opaque argument and forwards it to xfreerdp3.

2. Apply file hygiene for the xinitrc temp file: replace the predictable world-readable `/tmp/.xinitrc-rdp` path with `xinitrc="$(mktemp /tmp/.xinitrc-rdp-XXXXXX)"`. Immediately after successful mktemp and BEFORE writing the credential-bearing heredoc, install cleanup traps for EXIT, INT, HUP, and TERM: `trap 'rm -f "$xinitrc"' EXIT INT HUP TERM`. Then `chmod 600 "$xinitrc"` before writing the heredoc. This ensures the temp file containing `/p:$PASS` is private (0600), has an unpredictable name, and is cleaned up on normal return, interruption, or signal (D-23, Pitfall 6).

3. In the xinitrc heredoc, preserve the rotation commands unchanged: `xrandr --output eDP-1 --mode 1600x2560 --rotate left` and the `xinput set-prop "GXTP7386:00 27C6:0113" "Coordinate Transformation Matrix" 0 -1 1 1 0 0 0 0 1` (D-22).

4. In the xinitrc heredoc, add `export XDG_SESSION_TYPE=x11` before calling the wrapper so the wrapper's check-x11-session.sh gate passes in the startx session (D-22).

5. In the xinitrc heredoc, replace the build-tree binary path and its full FreeRDP command with a call to the wrapper: `/home/hoang/freerdp-touch/scripts/launch-touch.sh` followed by the connection/session arguments (`/v:192.168.1.10`, `/u:ngleh`, `/p:$PASS`, `/f`, `/smart-sizing:2560x1600`, `/scale-desktop:200`, `/clipboard`, `/sound:sys:pulse`, `/microphone:sys:pulse`, `/drive:Onemix,$HOME`, `/cert:ignore`) including the literal `/p:$PASS` handoff from the private external menu (D-23 — the wrapper stores/logs no credential; the menu preserves read-s and forwards /p:$PASS through private temporary xinit material). The wrapper adds the touch options (+touch-pinch-wheel-fallback, /touch-long-press:600, /touch-slop:8) — remove those from the menu command (D-18, D-23).

6. Remove `WLOG_LEVEL=DEBUG` from the xinitrc — diagnostics are now gated by FREERDP_TOUCH_DIAG in the wrapper (D-03, D-05, D-23).

7. After `startx "$xinitrc"` returns normally, add `rm -f "$xinitrc"` to remove the temp file containing the password. The EXIT/INT/HUP/TERM trap installed in step 2 serves as the backstop for interruption or signal (D-23).

The menu must NOT contain a duplicate of the full FreeRDP touch command — it delegates only the touch preset to the wrapper (D-18). The menu must NOT contain `WLOG_LEVEL=DEBUG` or the build-tree binary path. The touch options must NOT appear in the menu — the wrapper owns them (D-18, D-22, D-23). The `/p:$PASS` remains in the xinitrc heredoc but is written to a private temp file and cleaned up after startx (D-23).

Use sudo to write the file if needed: the executor should read the current menu, apply the changes, and write back with sudo if the file is not writable as the current user.
  </action>
  <verify>
    <automated>grep -q 'launch-touch.sh' /usr/local/bin/menu && ! grep -q 'WLOG_LEVEL=DEBUG' /usr/local/bin/menu && ! grep -q 'build/freerdp3' /usr/local/bin/menu && ! grep -q 'touch-pinch-wheel-fallback' /usr/local/bin/menu && ! grep -q '/touch-long-press' /usr/local/bin/menu && ! grep -q '/touch-slop' /usr/local/bin/menu && grep -q 'umask 077' /usr/local/bin/menu && grep -q 'mktemp' /usr/local/bin/menu && grep -q 'XDG_SESSION_TYPE' /usr/local/bin/menu && grep -q 'startx' /usr/local/bin/menu && grep -q 'xrandr' /usr/local/bin/menu && grep -q 'read -s' /usr/local/bin/menu && grep -q 'rm -f' /usr/local/bin/menu && ! grep -qE '^set -[a-z]*x' /usr/local/bin/menu && ! grep -qw 'eval' /usr/local/bin/menu && ! grep -qE '(echo|printf).*\$PASS' /usr/local/bin/menu && ! grep -qE '(echo|printf).*\$@' /usr/local/bin/menu && grep -qF '/p:$PASS' /usr/local/bin/menu && ! grep -qE '/p:[^$[:space:]]' /usr/local/bin/menu && (
td=$(mktemp -d) || exit 1
trap 'rm -rf "$td"' EXIT
mkdir -p "$td/bin"
cat > "$td/bin/startx" <<'SX'
#!/usr/bin/env bash
echo "$1" > "$MOCK_STARTX_PATH"
case "${MOCK_STARTX_MODE:-normal}" in
  normal) exit 0 ;;
  fail)   exit 1 ;;
  term)   kill -TERM "$PPID" 2>/dev/null; exit 1 ;;
esac
SX
chmod +x "$td/bin/startx"
printf '3\ndummypw\n\n6\n' > "$td/in.txt"
printf '3\ndummypw\n' > "$td/in-term.txt"
fails=0
TERM=dumb PATH="$td/bin:$PATH" MOCK_STARTX_MODE=normal MOCK_STARTX_PATH="$td/normal.path" bash /usr/local/bin/menu < "$td/in.txt" >/dev/null 2>&1 || true
[ ! -f "$(cat "$td/normal.path" 2>/dev/null)" ] || { echo FAIL:normal-cleanup; fails=1; }
TERM=dumb PATH="$td/bin:$PATH" MOCK_STARTX_MODE=fail MOCK_STARTX_PATH="$td/fail.path" bash /usr/local/bin/menu < "$td/in.txt" >/dev/null 2>&1 || true
[ ! -f "$(cat "$td/fail.path" 2>/dev/null)" ] || { echo FAIL:fail-cleanup; fails=1; }
TERM=dumb PATH="$td/bin:$PATH" MOCK_STARTX_MODE=term MOCK_STARTX_PATH="$td/term.path" bash /usr/local/bin/menu < "$td/in-term.txt" >/dev/null 2>&1 || true
[ ! -f "$(cat "$td/term.path" 2>/dev/null)" ] || { echo FAIL:term-cleanup; fails=1; }
exit $fails
)</automated>
  </verify>
  <acceptance_criteria>
- /usr/local/bin/menu option 3 calls scripts/launch-touch.sh (wrapper delegation)
- WLOG_LEVEL=DEBUG is NOT in the menu
- The build-tree binary path is NOT in the menu
- Touch options (+touch-pinch-wheel-fallback, /touch-long-press, /touch-slop) are NOT in the menu (wrapper owns them)
- umask 077 and mktemp are present (private temp file hygiene per D-23)
- XDG_SESSION_TYPE=x11 is set in the xinitrc (gate passes in startx session per D-22)
- startx, xrandr, and read -s (password prompt) are preserved (TTY/startx/rotation flow per D-22, D-23)
- rm -f cleanup of the temp file is present (reliable cleanup per D-23)
- Security static checks pass on the menu: no set -x, no eval, no echo/printf of $PASS or $@, no persistent credential file; the only permitted credential form is the exact variable form /p:$PASS (no literal /p:<value>)
- Dynamic copied-menu + mocked-startx verification proves the temp xinitrc is cleaned up after normal return (mock startx exit 0), failure return (mock startx exit 1), and interruption (mock startx sends SIGTERM to the menu, triggering the EXIT/INT/HUP/TERM trap). The existing read -s/startx/rotation UX is preserved (D-22, D-23)
  </acceptance_criteria>
  <done>
/usr/local/bin/menu option 3 delegates the FreeRDP invocation to scripts/launch-touch.sh, preserves the TTY password prompt (read -s) and startx/rotation flow, uses a private temp xinitrc (umask 077 + mktemp) with an EXIT/INT/HUP/TERM cleanup trap installed immediately after mktemp and BEFORE the credential-bearing heredoc, plus rm -f after startx returns, sets XDG_SESSION_TYPE=x11, removes WLOG_LEVEL=DEBUG, removes the build-tree binary path, and removes touch options from the menu (wrapper owns them). Security static checks pass: no set -x, no eval, no echo/printf of $PASS or $@, no persistent credential file, and the only credential form is the exact variable form /p:$PASS (no literal /p:<value>). A dynamic copied-menu + mocked-startx verification proves the temp xinitrc is cleaned up after normal return, failure return, and interruption (SIGTERM triggers the trap). The canonical operating flow is TTY → startx → rotation → wrapper → installed xfreerdp3 (D-22, D-23).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variables → wrapper | Untrusted env values (calibration overrides) cross here; must be validated before command construction |
| Wrapper → FreeRDP | The wrapper composes args and execs/tees the client; no credential material should cross |
| Menu → wrapper | The menu preserves the TTY `read -s` password prompt and forwards the literal `/p:$PASS` through private temporary xinit material; the wrapper stores/logs no credential (D-19, D-23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | Wrapper / menu echoing credentials | high | mitigate | No echo of $@ in wrapper, no eval, no xtrace; diagnostic logs use umask 077 and mktemp and contain no command args; the menu preserves the TTY password prompt but writes /p:$PASS only to a private temp file (umask 077 + mktemp) that is cleaned up after startx (D-06, D-22, D-23, Pitfall 6). |
| T-04-07 | Tampering | Malformed calibration env var | high | mitigate | Digits-only validation + range check before launch; Bash array-separated args, never eval or string-built commands (D-16, ASVS V5). Reject, never coerce. |
| T-04-08 | Information Disclosure | Menu password handling | high | mitigate | Delete WLOG_LEVEL=DEBUG from menu; preserve read -s password prompt and /p:$PASS per D-23 but write to a private temp file (umask 077 + mktemp) with reliable cleanup; the wrapper does not own or persist credentials (D-19). Automated grep verifies no WLOG_LEVEL=DEBUG and no build-tree binary in menu, and umask/mktemp present. |
| T-04-09 | Spoofing | Wrapper calling wrong binary | medium | mitigate | Use absolute /usr/bin/xfreerdp3 path, not a PATH-relative lookup (verified by debian/freerdp3-x11.install). |
</threat_model>

<verification>
**Automated/static (can run in any session):**
- `bash -n scripts/launch-touch.sh` (syntax check)
- Dry-run mock test via temp rewritten copy: defaults, valid overrides, non-integer rejection, out-of-range rejection, --mouse-only bypasses calibration validation and omits touch args, FREERDP_TOUCH_DIAG=1 log creation with 0700/0600 permissions, client exit status preserved
- grep confirms no +multitouch in wrapper or menu (D-01)
- grep confirms no WLOG_LEVEL=DEBUG in menu, no build-tree binary in menu, no touch options in menu (D-22, D-23)
- grep confirms umask 077, mktemp, XDG_SESSION_TYPE, startx, xrandr, read -s, rm -f cleanup in menu (D-22, D-23)
- grep confirms no eval in wrapper (ASVS V5)

**Native-X11/on-device (deferred to Plan 03 verification):**
- Normal launch through menu option 3 reaches Windows desktop with working touch gestures
- FREERDP_TOUCH_DIAG=1 through menu produces a timestamped log file
- --mouse-only through menu works without touch gestures
</verification>

<success_criteria>
1. scripts/launch-touch.sh composes the frozen touch preset, validates calibration, supports --mouse-only and diagnostic tee, and is credential-free — CONF-01 wrapper artifact complete.
2. /usr/local/bin/menu option 3 delegates the FreeRDP invocation to the wrapper, preserves the TTY password prompt and startx/rotation flow, uses a private temp xinitrc with reliable cleanup, and removes WLOG_LEVEL=DEBUG, build-tree binary, and touch options — D-18, D-22, D-23 complete.
3. No +multitouch, no eval, no unrequested calibration knobs — D-01, D-14, D-15 preserved.
4. FreeRDP exit status preserved via PIPESTATUS[0] in diagnostic mode — Pitfall 5 addressed.
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| `scripts/launch-touch.sh` | Bash script | repo (committed) | Canonical credential-free launch wrapper |
| `/usr/local/bin/menu` option 3 | menu entry | external (not committed) | Delegates FreeRDP invocation to wrapper; preserves TTY password prompt and startx/rotation flow (D-22, D-23) |
| `FREERDP_TOUCH_LONG_PRESS_MS` | environment variable | process env | Long-press calibration override (500-700, default 600) |
| `FREERDP_TOUCH_SLOP_PX` | environment variable | process env | Touch slop calibration override (4-16, default 8) |
| `--mouse-only` | wrapper mode flag | wrapper CLI | Escape hatch: omit touch options, keep connection args |
| `FREERDP_TOUCH_DIAG=1` | environment variable | process env | Diagnostic mode: tee to XDG state log |