---
phase: 04-diagnostics-packaging-launch-configuration
plan: 06
type: execute
wave: 2
depends_on: [04-04, 04-05]
files_modified:
  - .planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md
autonomous: false
gap_closure: true
requirements: [DIAG-01, PACK-01, PACK-02, CONF-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh build via the fixed scripts/build-release.sh produces dist as a symlink to a bundle with exactly 4 .deb files with exact package names (freerdp3-x11, libwinpr3-3, libfreerdp3-3, libfreerdp-client3-3), each at version 3.15.0+dfsg-2.1+deb13u3+onemix1 with amd64 architecture, plus SHA256SUMS (PACK-01)"
    - "Two clean builds produce the same exact basenames, Package/Version/Architecture for every .deb; each build's SHA256SUMS independently validates; cross-build .deb byte equality is NOT required since Debian metadata may embed timestamps (PACK-01 reproducibility per 04-01's locked definition)"
    - "An interrupted build (TERM signal) exits with code 143 (128+15) and the prior valid dist bundle is preserved; deterministic pre-swap and post-swap interruption both tested (GAP-06 closure)"
    - "All production-path regression checks pass: classifier (standalone), consolidated internal-helper check (WR-01 classifier with production constants, GAP-02 cancel-twice recovery, GAP-03 bounds-End gate, GAP-04 fractional pinch both directions, GAP-10 diagnostic snapshot last-coordinate cancel with duplicate Begin), ownership layout, parser accepted/rejected including overflow against the just-built dist package, wrapper production (real wrapper copy with only two absolute paths rewritten to mocks through startx mock), README doc regression (exact marked block text), X11 gate with Wayland-negative cases, clean extraction + quilt apply + build, no-+multitouch across patch/wrapper/menu"
    - "On-device verification confirms: install succeeds with /cert:tofu, touch gestures work, diagnostic log contains accurate fallback cancel evidence (event=cancel from diagnostic lifecycle store at last Update coordinates with diag count > 0), quiet launch produces no touch-diag markers, mouse-only works, rollback restores all four packages to stock (DIAG-01, PACK-02, CONF-01)"
    - "The updated verify-record file conforms to the deterministic state-machine grammar and passes the inline Python/stdlib parser with all negative fixtures"
    - "All on-device checks use the three locked menu invocations only (D-24): menu, FREERDP_TOUCH_DIAG=1 menu, menu --mouse-only"
  artifacts:
    - path: ".planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md"
      provides: "Gap-closure on-device verification evidence for all 10 checks with updated diagnostic lifecycle store evidence"
      contains: "check-x11-session"
    - path: "dist (relative symlink, gitignored generated output)"
      provides: "Updated validated 4-package bundle built from the fixed patch and fixed build script"
      contains: "SHA256SUMS"
    - path: ".dist-bundle-* (bundle directory, gitignored generated output)"
      provides: "Generated release bundle directory with 4 .deb files + SHA256SUMS"
      contains: "freerdp3-x11"
  key_links:
    - from: "scripts/build-release.sh"
      to: "patches/onemix-touch.patch"
      via: "fixed build script applies the re-crystallized patch to a fresh extraction and builds the 4-package closure"
      pattern: "onemix-touch.patch"
  prohibitions:
    - requirement_id: PACK-02
      category: safety
      statement: "MUST NOT install a broad freerdp3-*.deb glob — only the explicit 4-package closure (D-10)"
    - requirement_id: PACK-01
      category: safety
      statement: "MUST NOT create an APT hold or pin — the +onemix1 build may be replaced by a future Debian security update (D-08)"
    - requirement_id: DIAG-01
      category: privacy
      statement: "MUST NOT log credentials or /p: values in diagnostic output — the verify-record uses sanitized evidence only (D-06)"
---

<objective>
Rebuild the exact four-package release bundle from the fixed patch and fixed build script, rerun all production-path and existing automated regressions with exact package/version/architecture assertions and deterministic pre/post-swap interruption coverage, and perform native-device verification with updated evidence to close all confirmed gaps before Phase 4 can seal.

Purpose: This is the final gap-closure verification. It depends on both Plan 04-04 (source fixes + re-crystallized patch + production-path regressions) and Plan 04-05 (script/doc/menu fixes + WR-02/WR-03 test improvements). It proves the fixes work end-to-end: the bundle builds with the exact four-package closure, automated tests pass including production-path regressions, and on-device verification confirms the defect closures with accurate diagnostic lifecycle store evidence.
Output: Rebuilt dist/ bundle, passing full automated regression suite, and 04-06-verify-record.md with updated on-device evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-VERIFICATION.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-01-PLAN.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-03-PLAN.md
@scripts/build-release.sh
@scripts/launch-touch.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild 4-package bundle with fixed build script and rerun all automated regressions</name>
  <files>(no tracked source files modified — dist and .dist-bundle-* are generated gitignored outputs)</files>
  <read_first>
    - scripts/build-release.sh (fixed in Plan 04-05 — EXIT-only cleanup, 128+signal handlers)
    - patches/onemix-touch.patch (re-crystallized in Plan 04-04 — all six source fixes with diagnostic lifecycle store)
    - scripts/launch-touch.sh (fixed in Plan 04-05 — overlong/noncanonical rejection)
    - tests/gap01_ownership_layout.c, gap02_cancel_recovery.c, gap03_bounds_end.c, gap04_pinch_fractional.c, gap05_parser_check.sh, gap10_diag_snapshot.c, classifier_production_check.c (created in Plan 04-04 — production-path regressions)
    - tests/wrapper_production_check.sh (created in Plan 04-05 — WR-02 real wrapper test)
    - tests/check_x11_session_check.sh (extended in Plan 04-05 — WR-03 Wayland-negative cases)
    - tests/menu_diagnostic_env_check.sh (existing menu diagnostic env propagation regression)
    - tests/pinch_reversal_check.c (existing pinch reversal check)
    - 04-01-PLAN.md Task 3 STEP 4-5 (the original reproducibility and interruption test patterns)
    - 04-VERIFICATION.md GAP-06 (signal-safe publication closure requirements)
  </read_first>
  <precondition>Plan 04-04 Task 2 is complete (patch re-crystallized, applies cleanly, builds). Plan 04-05 Task 1 is complete (build-release.sh fixed, launch-touch.sh fixed, menu /cert:tofu).</precondition>
  <action>
Rebuild the release bundle using the fixed build script and run all automated regressions to prove the gap fixes work. No tracked source files are modified in this task — `dist` and `.dist-bundle-*` are generated gitignored outputs.

STEP 1 — Rebuild the 4-package bundle with exact package/version/architecture assertions:
Remove the old dist symlink: `rm -f dist`. Run the fixed build script: `./scripts/build-release.sh`. Verify: `[ -L dist ]`, `[ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ]`, `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes. Define the exact expected package-name array: `EXPECTED_PKGS="freerdp3-x11 libfreerdp-client3-3 libfreerdp3-3 libwinpr3-3"`. For EVERY .deb file in the bundle, assert via `dpkg-deb -f <file> Package`, `dpkg-deb -f <file> Version`, and `dpkg-deb -f <file> Architecture`: Package must be one of the four expected names (no extras, no missing), Version must be exactly `3.15.0+dfsg-2.1+deb13u3+onemix1`, Architecture must be `amd64`. Assert the sorted set of Package names matches the sorted expected set exactly.

STEP 2 — Reproducibility with package identity comparison (not cross-build SHA equality):
Save the first-run package identity: for each .deb, record basename, Package, Version, and Architecture into a normalized file. Do NOT include SHA256 hashes in the cross-build comparison — Debian metadata may embed timestamps so .deb byte content can differ between builds while package identity remains stable (per 04-01's locked reproducibility definition). Remove dist: `rm -f dist`. Run `./scripts/build-release.sh` again. Generate the second-run package identity the same way. Assert: the sorted list of basenames matches, every Package/Version/Architecture matches, and the normalized identity manifests are identical (diff returns no differences). Independently validate each build's checksums: `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes on both builds. Do not add SOURCE_DATE_EPOCH or any deterministic-build input unless explicitly required.

STEP 3 — Signal-safe interruption test (GAP-06 closure) with deterministic pre/post-swap coverage:
Record `before_target="$(readlink -f dist)"` and `before_checksums` (sha256sum of all .debs in the prior bundle). Use explicit pre/post-swap marker injection plus bounded polling (not sleep):

**Pre-swap interruption:** Copy build-release.sh to a temp file. Inject a marker line `echo "PRE_SWAP_MARKER" >&2; sleep 30` immediately BEFORE the `mv -Tf "$tmp_link" dist` line (the atomic swap). Run the modified script in the background. Poll for the marker string in the background process's stderr output (bounded loop: check stderr file for "PRE_SWAP_MARKER" up to 60 times with 1-second sleep). Once the marker appears, send TERM: `kill -TERM "$test_pid"`. Use `set +e` around the wait: `set +e; wait "$test_pid" 2>/dev/null; test_rc=$?; set -e`. Assert: `test_rc -eq 143` (128+15, not 0), `readlink -f dist` still equals `$before_target`, and the prior bundle checksums are unchanged (diff before/after sha256 files).

**Post-swap interruption:** Copy build-release.sh to a fresh temp file. Inject a marker line `echo "POST_SWAP_MARKER" >&2; sleep 30` immediately AFTER the `mv -Tf "$tmp_link" dist` line. Run in background. Poll for "POST_SWAP_MARKER" in stderr. Once seen, send TERM. `set +e; wait "$test_pid" 2>/dev/null; test_rc=$?; set -e`. Assert: `test_rc -eq 143`, `dist` now points to the NEW bundle (not `$before_target`), the new bundle persists (directory exists), and the new bundle's checksums validate (`sha256sum -c SHA256SUMS` in the new bundle passes).

STEP 4 — Full automated regression suite:
Run ALL existing automated checks and the new gap-closure production-path regressions:
1. Classifier regression (standalone model): `cc -DWITH_XI -o /tmp/test_scroll_classifier build/freerdp3-3.15.0+dfsg/client/X11/test_scroll_classifier.c -lm && /tmp/test_scroll_classifier` — prints OK.
2. Consolidated production-path regression (WR-01, GAP-02, GAP-03, GAP-04, GAP-10): `grep '#define PINCH_' build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c > /tmp/xf_prod_constants.h && cc -std=c17 -include /tmp/xf_prod_constants.h -I build/freerdp3-3.15.0+dfsg/client/X11 -o /tmp/xf_touch_internal_check tests/xf_touch_internal_check.c -lm && /tmp/xf_touch_internal_check` — prints OK. Calls the same shared helpers as production; covers classifier, cancel-twice recovery, bounds-End, fractional pinch, and diagnostic snapshot with duplicate Begin.
3. Ownership layout (GAP-01): `cc -std=c17 -o /tmp/gap01_ownership_layout tests/gap01_ownership_layout.c && /tmp/gap01_ownership_layout` — prints OK.
4. Parser (GAP-05): `bash tests/gap05_parser_check.sh "$(readlink -f dist)"` — prints OK. Passes the just-built dist package, not a stale working-tree build.
5. X11 gate regression (with WR-03 Wayland-negative cases): `bash tests/check_x11_session_check.sh` — passes including Wayland rejection.
6. Menu diagnostic env regression: `bash tests/menu_diagnostic_env_check.sh` — passes.
7. Wrapper production check (WR-02): `bash tests/wrapper_production_check.sh` — passes (real wrapper copy with only two absolute paths rewritten to mocks, run through startx mock).
8. README doc regression: `bash tests/readme_doc_regression.sh` — passes (extracts and executes exact marked README block text with fake dist/dpkg-query).
9. Pinch reversal check: `cc -o /tmp/pinch_reversal_check tests/pinch_reversal_check.c -lm && /tmp/pinch_reversal_check` — passes.
10. Wrapper validation regression (overlong/noncanonical): run the full mock test from 04-05 Task 1 verify (both calibration variables, overlong 84-digit, leading-zero 0600, valid boundaries 500/700/4/16) — all pass.
11. Patch clean-apply: extract a fresh tree, apply the re-crystallized patch via quilt push -a, build via dpkg-buildpackage — succeeds.
12. Cert:tofu in menu: `grep -q '/cert:tofu' /usr/local/bin/menu`; assert /cert:ignore absent via if-grep-fail.
13. No +multitouch across patch, wrapper, and menu: assert absent via if-grep-fail on each file.
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
# STEP 1: Rebuild with exact package/version/architecture assertions
rm -f dist; ./scripts/build-release.sh
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
EXPECTED_PKGS_SORTED="freerdp3-x11 libfreerdp-client3-3 libfreerdp3-3 libwinpr3-3"
for deb in "$(readlink -f dist)"/*.deb; do
  pkg=$(dpkg-deb -f "$deb" Package)
  ver=$(dpkg-deb -f "$deb" Version)
  arch=$(dpkg-deb -f "$deb" Architecture)
  [ "$ver" = "3.15.0+dfsg-2.1+deb13u3+onemix1" ] || { echo "FAIL: version $ver for $pkg"; exit 1; }
  [ "$arch" = "amd64" ] || { echo "FAIL: arch $arch for $pkg"; exit 1; }
  echo "$EXPECTED_PKGS_SORTED" | tr ' ' '\n' | grep -qxF "$pkg" || { echo "FAIL: unexpected package $pkg"; exit 1; }
done
actual_sorted=$(for deb in "$(readlink -f dist)"/*.deb; do dpkg-deb -f "$deb" Package; done | sort)
expected_sorted=$(echo "$EXPECTED_PKGS_SORTED" | tr ' ' '\n' | sort)
[ "$actual_sorted" = "$expected_sorted" ] || { echo "FAIL: package set mismatch"; exit 1; }
# STEP 2: Reproducibility with package identity comparison (no cross-build SHA equality)
gen_identity() {
  local dir=$(readlink -f dist)
  for deb in "$dir"/*.deb; do
    basename=$(basename "$deb")
    pkg=$(dpkg-deb -f "$deb" Package)
    ver=$(dpkg-deb -f "$deb" Version)
    arch=$(dpkg-deb -f "$deb" Architecture)
    echo "$basename $pkg $ver $arch"
  done | sort
}
gen_identity > /tmp/gap-identity-1.txt
# Independently validate first build's checksums
(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
rm -f dist; ./scripts/build-release.sh
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
gen_identity > /tmp/gap-identity-2.txt
diff /tmp/gap-identity-1.txt /tmp/gap-identity-2.txt
# STEP 3a: Pre-swap TERM interruption with marker injection
before_target="$(readlink -f dist)"
sha256sum "$(readlink -f dist)"/*.deb > /tmp/gap-dist-before.sha256
test_pre=$(mktemp /tmp/build-release-pre-XXXXXX.sh)
cp scripts/build-release.sh "$test_pre"
sed -i '/mv -Tf "$tmp_link" dist/i\echo "PRE_SWAP_MARKER" \>\&2; sleep 30' "$test_pre"
chmod +x "$test_pre"
"$test_pre" > /tmp/gap-pre.out 2>/tmp/gap-pre.err & test_pid=$!
for i in $(seq 1 60); do grep -q 'PRE_SWAP_MARKER' /tmp/gap-pre.err 2>/dev/null && break; sleep 1; done
kill -TERM "$test_pid" 2>/dev/null
set +e; wait "$test_pid" 2>/dev/null; test_rc=$?; set -e
[ "$test_rc" -eq 143 ] || { echo "FAIL:pre-swap exit=$test_rc"; exit 1; }
[ "$(readlink -f dist)" = "$before_target" ] || { echo "FAIL:pre-swap dist changed"; exit 1; }
sha256sum "$(readlink -f dist)"/*.deb > /tmp/gap-dist-after-pre.sha256
diff /tmp/gap-dist-before.sha256 /tmp/gap-dist-after-pre.sha256
rm -f "$test_pre"
# STEP 3b: Post-swap TERM interruption with marker injection
test_post=$(mktemp /tmp/build-release-post-XXXXXX.sh)
cp scripts/build-release.sh "$test_post"
sed -i '/mv -Tf "$tmp_link" dist/a\echo "POST_SWAP_MARKER" \>\&2; sleep 30' "$test_post"
chmod +x "$test_post"
"$test_post" > /tmp/gap-post.out 2>/tmp/gap-post.err & test_pid=$!
for i in $(seq 1 60); do grep -q 'POST_SWAP_MARKER' /tmp/gap-post.err 2>/dev/null && break; sleep 1; done
kill -TERM "$test_pid" 2>/dev/null
set +e; wait "$test_pid" 2>/dev/null; test_rc=$?; set -e
[ "$test_rc" -eq 143 ] || { echo "FAIL:post-swap exit=$test_rc"; exit 1; }
new_target=$(readlink -f dist)
[ "$new_target" != "$before_target" ] || { echo "FAIL:post-swap dist not updated"; exit 1; }
[ -d "$new_target" ] || { echo "FAIL:post-swap new bundle missing"; exit 1; }
(cd "$new_target" && sha256sum -c SHA256SUMS)
rm -f "$test_post"
# STEP 4: Full automated regression suite
# Classifier (standalone + production-path)
cc -DWITH_XI -o /tmp/test_scroll_classifier build/freerdp3-3.15.0+dfsg/client/X11/test_scroll_classifier.c -lm && /tmp/test_scroll_classifier
# Consolidated production-path regression (calls same helpers as production)
grep '#define PINCH_' build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c > /tmp/xf_prod_constants.h
cc -std=c17 -include /tmp/xf_prod_constants.h -I build/freerdp3-3.15.0+dfsg/client/X11 -o /tmp/xf_touch_internal_check tests/xf_touch_internal_check.c -lm && /tmp/xf_touch_internal_check
# Ownership layout probe
cc -std=c17 -o /tmp/gap01_ownership_layout tests/gap01_ownership_layout.c && /tmp/gap01_ownership_layout
# Parser (GAP-05) against the just-built dist package
bash tests/gap05_parser_check.sh "$(readlink -f dist)"
# Existing regressions
bash tests/check_x11_session_check.sh
bash tests/menu_diagnostic_env_check.sh
bash tests/wrapper_production_check.sh
bash tests/readme_doc_regression.sh
cc -o /tmp/pinch_reversal_check tests/pinch_reversal_check.c -lm && /tmp/pinch_reversal_check
# Wrapper validation (overlong/noncanonical from 04-05)
td=$(mktemp -d); trap 'rm -rf "$td"' EXIT
cp scripts/launch-touch.sh "$td/launch-touch.sh"; chmod +x "$td/launch-touch.sh"
echo '#!/usr/bin/env bash' > "$td/check-x11-session.sh"; echo 'exit 0' >> "$td/check-x11-session.sh"; chmod +x "$td/check-x11-session.sh"
echo '#!/usr/bin/env bash' > "$td/xfreerdp3"; echo 'exit 0' >> "$td/xfreerdp3"; chmod +x "$td/xfreerdp3"
sed -i "s|/usr/bin/xfreerdp3|$td/xfreerdp3|g" "$td/launch-touch.sh"
LONG=$(printf '9%.0s' $(seq 1 84))
set +e; XDG_STATE_HOME="$td/s1" FREERDP_TOUCH_LONG_PRESS_MS="$LONG" "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:long-lp; exit 1; }
set +e; XDG_STATE_HOME="$td/s2" FREERDP_TOUCH_SLOP_PX="$LONG" "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:long-slop; exit 1; }
set +e; XDG_STATE_HOME="$td/s3" FREERDP_TOUCH_LONG_PRESS_MS=0600 "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:leading-zero; exit 1; }
XDG_STATE_HOME="$td/s4" FREERDP_TOUCH_LONG_PRESS_MS=600 "$td/launch-touch.sh" /f >/dev/null 2>&1
XDG_STATE_HOME="$td/s5" FREERDP_TOUCH_LONG_PRESS_MS=500 "$td/launch-touch.sh" /f >/dev/null 2>&1
XDG_STATE_HOME="$td/s6" FREERDP_TOUCH_SLOP_PX=16 "$td/launch-touch.sh" /f >/dev/null 2>&1
# Patch clean-apply
rm -rf /tmp/onemix-verify-gap06
dpkg-source -x build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc /tmp/onemix-verify-gap06 2>&1 | tail -3
cp patches/onemix-touch.patch /tmp/onemix-verify-gap06/debian/patches/
printf 'onemix-touch.patch\n' >> /tmp/onemix-verify-gap06/debian/patches/series
(cd /tmp/onemix-verify-gap06 && QUILT_PATCHES=debian/patches quilt push -a 2>&1 | tail -3)
[ "$(cd /tmp/onemix-verify-gap06 && dpkg-parsechangelog -SVersion)" = "3.15.0+dfsg-2.1+deb13u3+onemix1" ]
# Cert:tofu and no-+multitouch
grep -q '/cert:tofu' /usr/local/bin/menu
if grep -q '/cert:ignore' /usr/local/bin/menu; then echo "FAIL: /cert:ignore in menu"; exit 1; fi
if grep -q '+multitouch' patches/onemix-touch.patch; then echo "FAIL: +multitouch in patch"; exit 1; fi
if grep -q '+multitouch' scripts/launch-touch.sh; then echo "FAIL: +multitouch in wrapper"; exit 1; fi
if grep -q '+multitouch' /usr/local/bin/menu; then echo "FAIL: +multitouch in menu"; exit 1; fi
# README fail-closed (GAP-08/09) with markers and doc regression
grep -q '# BEGIN: checksum-verify' README.md
grep -q '# BEGIN: rollback-check' README.md
grep -qi 'DPKG-QUERY FAILED' README.md
grep -qi 'INCOMPLETE CLOSURE' README.md
bash tests/readme_doc_regression.sh
echo "REBUILD-AND-REGRESSION-PASS"</automated>
  </verify>
  <done>
The fixed build-release.sh produces dist as a symlink to a bundle with exactly 4 .deb files with exact package names (freerdp3-x11, libfreerdp-client3-3, libfreerdp3-3, libwinpr3-3), each at version 3.15.0+dfsg-2.1+deb13u3+onemix1 with amd64 architecture, plus SHA256SUMS. Two clean builds produce identical package identity manifests (basenames, Package, Version, Architecture); each build's SHA256SUMS independently validates; cross-build .deb byte equality is NOT required. Pre-swap TERM interruption exits 143 and preserves the prior valid dist bundle (same target and checksums); post-swap TERM interruption exits 143 and the new bundle persists with valid checksums. All production-path and existing automated regressions pass: classifier (standalone), consolidated internal-helper check (WR-01 classifier, GAP-02 cancel recovery, GAP-03 bounds End, GAP-04 fractional pinch, GAP-10 diagnostic snapshot with duplicate Begin), ownership layout, parser against dist package, X11 gate (with Wayland-negative), menu diagnostic env, wrapper production (real wrapper copy with only two paths rewritten), README doc regression (exact marked block text), pinch reversal, wrapper overlong/noncanonical, patch clean-apply, cert:tofu, no +multitouch across patch/wrapper/menu.
  </done>
  <acceptance_criteria>
  - dist is a relative symlink resolving to a bundle with exactly 4 .deb files
  - Every .deb has exact Package (one of freerdp3-x11, libfreerdp-client3-3, libfreerdp3-3, libwinpr3-3), Version (3.15.0+dfsg-2.1+deb13u3+onemix1), and Architecture (amd64) — no extras, no missing
  - (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS) passes
  - Two clean builds produce identical package identity manifests (basenames + Package/Version/Architecture); each build's SHA256SUMS independently validates; no cross-build SHA equality required
  - Pre-swap TERM interruption exits 143 and preserves prior valid dist bundle (same target and checksums)
  - Post-swap TERM interruption exits 143 and new bundle persists with valid checksums
  - Classifier regression (standalone) prints OK
  - Consolidated xf_touch_internal_check passes (WR-01 classifier, GAP-02 cancel recovery, GAP-03 bounds End, GAP-04 fractional pinch, GAP-10 diagnostic snapshot with duplicate Begin)
  - tests/gap01_ownership_layout.c compiles and passes
  - tests/gap05_parser_check.sh passes against the just-built dist package (accepted/rejected including overflow)
  - tests/check_x11_session_check.sh passes including Wayland-negative cases (WR-03)
  - tests/wrapper_production_check.sh passes (WR-02 — real wrapper copy with only two absolute paths rewritten to mocks, run through startx mock)
  - tests/readme_doc_regression.sh passes (extracts and executes exact marked README block text)
  - tests/menu_diagnostic_env_check.sh passes
  - tests/pinch_reversal_check.c compiled and passes
  - Wrapper overlong (84-digit) and leading-zero (0600) rejection tests pass
  - Patch clean-apply: fresh extraction + quilt push -a + dpkg-buildpackage succeeds; changelog returns +onemix1
  - /cert:tofu present in /usr/local/bin/menu, /cert:ignore absent
  - README.md contains DPKG-QUERY FAILED and INCOMPLETE CLOSURE fail-closed messages with stable markers; doc regression passes
  - No +multitouch in patches/onemix-touch.patch, scripts/launch-touch.sh, or /usr/local/bin/menu
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: On-device gap-closure verification — install, touch, diagnostics with fallback cancel evidence, quiet, mouse-only, rollback</name>
  <files>.planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md</files>
  <read_first>
    - README.md (updated operations guide with subshell checksum and fail-closed rollback)
    - dist/SHA256SUMS (the rebuilt bundle from Task 1)
    - scripts/launch-touch.sh (fixed wrapper with overlong rejection)
    - /usr/local/bin/menu (updated with /cert:tofu)
    - 04-03-verify-record.md (the original verification record — use as the grammar template)
    - 04-03-PLAN.md Task 2 (the deterministic state-machine grammar and inline parser — reuse exactly)
    - 04-VERIFICATION.md GAP-10 (diagnostic lifecycle store cancel contract closure)
    - 04-CONTEXT.md D-22, D-24 (canonical operating flow and locked invocations)
  </read_first>
  <precondition>Task 1 is complete: the rebuilt bundle passes all automated regressions and the signal-safe interruption test.</precondition>
  <action>
This is the native-X11 on-device gap-closure verification checkpoint. It must be performed in the user's confirmed TTY/startx path per D-22. All on-device checks use the three locked menu invocations only (D-24): `menu` (normal), `FREERDP_TOUCH_DIAG=1 menu` (diagnostic), `menu --mouse-only` (mouse-only) — never invoke the wrapper bare from the TTY.

Create the verify-record file at `.planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md` using the SAME deterministic state-machine grammar as 04-03-verify-record.md: exactly ten sections numbered 1-10, each five contiguous lines (heading `N. **<title>**`, then `Command:`, `Expected:`, `Actual: PASS`, `Notes:`), raw byte inspection before decode, no CR/control/brackets/forbidden keywords, `<connection-args>` placeholder for sections 5-8, allowed character set for Notes. The inline Python/stdlib parser from 04-03-PLAN Task 2 is reused exactly (same parser, same negative fixtures).

The ten checks are the same as 04-03 but with updated expectations for the gap closures:

1. **Session gate**: Run `./scripts/check-x11-session.sh` — must print OK.
2. **Checksum verification**: `cd /home/hoang/freerdp-touch && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` — must pass (subshell pattern from GAP-08 fix).
3. **Install**: `sudo apt install -y --allow-downgrades ./dist/libwinpr3-3_*_amd64.deb ./dist/libfreerdp3-3_*_amd64.deb ./dist/libfreerdp-client3-3_*_amd64.deb ./dist/freerdp3-x11_*_amd64.deb` — must succeed.
4. **Version verification (all four packages)**: `dpkg-query -W -f='${Package} ${Version}\n' freerdp3-x11 libfreerdp-client3-3 libfreerdp3-3 libwinpr3-3` — ALL FOUR must show +onemix1.
5. **Normal launch with touch gestures and /cert:tofu**: Run `menu`, choose option 3. Verify: desktop reached (certificate trusted via TOFU on first connection), one-finger left-click/drag, long-press right-click, two-finger wheel scroll, pinch Ctrl+wheel, three-finger middle-button drag all work. The /cert:tofu policy is active (GAP-07 closure).
6. **Diagnostic launch with fallback cancel evidence (GAP-10 closure)**: Run `FREERDP_TOUCH_DIAG=1 menu`, choose option 3. Verify: log file created (0600/0700). DELIBERATELY trigger forced cancellation during an active fallback gesture (e.g., focus loss / Alt+Tab, fullscreen transition, or disconnect while a one-finger drag or two-finger pinch is in progress). The log MUST contain:
   - touch-diag: touch ev=Begin/Update/End records at ingress
   - touch-diag: state transition records (pending/pinch/scroll/long-press/tap/drag)
   - touch-diag: synth records (button/wheel/ctrl)
   - touch-diag: force_cancel summary with `diag=` count > 0 (the diagnostic lifecycle store active count, NOT active_contacts=0)
   - touch-diag: event=cancel records from the diagnostic lifecycle store at their last Update coordinates (not initial Begin coordinates) — these are the observational cancel records that satisfy GAP-10
   - No password or /p: values in the log
   Record in Notes (sanitized, allowed character set) that the force_cancel summary showed diag positive and event=cancel records were emitted at last Update coordinates from the diagnostic lifecycle store.
7. **Normal launch is quiet (default-off)**: Before launching, snapshot the diagnostic directory. Run `menu`, choose option 3, with FREERDP_TOUCH_DIAG unset. Capture the stream. After, snapshot again — no new file. Assert zero `touch-diag:` markers in the captured stream. Record in Notes that the diagnostic directory snapshot was unchanged and zero touch-diag markers observed.
8. **Mouse-only mode**: Run `menu --mouse-only`, choose option 3. Verify: desktop reached, touch does not trigger gestures, connection works with /cert:tofu.
9. **Rollback (fail-closed assertion)**: `sudo apt install --reinstall -y freerdp3-x11 libfreerdp-client3-3 libfreerdp3-3 libwinpr3-3`. Inspect ALL FOUR package versions. If any still shows +onemix1, force trixie closure with --allow-downgrades. Use the fail-closed assertion from the updated README (requires exactly four results). Assert ALL FOUR are stock (no +onemix1). Launch stock via `menu --mouse-only` — must work with mouse.
10. **Security-update detection**: `dpkg-query -W -f='${Version}' freerdp3-x11 | grep -q onemix1 && echo "patched" || echo "stock"` — after rollback, prints "stock".

If any step fails, recording `Actual: PASS` is NOT permitted — halt and describe the issue. The verify-record file must pass the same inline Python/stdlib parser as 04-03 (same grammar, same negative fixtures).
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re, sys

def parse(raw):
    e=[]
    if b'\r' in raw: e.append("cr")
    if b'\x1b' in raw: e.append("esc")
    if any(b < 0x20 and b != 0x0a for b in raw): e.append("ctrl")
    try:
        data=raw.decode('utf-8',errors='strict')
    except Exception:
        e.append("decode"); return e
    low=data.lower()
    for b in ["/p:","password","credential","secret"]:
        if b in low: e.append("forbid:"+b)
    if "[" in data or "]" in data: e.append("bracket")
    if re.search(r"\b\d{1,3}(\.\d{1,3}){3}\b",data): e.append("ipv4")
    L=data.split("\n")
    while L and L[-1]=="": L.pop()
    sec=re.compile(r"^([1-9]|10)\. \*\*(.+?)\*\*$")
    fld=re.compile(r"^(Command|Expected|Actual|Notes): (.*)$")
    NA=set("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789 .,;:()_-/=+")
    i=0; ap=0; ao=0; bad=False
    for s in range(1,11):
        if i>=len(L): e.append("eof-%d"%s); bad=True; break
        m=sec.match(L[i])
        if not m or int(m.group(1))!=s:
            e.append("head-%d:%s"%(s,L[i][:40])); bad=True; break
        i+=1
        for fn in ("Command","Expected","Actual","Notes"):
            if i>=len(L): e.append("miss-%d-%s"%(s,fn)); bad=True; break
            fm=fld.match(L[i])
            if not fm or fm.group(1)!=fn:
                e.append("field-%d-%s:%s"%(s,fn,L[i][:40])); i+=1; bad=True; break
            v=fm.group(2)
            if fn=="Command":
                if not(1<=len(v)<=200): e.append("cmd-len-%d"%s)
                if s in (5,6,7,8) and "<connection-args>" not in v: e.append("cmd-ph-%d"%s)
            elif fn=="Expected":
                if not(1<=len(v)<=200): e.append("exp-len-%d"%s)
            elif fn=="Actual":
                if v=="PASS": ap+=1
                else: ao+=1; e.append("actual-%d"%s)
            else:
                if not(1<=len(v)<=240): e.append("notes-len-%d"%s)
                elif any(c not in NA for c in v): e.append("notes-char-%d"%s)
            i+=1
        if bad: break
    if not bad and i!=len(L): e.append("trailing")
    if ap!=10: e.append("ap=%d"%ap)
    if ao: e.append("ao=%d"%ao)
    return e

f=".planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md"
try:
    raw=open(f,'rb').read()
except FileNotFoundError:
    sys.stderr.write("VRFAIL:no-file\n"); sys.exit(1)
e=parse(raw)
if e:
    sys.stderr.write("VRFAIL:"+",".join(e)+"\n"); sys.exit(1)

# Negative fixtures (same as 04-03)
def gen_sections(bad_section, bad_field, bad_value):
    lines=[]
    for s in range(1,11):
        lines.append("%d. **S%d**"%(s,s))
        for fn in ("Command","Expected","Actual","Notes"):
            if s==bad_section and fn==bad_field:
                lines.append("%s: %s"%(fn,bad_value))
            elif fn=="Command":
                cmd = "test" if s not in (5,6,7,8) else "test <connection-args>"
                lines.append("%s: %s"%(fn,cmd))
            elif fn=="Actual":
                lines.append("%s: PASS"%fn)
            else:
                lines.append("%s: test"%(fn))
    return ("\n".join(lines)+"\n").encode('utf-8')

def check_rejected(content, label):
    errs=parse(content)
    if not errs:
        sys.stderr.write("VRFAIL:fixture-%s-not-rejected\n"%label)
        return False
    return True

fx1=gen_sections(1,"Command","")
fx1_errs=parse(fx1)
if not fx1_errs or "cmd-len-1" not in fx1_errs:
    sys.stderr.write("VRFAIL:fixture-empty-cmd: %s\n"%",".join(fx1_errs))
    sys.exit(1)
good_lines=[]
for s in range(1,11):
    good_lines.append("%d. **S%d**"%(s,s))
    good_lines.append("Command: test")
    good_lines.append("Expected: test")
    good_lines.append("Actual: PASS")
    good_lines.append("Notes: test")
fx2=("\r\n".join(good_lines)+"\r\n").encode('utf-8')
if not check_rejected(fx2,"crlf"): sys.exit(1)
fx3=gen_sections(1,"Command","\x07test")
if not check_rejected(fx3,"ctrl-bytes"): sys.exit(1)
sys.exit(0)
PY</automated>
  </verify>
  <acceptance_criteria>
  - The verify-record file exists at .planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md and passes the inline Python/stdlib deterministic parser (same grammar as 04-03: ten sections, five lines each, Actual: PASS x10, raw-byte sanitization, negative fixtures for empty Command/CRLF/BEL)
  - All 10 checks pass via the three locked menu invocations only (D-24): session gate, checksums (subshell), install, four-package version, normal launch with /cert:tofu and all gestures, diagnostic launch with diagnostic lifecycle store cancel evidence (force_cancel summary with diag positive, event=cancel at last Update coordinates), quiet launch (no touch-diag markers), mouse-only, rollback with fail-closed assertion, security-update detection
  - Diagnostic log (check 6) contains: touch ev=Begin/Update/End, state transitions, synth output, force_cancel summary with diag= positive (not active_contacts=0), event=cancel from diagnostic lifecycle store at last Update coordinates, no /p: or sensitive material
  - Rollback (check 9) uses the fail-closed assertion requiring exactly four dpkg-query results; all four restored to stock
  - Normal launch (check 5) uses /cert:tofu (not /cert:ignore)
  </acceptance_criteria>
  <what-built>
Plan 04-04 fixed six source-level defects and re-crystallized the quilt patch with production-path regressions. Plan 04-05 fixed five script/doc/menu defects and added WR-02/WR-03 test improvements. This checkpoint verifies the complete gap-closure release end-to-end on the native-X11 device via the TTY/startx path, with updated evidence for the diagnostic lifecycle store cancel (GAP-10), certificate trust (GAP-07), and all other gap closures.
  </what-built>
  <how-to-verify>
Follow the 10 steps in the user's confirmed TTY/startx session (D-22). All on-device checks use the three locked menu invocations only (D-24). Record pass/fail in the verify-record file using the deterministic grammar. All 10 must pass. Pay special attention to: (a) /cert:tofu is active on first connection, (b) diagnostic log contains event=cancel from the diagnostic lifecycle store at last Update coordinates with force_cancel summary showing diag positive, (c) quiet launch has zero touch-diag markers, (d) rollback uses the fail-closed assertion and restores all four packages.
  </how-to-verify>
  <done>
The verify-record file exists at .planning/phases/04-diagnostics-packaging-launch-configuration/04-06-verify-record.md and passes the inline Python/stdlib deterministic parser: ten sections, five lines each, Actual: PASS x10, mechanically-verified sanitization, negative fixtures pass. The ten checks via the three locked menu invocations (D-24) confirm: (1) session gate, (2) checksums via subshell, (3) install with --allow-downgrades, (4) all four packages +onemix1, (5) normal launch with /cert:tofu and all gestures working, (6) diagnostic launch with force_cancel summary showing diag positive and event=cancel records from diagnostic lifecycle store at last Update coordinates (GAP-10 closure) and no sensitive material, (7) quiet launch with no touch-diag markers, (8) mouse-only mode, (9) rollback with fail-closed assertion restoring all four to stock, (10) security-update detection prints stock.
  </done>
  <resume-signal>Type "approved" if all 10 steps pass, or describe which steps failed and what happened</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rebuilt bundle → installed system | The user installs the rebuilt 4-package closure; checksums must verify and the signal-safe build must have produced a valid bundle |
| Menu → RDP server | /cert:tofu enables server identity verification on first connection |
| Diagnostic log → user | The log must not contain credentials; the verify-record uses sanitized evidence only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-G06-01 | Tampering | Interrupted build corrupts bundle | high | mitigate | Signal-safe trap (EXIT-only + 128+signal handlers); deterministic pre-swap TERM test exits 143 and preserves prior bundle (same target/checksums); post-swap TERM test exits 143 and new bundle persists with valid checksums (GAP-06 closure). |
| T-04-G06-02 | Spoofing | Certificate trust bypass | high | mitigate | /cert:tofu replaces /cert:ignore; on-device verification confirms TOFU is active on first connection (GAP-07 closure). |
| T-04-G06-03 | Information Disclosure | Diagnostic log leaks credentials | high | mitigate | Verify-record uses deterministic sanitized grammar (no /p:, no password/credential/secret keywords); on-device diagnostic log checked for no sensitive material (D-06). |
| T-04-G06-04 | Tampering | Fail-open rollback | high | mitigate | README rollback assertion requires exactly four dpkg-query results; on-device rollback uses the fail-closed assertion (GAP-09 closure). |
| T-04-G06-05 | Tampering | Package integrity | high | mitigate | SHA256SUMS verified before install; exact 4-package closure; no glob (D-10). |
</threat_model>

<verification>
**Automated/static (Task 1):**
- Fixed build-release.sh produces dist with exactly 4 .deb files with exact package names (freerdp3-x11, libfreerdp-client3-3, libfreerdp3-3, libwinpr3-3), each at +onemix1 with amd64 architecture, plus SHA256SUMS
- Two clean builds produce identical package identity manifests (basenames + Package/Version/Architecture); each build's SHA256SUMS independently validates; no cross-build SHA equality
- Pre-swap TERM interruption exits 143 and preserves prior valid dist bundle (same target and checksums)
- Post-swap TERM interruption exits 143 and new bundle persists with valid checksums
- All production-path regressions pass: classifier (standalone), consolidated xf_touch_internal_check (WR-01 classifier, GAP-02 cancel recovery, GAP-03 bounds End, GAP-04 fractional pinch, GAP-10 diagnostic snapshot with duplicate Begin), ownership layout, parser against dist package, README doc regression
- All existing regressions pass: X11 gate (with Wayland-negative WR-03), menu diagnostic env, wrapper production (WR-02 real wrapper copy with only two paths rewritten), pinch reversal, wrapper overlong/noncanonical rejection
- Patch clean-apply: fresh extraction + quilt push -a + dpkg-buildpackage succeeds
- Cert:tofu in menu; no +multitouch across patch/wrapper/menu
- README fail-closed: DPKG-QUERY FAILED and INCOMPLETE CLOSURE messages present; subshell checksum present

**Native-X11/on-device (Task 2 checkpoint):**
- 10-step verification via TTY/startx path (D-22) using the three locked menu invocations (D-24)
- /cert:tofu active on first connection (GAP-07)
- Diagnostic log contains force_cancel summary with diag positive and event=cancel from diagnostic lifecycle store at last Update coordinates (GAP-10)
- Quiet launch: no touch-diag markers, diagnostic dir unchanged
- Rollback: fail-closed assertion, all four stock
- Verify-record passes deterministic parser with negative fixtures
</verification>

<success_criteria>
1. The rebuilt 4-package bundle is valid with exact package names/versions/architecture, reproducible with identical package identity manifests (not cross-build SHA equality), and signal-safe with deterministic pre/post-swap interruption — PACK-01 fully closed.
2. All production-path and existing automated regressions pass with the gap fixes applied — DIAG-01, CONF-01, PACK-01, PACK-02 evidence updated. WR-01/02/03 review warnings closed.
3. On-device verification confirms all 10 checks pass with /cert:tofu, accurate diagnostic lifecycle store cancel evidence at last Update coordinates, quiet default-off, and fail-closed rollback — all four phase requirements verified.
4. The verify-record passes the deterministic parser — evidence is sanitized and mechanically verified.
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| `dist` (rebuilt) | relative symlink | repo (gitignored) | Points to the gap-closure validated 4-package bundle |
| `.dist-bundle-*` (rebuilt) | bundle directory | repo (gitignored) | Generated release bundle with 4 .deb files + SHA256SUMS |
| `04-06-verify-record.md` | verification record | .planning/ (committed) | Gap-closure on-device verification evidence with diagnostic lifecycle store cancel and cert:tofu evidence |
| None (no new source symbols) | — | — | This plan exercises existing artifacts; no new functions, fields, or CLI behaviors are created. No tracked source file is modified — dist and .dist-bundle-* are generated gitignored outputs. |