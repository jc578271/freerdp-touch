---
phase: 04-diagnostics-packaging-launch-configuration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-touch.sh
  - scripts/build-release.sh
  - /usr/local/bin/menu
  - README.md
  - tests/readme_doc_regression.sh
  - tests/wrapper_production_check.sh
  - tests/check_x11_session_check.sh
autonomous: true
gap_closure: true
requirements: [CONF-01, PACK-01, PACK-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/launch-touch.sh rejects overlong calibration values (more than 10 digits) before shell arithmetic; noncanonical values (leading zeros on multi-digit numbers) are rejected; the digits-only and range checks run only after the length guard passes (GAP-05 shell)"
    - "scripts/build-release.sh registers cleanup only for EXIT; INT/HUP/TERM handlers exit with 128+signal without invoking cleanup directly; the EXIT cleanup preserves the nonzero signal exit status; an interrupted build exits nonzero and preserves the prior valid dist bundle (GAP-06)"
    - "/usr/local/bin/menu uses /cert:tofu instead of /cert:ignore in the canonical launch flow; server certificate trust uses FreeRDP's built-in first-use trust workflow with no custom certificate code or new config system (GAP-07, locked decision 1)"
    - "README.md checksum verification runs in a subshell so the following install command resolves ./dist/... from the repository root; the documented sequence works when copy-pasted sequentially from a clean shell; a real clean-root test proves the subshell leaves cwd at repo root (GAP-08)"
    - "README.md rollback assertion requires exactly four successful dpkg-query results before checking for +onemix1; dpkg-query failure or partial output (fewer than four lines) is treated as unsafe and never prints ALL STOCK; a fake-tool regression proves both failure modes (GAP-09)"
    - "scripts/launch-touch.sh accepts valid calibration values and invokes xfreerdp3 with /touch-pinch-wheel-fallback (local-only, no +multitouch) in normal mode, and omits all touch options in --mouse-only mode (D-20)"
    - "tests/wrapper_production_check.sh executes the real wrapper with a mocked xfreerdp3 client through a deployed-style startx mock (WR-02 closure)"
    - "tests/check_x11_session_check.sh rejects XDG_SESSION_TYPE=wayland and nonempty WAYLAND_DISPLAY cases (WR-03 closure)"
    - "The launch wrapper is stateless: running it twice with the same environment variables and arguments produces the same FreeRDP invocation; calibration validation is deterministic — the same invalid values are rejected and the same valid values are accepted every time (CONF-01 idempotency, specless-probe resolved)"
    - "The build script starts from a clean source extraction each time: two runs produce the same package identity (basenames, Package, Version, Architecture) — the build is idempotent (PACK-01 idempotency, specless-probe resolved)"
    - "Install and rollback are idempotent: apt install of the same .deb set produces the same installed state; apt install --reinstall of stock packages produces the same stock state; the fail-closed assertion prevents partial states from being declared safe (PACK-02 idempotency, specless-probe resolved)"
  artifacts:
    - path: "scripts/launch-touch.sh"
      provides: "Wrapper with overlong/noncanonical calibration rejection before arithmetic"
      contains: "FREERDP_TOUCH_LONG_PRESS_MS"
    - path: "scripts/build-release.sh"
      provides: "Signal-safe release publication with EXIT-only cleanup and 128+signal exit codes"
      contains: "trap cleanup EXIT"
    - path: "/usr/local/bin/menu"
      provides: "Menu using /cert:tofu (FreeRDP built-in trust) instead of /cert:ignore"
      contains: "/cert:tofu"
    - path: "README.md"
      provides: "Operations guide with subshell checksum verification and fail-closed rollback assertion"
      contains: "sha256sum -c"
    - path: "tests/wrapper_production_check.sh"
      provides: "Real-wrapper test through deployed-style startx mock with mocked client (WR-02)"
      contains: "launch-touch.sh"
    - path: "tests/check_x11_session_check.sh"
      provides: "X11 gate with Wayland-negative rejection cases (WR-03)"
      contains: "wayland"
  key_links:
    - from: "README.md"
      to: "scripts/build-release.sh"
      via: "documentation references the build script and install/rollback commands"
      pattern: "build-release.sh"
  prohibitions:
    - requirement_id: CONF-01
      category: safety
      status: unresolved
      statement: "MUST NOT reintroduce +multitouch or native RDPEI touch forwarding in the menu or wrapper (D-01)"
    - requirement_id: CONF-01
      category: privacy
      status: unresolved
      statement: "MUST NOT store, echo, or log credentials in the menu, wrapper, or README — the cert:tofu change and shell validation must not introduce credential handling (D-06, D-19)"
    - requirement_id: DIAG-01
      category: safety
      status: unresolved
      statement: "MUST NOT enable diagnostic logging when FREERDP_TOUCH_DIAG is unset, 0, false, or any other value — the wrapper must not add WLOG_LEVEL=DEBUG or equivalent global debug override (D-03)"
    - requirement_id: PACK-01
      category: safety
      status: unresolved
      statement: "MUST NOT create an APT hold or pin the local package above future Debian security releases (D-08)"
    - requirement_id: PACK-02
      category: safety
      status: unresolved
      statement: "MUST NOT document a broad freerdp3-*.deb glob install — only the explicit 4-package closure (D-10)"
---

<objective>
Fix the five confirmed script/doc/menu defects (GAP-05 shell, GAP-06, GAP-07, GAP-08, GAP-09) that affect launch validation, release publication safety, certificate trust, and documented install/rollback correctness.

Purpose: These are the non-source fixes: shell validation hardening, signal-safe build publication, certificate trust policy (per locked decision: /cert:tofu replaces /cert:ignore), and README command-sequence correctness. All are independent of the source-level fixes in Plan 04-04 and can run in parallel.
Output: Updated scripts/launch-touch.sh, scripts/build-release.sh, /usr/local/bin/menu, and README.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-VERIFICATION.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-REVIEW.md
@scripts/launch-touch.sh
@scripts/build-release.sh
@scripts/check-x11-session.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GAP-05 shell overlong validation + GAP-06 signal-safe build-release + GAP-07 menu /cert:tofu</name>
  <files>scripts/launch-touch.sh, scripts/build-release.sh, /usr/local/bin/menu</files>
  <read_first>
    - scripts/launch-touch.sh (current wrapper — calibration validation uses digits-only case check then [ "$val" -lt/-gt ]; overlong values cause integer-expression errors in the [ comparison and bypass validation)
    - scripts/build-release.sh lines 40-70 (current trap: `trap cleanup EXIT INT HUP TERM` on line 63; cleanup function captures `local rc=$?` which can be 0 when a signal arrives during a child wait)
    - /usr/local/bin/menu line 83 (currently contains `/cert:ignore` in the xinitrc printf)
    - 04-VERIFICATION.md GAP-05, GAP-06, GAP-07 — exact required closure
    - 04-REVIEW.md BL-05 (shell half), BL-06, BL-07 — exact defect evidence and recommended fixes
    - 04-CONTEXT.md D-16 (calibration validation), D-19/D-23 (credential-free), locked gap decision 1 (cert:tofu)
  </read_first>
  <action>
Fix three independent script/menu defects. Each file is separate; no cross-file dependency.

**GAP-05 (shell) — Oversized calibration bypasses validation (BL-05 shell half):**
In `scripts/launch-touch.sh`, the calibration validation currently does: digits-only case check, then `[ "$lp_raw" -lt 500 ] || [ "$lp_raw" -gt 700 ]`. For a very long digit-only value (e.g., 84 digits), the `[ "$val" -lt ... ]` comparison produces an "integer expression expected" error (non-zero exit from `[`), but because both sides of the `||` fail, the `if` condition is false and the script continues to invoke FreeRDP with the oversized value.

Fix: Add a length guard BEFORE the arithmetic comparison, for both FREERDP_TOUCH_LONG_PRESS_MS and FREERDP_TOUCH_SLOP_PX. After the existing digits-only `case` check passes and BEFORE the `[ "$val" -lt ... ]` comparison, add:
```
if [ "${#val}" -gt 10 ]; then
    echo "ERROR: <VARNAME> is too long (max 10 digits), got '${#val}' digits" >&2
    exit 1
fi
```
10 digits covers up to 9,999,999,999 which is far beyond any ms/px value (500-700 ms, 4-16 px). This rejects overlong values before they reach shell arithmetic. Also add a noncanonical check: reject multi-digit values with leading zeros (e.g., "0600") since those are noncanonical decimal. After the digits-only check and before the length check, add:
```
case "$val" in
    0[0-9]*) echo "ERROR: <VARNAME> must not have leading zeros, got '$val'" >&2; exit 1 ;;
esac
```
This rejects "0600" but allows "0" alone (though "0" would fail the range check anyway). Apply the same pattern to both FREERDP_TOUCH_LONG_PRESS_MS and FREERDP_TOUCH_SLOP_PX. Keep the existing digits-only and range checks after the new guards.

**GAP-06 — Interrupted release build exits successfully (BL-06):**
In `scripts/build-release.sh`, the single `trap cleanup EXIT INT HUP TERM` (line 63) causes two problems: (1) a signal arriving while waiting for a child can enter cleanup with `$? == 0`, making the interruption appear successful; (2) the signal trap runs cleanup, then `exit $rc` triggers the EXIT trap which runs cleanup again (double cleanup).

Fix:
1. Change the trap registration from `trap cleanup EXIT INT HUP TERM` to `trap cleanup EXIT` only. The EXIT trap fires once on any exit (normal or signal-triggered) and captures the correct `$?`.
2. Add separate signal handlers that set the correct exit code without calling cleanup directly:
```
trap 'exit 130' INT    # 128 + 2 (SIGINT)
trap 'exit 129' HUP   # 128 + 1 (SIGHUP)
trap 'exit 143' TERM  # 128 + 15 (SIGTERM)
```
When a signal arrives, its handler calls `exit <128+signal>`. This triggers the EXIT trap, which runs `cleanup` once with `rc=$?` set to the 128+signal value. The cleanup function captures `local rc=$?` (which is now 130/129/143, not 0), does its publication-aware cleanup, and calls `exit $rc`. This fixes both the wrong exit code and the double-cleanup. The publication-aware guard in cleanup (comparing `readlink -f dist` to `bundle_dir`) already preserves the prior valid bundle when the swap has not completed.

**GAP-07 — Certificate validation disabled (BL-07, locked decision 1):**
In `/usr/local/bin/menu` line 83, replace `/cert:ignore` with `/cert:tofu` in the xinitrc printf string. FreeRDP's `/cert:tofu` implements trust-on-first-use: on first connection, it records the server certificate fingerprint and trusts it on subsequent connections. This is FreeRDP's built-in trust workflow — no custom certificate code, no new config system, no credential storage. The change is a single token replacement in the existing printf format string. Do not add any certificate-pinning code, config file, or credential handling. Do not alter any other part of the menu.

After editing, verify: `grep -q '/cert:tofu' /usr/local/bin/menu` and `! grep -q '/cert:ignore' /usr/local/bin/menu`.
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
# GAP-05 shell: overlong rejection
bash -n scripts/launch-touch.sh
td=$(mktemp -d); trap 'rm -rf "$td"' EXIT
cp scripts/launch-touch.sh "$td/launch-touch.sh"; chmod +x "$td/launch-touch.sh"
echo '#!/usr/bin/env bash' > "$td/check-x11-session.sh"; echo 'exit 0' >> "$td/check-x11-session.sh"; chmod +x "$td/check-x11-session.sh"
echo '#!/usr/bin/env bash' > "$td/xfreerdp3"; echo 'exit 0' >> "$td/xfreerdp3"; chmod +x "$td/xfreerdp3"
sed -i "s|/usr/bin/xfreerdp3|$td/xfreerdp3|g" "$td/launch-touch.sh"
# Overlong value rejected before FreeRDP invoked
LONG=$(printf '9%.0s' $(seq 1 84))
set +e; XDG_STATE_HOME="$td/s1" FREERDP_TOUCH_LONG_PRESS_MS="$LONG" "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:long-lp; exit 1; }
set +e; XDG_STATE_HOME="$td/s2" FREERDP_TOUCH_SLOP_PX="$LONG" "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:long-slop; exit 1; }
# Leading-zero noncanonical rejected
set +e; XDG_STATE_HOME="$td/s3" FREERDP_TOUCH_LONG_PRESS_MS=0600 "$td/launch-touch.sh" /f >/dev/null 2>&1; rc=$?; set -e
[ "$rc" -ne 0 ] || { echo FAIL:leading-zero; exit 1; }
# Valid 600 still works (normal mode execs mock which exits 0)
XDG_STATE_HOME="$td/s4" FREERDP_TOUCH_LONG_PRESS_MS=600 "$td/launch-touch.sh" /f >/dev/null 2>&1
# Inclusive boundaries still work
XDG_STATE_HOME="$td/s5" FREERDP_TOUCH_LONG_PRESS_MS=500 "$td/launch-touch.sh" /f >/dev/null 2>&1
XDG_STATE_HOME="$td/s6" FREERDP_TOUCH_LONG_PRESS_MS=700 "$td/launch-touch.sh" /f >/dev/null 2>&1
XDG_STATE_HOME="$td/s7" FREERDP_TOUCH_SLOP_PX=4 "$td/launch-touch.sh" /f >/dev/null 2>&1
XDG_STATE_HOME="$td/s8" FREERDP_TOUCH_SLOP_PX=16 "$td/launch-touch.sh" /f >/dev/null 2>&1
# GAP-06: signal-safe build-release trap
grep -q 'trap cleanup EXIT' scripts/build-release.sh
if grep -q 'trap cleanup EXIT INT HUP TERM' scripts/build-release.sh; then echo "FAIL: shared trap still present"; exit 1; fi
grep -q "exit 130" scripts/build-release.sh
grep -q "exit 129" scripts/build-release.sh
grep -q "exit 143" scripts/build-release.sh
# GAP-07: cert:tofu in menu
grep -q '/cert:tofu' /usr/local/bin/menu
if grep -q '/cert:ignore' /usr/local/bin/menu; then echo "FAIL: /cert:ignore still in menu"; exit 1; fi
echo "GAP-05-06-07-PASS"</automated>
  </verify>
  <done>
scripts/launch-touch.sh rejects overlong calibration values (84-digit) before shell arithmetic, rejects noncanonical leading-zero values (0600), and still accepts valid values including inclusive boundaries (500, 700, 4, 16). scripts/build-release.sh uses trap cleanup EXIT only with separate signal handlers exiting 130/129/143 for INT/HUP/TERM; the EXIT cleanup runs once with the correct 128+signal exit status. /usr/local/bin/menu uses /cert:tofu (FreeRDP built-in trust-on-first-use) instead of /cert:ignore; no custom certificate code or credential handling added.
  </done>
  <acceptance_criteria>
  - bash -n scripts/launch-touch.sh passes
  - Overlong FREERDP_TOUCH_LONG_PRESS_MS (84 digits) is rejected with nonzero exit before FreeRDP is invoked
  - Overlong FREERDP_TOUCH_SLOP_PX (84 digits) is rejected with nonzero exit before FreeRDP is invoked
  - Noncanonical leading-zero value (0600) is rejected with nonzero exit
  - Valid values (600, 500, 700, 4, 16) are accepted and reach the client
  - scripts/build-release.sh contains `trap cleanup EXIT` (not `trap cleanup EXIT INT HUP TERM`)
  - scripts/build-release.sh contains signal handlers with `exit 130` (INT), `exit 129` (HUP), `exit 143` (TERM)
  - /usr/local/bin/menu contains `/cert:tofu` and does NOT contain `/cert:ignore`
  - No +multitouch, no eval, no WLOG_LEVEL=DEBUG in any modified file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix GAP-08 README install cd + GAP-09 README rollback fail-closed</name>
  <files>README.md, tests/readme_doc_regression.sh</files>
  <read_first>
    - README.md lines 40-60 (checksum verification uses `cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS` which leaves the shell in the bundle directory; the next install command uses `./dist/...` which resolves wrong)
    - README.md lines 145-160 (rollback assertion pipelines `dpkg-query` into `grep`; if dpkg-query fails for a missing package, grep sees no +onemix1 and the `||` branch prints "ALL STOCK -- safe to launch")
    - 04-VERIFICATION.md GAP-08, GAP-09 — exact required closure
    - 04-REVIEW.md BL-08, BL-09 — exact defect evidence and recommended fixes
  </read_first>
  <action>
Fix two README documentation defects and add stable markers for doc regression. Both fixes are in README.md; no other files.

**GAP-08 — README checksum instructions break the following install command (BL-08):**
The current checksum verification command is `cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS` which leaves the caller inside the bundle directory.

Fix: Wrap the checksum verification in a subshell: `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)`. Do not change the install command itself — it already uses `./dist/...` which is correct from the repository root.

Add stable markers around the checksum command block so the doc regression can extract and execute the EXACT documented text. Inside the fenced code block, add shell-comment markers on their own lines:
```
# BEGIN: checksum-verify
(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
# END: checksum-verify
```
These are valid shell comments that do not break the documented command.

**GAP-09 — README rollback assertion fails open (BL-09):**
The current rollback assertion pipelines `dpkg-query` into `grep`, failing open when dpkg-query fails.

Fix: Replace with a fail-closed assertion that explicitly branches on dpkg-query failure and requires exactly four results before checking versions:
```
versions=$(dpkg-query -W -f='${Package} ${Version}\n' \
  freerdp3-x11 libfreerdp-client3-3 libfreerdp3-3 libwinpr3-3 2>/dev/null)
rc=$?
if [ "$rc" -ne 0 ]; then
  echo "DPKG-QUERY FAILED -- unsafe to launch"
elif [ "$(printf '%s\n' "$versions" | wc -l)" -ne 4 ]; then
  echo "INCOMPLETE CLOSURE -- unsafe to launch"
else
  printf '%s\n' "$versions" | grep -q '+onemix1' \
    && echo "STILL PATCHED -- retry trixie closure" \
    || echo "ALL STOCK -- safe to launch"
fi
```
Use the exact package names from the existing README (freerdp3-x11, libfreerdp-client3-3, libfreerdp3-3, libwinpr3-3).

Add stable markers around the rollback assertion block:
```
# BEGIN: rollback-check
versions=$(dpkg-query -W ...
...
fi
# END: rollback-check
```

**Doc regression test (commited as tests/readme_doc_regression.sh):**
Create a test that extracts the exact marked block text from README.md and executes it in a temporary repository root with a fake dist symlink and fake apt/dpkg-query. The test:
1. Extracts the checksum-verify block: `sed -n '/# BEGIN: checksum-verify/,/# END: checksum-verify/p' README.md | grep -v '^# BEGIN\|^# END'` — yields the exact documented command.
2. Creates a temp directory as a fake repo root, creates a `dist` symlink to a temp bundle dir with a fake `SHA256SUMS` and `.deb` file. Runs the extracted checksum command from the temp root. Asserts: (a) the command succeeds (checksum passes against the fake bundle), (b) `pwd` after the command is still the temp root (subshell preserved cwd).
3. Extracts the rollback-check block the same way. Creates a fake `dpkg-query` on PATH that can be controlled (exit nonzero, return 3 lines, return 4 stock lines, return 4 patched lines). Runs the extracted rollback text in each mode. Asserts: nonzero exit → "DPKG-QUERY FAILED" (never "ALL STOCK"), 3 lines → "INCOMPLETE CLOSURE" (never "ALL STOCK"), 4 stock → "ALL STOCK", 4 patched → "STILL PATCHED".
4. The test uses `set -euo pipefail` and prints PASS on success.

This test executes the EXACT documented text — not a rewritten duplicate. If the README commands break, the test fails. Rerun in 04-06.
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
# GAP-08: subshell checksum verification pattern present with markers
grep -q '# BEGIN: checksum-verify' README.md
grep -q '# END: checksum-verify' README.md
grep -q '(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)' README.md
# GAP-09: fail-closed rollback assertion with markers
grep -q '# BEGIN: rollback-check' README.md
grep -q '# END: rollback-check' README.md
grep -qi 'DPKG-QUERY FAILED' README.md
grep -qi 'INCOMPLETE CLOSURE' README.md
# Verify the old fail-open pattern is gone
set +e; grep -q "grep -q '+onemix1' && echo \"STILL PATCHED" README.md; old_pattern_rc=$?; set -e
[ "$old_pattern_rc" -ne 0 ] || { echo "FAIL: old fail-open pattern still present"; exit 1; }
# Verify the four-package names still appear
grep -q 'libwinpr3-3' README.md
grep -q 'freerdp3-x11' README.md
# Verify no /cert:ignore in README
if grep -q '/cert:ignore' README.md; then echo "FAIL: /cert:ignore in README"; exit 1; fi
# Doc regression: extracts and executes EXACT marked README blocks
bash -n tests/readme_doc_regression.sh
bash tests/readme_doc_regression.sh
echo "GAP-08-09-PASS"</automated>
  </verify>
  <done>
README.md checksum verification runs in a subshell with stable `# BEGIN: checksum-verify` / `# END: checksum-verify` markers. README.md rollback assertion explicitly branches on dpkg-query failure with stable `# BEGIN: rollback-check` / `# END: rollback-check` markers. tests/readme_doc_regression.sh extracts and executes the EXACT marked block text in a temporary repository root with a fake dist symlink and fake dpkg-query: proves cwd remains at repo root after the checksum subshell, and proves all four rollback branches (nonzero exit → DPKG-QUERY FAILED, 3 packages → INCOMPLETE CLOSURE, 4 stock → ALL STOCK, 4 patched → STILL PATCHED). No /cert:ignore in README.
  </done>
  <acceptance_criteria>
  - README.md contains `# BEGIN: checksum-verify` and `# END: checksum-verify` markers around `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)`
  - README.md contains `# BEGIN: rollback-check` and `# END: rollback-check` markers around the fail-closed rollback assertion
  - README.md contains "DPKG-QUERY FAILED" and "INCOMPLETE CLOSURE" unsafe messages
  - tests/readme_doc_regression.sh extracts and executes the EXACT marked block text (not a rewritten duplicate) in a temp repo root with fake dist symlink and fake dpkg-query
  - Doc regression proves: cwd remains at repo root after checksum subshell; dpkg-query nonzero exit → DPKG-QUERY FAILED (never ALL STOCK); 3 packages → INCOMPLETE CLOSURE (never ALL STOCK); 4 stock → ALL STOCK; 4 patched → STILL PATCHED
  - README.md does NOT contain `/cert:ignore`
  - The four package names (freerdp3-x11, libfreerdp-client3-3, libfreerdp3-3, libwinpr3-3) still appear in the rollback section
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create tests/wrapper_production_check.sh (WR-02) and extend tests/check_x11_session_check.sh with Wayland-negative cases (WR-03)</name>
  <files>tests/wrapper_production_check.sh, tests/check_x11_session_check.sh</files>
  <read_first>
    - tests/menu_diagnostic_env_check.sh (current menu test — substitutes the real wrapper with a mock and runs xinitrc via bash; WR-02 asks to use the real wrapper with a mocked client instead)
    - scripts/launch-touch.sh (the real wrapper — note: `gate_script="$script_dir/check-x11-session.sh"` resolves the gate relative to the wrapper; `/usr/bin/xfreerdp3` is hardcoded on lines 92 and 96)
    - scripts/check-x11-session.sh (the X11 gate — rejects non-X11 sessions)
    - tests/check_x11_session_check.sh (current X11 gate test — only tests x11 session with passing variables; WR-03 asks for Wayland-negative cases)
    - 04-REVIEW.md WR-02, WR-03 — exact defect evidence and recommended fixes
  </read_first>
  <action>
Create two test improvements that close the review warnings WR-02 and WR-03. Each is a separate file with no cross-file dependency.

**WR-02 — tests/wrapper_production_check.sh:**
The existing tests/menu_diagnostic_env_check.sh substitutes the real wrapper with a mock, so regressions in the wrapper itself are not detected. The wrapper also hardcodes `/usr/bin/xfreerdp3` (lines 92, 96) and resolves `check-x11-session.sh` relative to its own directory (line 11), so PATH-based mocking cannot reach either dependency deterministically.

Create a new test that copies the REAL scripts/launch-touch.sh to a temp file and rewrites ONLY its two absolute dependency paths to deterministic mocks:
1. `sed -i "s|/usr/bin/xfreerdp3|$MOCK_CLIENT|g"` — replaces the hardcoded client path.
2. `sed -i "s|gate_script=.*|gate_script=\"$MOCK_GATE\"|"` — replaces the gate script resolution line with an absolute mock gate path.
Assert these are the ONLY substitutions: `diff scripts/launch-touch.sh "$tmp_wrapper"` and verify every changed line contains either `$MOCK_CLIENT` or `$MOCK_GATE`. If any other line differs, fail.

The mock gate (`$MOCK_GATE`) simply exits 0 (simulating a passing X11 session check). The mock client (`$MOCK_CLIENT`) records its arguments to a file and exits 0. Create mock `xrandr` and `xinput` that exit 0.

The test uses the menu_diagnostic_env_check.sh startx mock pattern: the mock startx receives the rendered xinitrc path, verifies it has mode 0700 and a valid shebang (`#!/usr/bin/env bash`), then runs it via `bash` with `PATH` set so the mock gate and mock client are found. The xinitrc calls the temp wrapper copy (the menu's wrapper path is rewritten to the temp wrapper, similar to menu_diagnostic_env_check.sh's approach — but the temp wrapper IS the real wrapper with only two lines changed, not a mock).

The test runs three modes (same input pattern as menu_diagnostic_env_check.sh: `3\n\n\n6\n`):
- Normal: assert mock client receives `/touch-pinch-wheel-fallback` (local-only, no `+multitouch`).
- Diagnostic: `FREERDP_TOUCH_DIAG=1` — assert it propagates to the mock client environment.
- Mouse-only: `--mouse-only` — assert mock client does NOT receive `/touch-pinch-wheel-fallback` or `/touch-long-press` or `/touch-slop`.

Additional assertions: (a) set `FREERDP_TOUCH_LONG_PRESS_MS` to an 84-digit overlong value in normal mode — assert the wrapper rejects it before reaching the mock client (no client args recorded); (b) xinitrc has mode 0700 and valid shebang; (c) only two lines differ between the real wrapper and the temp copy.

The test uses `set -euo pipefail` and prints PASS on success.

**WR-03 — Extend tests/check_x11_session_check.sh:**
Add negative cases that exercise the Wayland rejection conditions:
1. `XDG_SESSION_TYPE=wayland` with a matching Xorg process on the same display — must FAIL.
2. Nonempty `WAYLAND_DISPLAY=wayland-0` (with `XDG_SESSION_TYPE=x11`) — must FAIL.

Add a `run_gate_wayland` function that sets `XDG_SESSION_TYPE=wayland` (instead of x11) and an `expect_failure` call using it with a matching Xorg. Add a `run_gate_wayland_display` function that sets `WAYLAND_DISPLAY=wayland-0` and an `expect_failure` call using it. Keep existing passing cases unchanged. Print the existing PASS message plus "PASS: Wayland-negative rejection" on success.
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
bash -n tests/wrapper_production_check.sh
bash -n tests/check_x11_session_check.sh
# WR-02: wrapper production check copies the real wrapper and rewrites only two paths
grep -q 'launch-touch.sh' tests/wrapper_production_check.sh
grep -q '/usr/bin/xfreerdp3' tests/wrapper_production_check.sh
grep -q 'gate_script=' tests/wrapper_production_check.sh
grep -q 'touch-pinch-wheel-fallback' tests/wrapper_production_check.sh
grep -q 'mouse-only' tests/wrapper_production_check.sh
# WR-02: test asserts only two substitutions (diff check)
grep -q 'diff\|only.*substitution\|ONLY.*substitution' tests/wrapper_production_check.sh
# WR-03: Wayland-negative cases present
grep -qi 'wayland' tests/check_x11_session_check.sh
grep -q 'XDG_SESSION_TYPE=wayland\|XDG_SESSION_TYPE.*wayland' tests/check_x11_session_check.sh
grep -q 'WAYLAND_DISPLAY=wayland\|WAYLAND_DISPLAY.*wayland' tests/check_x11_session_check.sh
echo "WR-02-03-PASS"</automated>
  </verify>
  <done>
tests/wrapper_production_check.sh copies the real scripts/launch-touch.sh to a temp file, rewrites ONLY the absolute `/usr/bin/xfreerdp3` client path and the `gate_script=` sibling gate path to deterministic mocks (asserting via diff that these are the only substitutions), and runs the rendered xinitrc through a startx mock. It verifies /touch-pinch-wheel-fallback in normal mode (no +multitouch), its absence in --mouse-only mode, FREERDP_TOUCH_DIAG propagation, overlong calibration rejection before reaching the mock client, xinitrc mode 0700, and that only two lines differ from the real wrapper (WR-02 closed). tests/check_x11_session_check.sh rejects XDG_SESSION_TYPE=wayland with matching Xorg and nonempty WAYLAND_DISPLAY (WR-03 closed). Both tests pass shell syntax checks.
  </done>
  <acceptance_criteria>
  - tests/wrapper_production_check.sh copies the real launch-touch.sh to a temp file and rewrites only /usr/bin/xfreerdp3 and gate_script= to mock paths
  - tests/wrapper_production_check.sh asserts via diff that these are the ONLY two substitutions (no other lines differ from the real wrapper)
  - tests/wrapper_production_check.sh verifies /touch-pinch-wheel-fallback in normal mode and its absence in --mouse-only mode
  - tests/wrapper_production_check.sh verifies FREERDP_TOUCH_DIAG propagation and overlong calibration rejection
  - tests/wrapper_production_check.sh verifies xinitrc mode 0700 and valid shebang
  - tests/check_x11_session_check.sh has negative cases for XDG_SESSION_TYPE=wayland and nonempty WAYLAND_DISPLAY
  - Both tests pass shell syntax checks
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell env → wrapper | Untrusted calibration env values cross here; overlong values must be rejected before arithmetic |
| Signal → build script | A signal during build publication must not appear successful or corrupt the prior bundle |
| Menu → RDP server | The certificate trust policy crosses here; /cert:ignore disables server identity verification |
| README → user operations | Documented commands must work sequentially from a clean shell; fail-open rollback is a safety risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-G05-01 | Tampering | Overlong calibration env value | high | mitigate | Length guard (max 10 digits) before shell arithmetic; noncanonical leading-zero rejection; digits-only and range checks preserved (GAP-05 shell). Tested with 84-digit value rejected. |
| T-04-G05-02 | Tampering | Interrupted build appears successful | high | mitigate | EXIT-only cleanup trap; separate signal handlers exit 128+signal; publication-aware guard preserves prior bundle (GAP-06). Grep-verified: no shared trap, signal codes present. |
| T-04-G05-03 | Spoofing | Certificate validation disabled | high | mitigate | Replace /cert:ignore with /cert:tofu (FreeRDP built-in trust-on-first-use); no custom certificate code or credential storage (GAP-07, locked decision 1). Grep-verified. |
| T-04-G05-04 | Tampering | README install sequence broken | medium | mitigate | Subshell checksum verification preserves caller's working directory; following ./dist/... resolves correctly (GAP-08). |
| T-04-G05-05 | Tampering | README rollback fails open | high | mitigate | Require exactly four dpkg-query results before version check; missing/incomplete closure declared unsafe (GAP-09). |
| T-04-G05-06 | Information Disclosure | Menu/wrapper credential handling | high | mitigate | No credential storage or logging introduced; cert:tofu change is a single token replacement; no echo of $@, no eval, no WLOG_LEVEL=DEBUG (D-06, D-19, D-23). |
</threat_model>

<verification>
**Automated/static (can run in any session):**
- bash -n scripts/launch-touch.sh passes
- Overlong FREERDP_TOUCH_LONG_PRESS_MS (84 digits) and FREERDP_TOUCH_SLOP_PX (84 digits) rejected with nonzero exit before FreeRDP invoked
- Noncanonical leading-zero value (0600) rejected
- Valid values (600, 500, 700, 4, 16) accepted
- scripts/build-release.sh: `trap cleanup EXIT` only (not EXIT INT HUP TERM); signal handlers with exit 130/129/143 present
- /usr/local/bin/menu: /cert:tofu present, /cert:ignore absent
- README.md: subshell checksum pattern with stable markers; fail-closed rollback assertion with stable markers; DPKG-QUERY FAILED and INCOMPLETE CLOSURE messages present; four package names present; no /cert:ignore
- tests/readme_doc_regression.sh: extracts and executes EXACT marked README block text in temp repo root with fake dist/dpkg-query; proves cwd unchanged and all four rollback branches
- tests/wrapper_production_check.sh: copies real launch-touch.sh, rewrites only /usr/bin/xfreerdp3 and gate_script= to mocks (diff-verified), runs through startx mock; verifies /touch-pinch-wheel-fallback, --mouse-only, FREERDP_TOUCH_DIAG, overlong rejection (WR-02)
- tests/check_x11_session_check.sh: Wayland-negative cases for XDG_SESSION_TYPE=wayland and nonempty WAYLAND_DISPLAY (WR-03)
- No +multitouch, no eval, no WLOG_LEVEL=DEBUG in any modified file
</verification>

<success_criteria>
1. GAP-05 shell: overlong and noncanonical calibration values rejected before shell arithmetic — CONF-01 validation hardened.
2. GAP-06: build-release.sh is signal-safe — interrupted builds exit nonzero and preserve the prior valid bundle — PACK-01 publication safety closed.
3. GAP-07: /cert:tofu replaces /cert:ignore — server identity verification enabled via FreeRDP's built-in trust workflow — CONF-01 certificate trust closed.
4. GAP-08: README checksum verification runs in a subshell with stable markers — doc regression extracts and executes the exact marked text — PACK-02 documentation corrected.
5. GAP-09: README rollback assertion explicitly branches on dpkg-query failure and fails closed for missing/incomplete closures — doc regression proves all four branches from exact marked text — PACK-02 documentation corrected.
6. WR-02: tests/wrapper_production_check.sh copies the real wrapper, rewrites only two absolute paths to mocks (diff-verified), and runs through startx mock — review warning closed.
7. WR-03: tests/check_x11_session_check.sh has Wayland-negative rejection cases — review warning closed.
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| `scripts/launch-touch.sh` (updated) | Bash script | repo (committed) | Overlong/noncanonical calibration rejection before arithmetic (GAP-05 shell) |
| `scripts/build-release.sh` (updated) | Bash script | repo (committed) | Signal-safe publication with EXIT-only cleanup and 128+signal exits (GAP-06) |
| `/usr/local/bin/menu` (updated) | menu (external) | /usr/local/bin/menu | /cert:tofu replaces /cert:ignore (GAP-07) |
| `README.md` (updated) | documentation | repo (committed) | Subshell checksum verification with stable markers (GAP-08) and fail-closed rollback assertion with stable markers (GAP-09) |
| `tests/readme_doc_regression.sh` | Bash test | repo (committed) | Extracts and executes exact marked README block text in temp repo root with fake dist/dpkg-query |
| `tests/wrapper_production_check.sh` | Bash test | repo (committed) | Real-wrapper test with only two absolute paths rewritten to mocks, run through startx mock (WR-02) |
| `tests/check_x11_session_check.sh` (extended) | Bash test | repo (committed) | Wayland-negative rejection cases (WR-03) |