---
phase: 04-diagnostics-packaging-launch-configuration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-touch.sh
  - scripts/build-release.sh
  - scripts/menu
  - README.md
  - tests/wrapper_production_check.sh
  - tests/check_x11_session_check.sh
  - tests/readme_doc_regression.sh
  - tests/build_release_signal_check.sh
autonomous: false
gap_closure: true
requirements: [CONF-01, PACK-01, PACK-02]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/launch-touch.sh rejects overlong calibration values before shell arithmetic and rejects noncanonical leading-zero values while preserving the documented 500-700 ms and 4-16 px ranges (GAP-05 shell, CONF-01)"
    - "scripts/build-release.sh registers cleanup only for EXIT; INT/HUP/TERM handlers exit with 128+signal; interrupted publication exits nonzero and preserves whichever valid dist bundle owns the published symlink (GAP-06, PACK-01)"
    - "scripts/build-release.sh creates the classifier executable per publisher with mktemp under ${TMPDIR:-/tmp}, registers that path in cleanup state before compilation, runs and removes it, and clears the state so parallel Wave-1 work shares no classifier executable (PACK-01)"
    - "tests/build_release_signal_check.sh is executable and directly runnable; its cc shim parses the dynamic -o target, removes stale output, creates that exact executable printing OK, chmods it, and records distinct fixture-local targets for each publisher; complete deterministic shims reach both markers around the unchanged mv -Tf line; scratch-only instrumentation records exactly one cleanup for pre-swap, post-swap, and no-prior-dist TERM cases without accessing live dist (GAP-06)"
    - "scripts/menu remains the tracked source of truth, preserves the existing /cert:ignore launch argument exactly per D-25, captures startx status immediately, removes the private xinitrc, and propagates failure before returning to the menu loop; /usr/local/bin/menu is deployed from this source with mode and byte-for-byte parity verification"
    - "tests/wrapper_production_check.sh executes the real wrapper through a deployed-style startx mock and executes the rendered xinitrc directly; it covers calibration boundaries, mouse-only, diagnostics, opaque arguments, client failure, and the actual copied scripts/menu startx-status path (WR-02)"
    - "Specless edge probe — CONF-01 idempotency: wrapper validation is stateless and deterministic; repeated processes with identical valid environment input produce byte-identical client argv and status, while repeated identical invalid input produces the same rejection before any client invocation (D-13, D-15, D-16, D-17, CONF-01)"
    - "tests/check_x11_session_check.sh rejects both XDG_SESSION_TYPE=wayland and nonempty WAYLAND_DISPLAY even when a matching Xorg process exists (WR-03)"
    - "README.md verifies SHA256SUMS in a subshell so the following ./dist install paths still resolve from the repository root (GAP-08, PACK-02)"
    - "README.md rollback logic requires exactly four successful dpkg-query results before checking local-version suffixes; query failure or incomplete output is unsafe and never reports ALL STOCK (GAP-09, PACK-02)"
    - "Specless edge probe — PACK-02 idempotency: the explicit four-package install and rollback command blocks can be executed repeatedly with identical results; each run requires all four exact package identities, and missing, fewer, duplicate, or mismatched packages fail closed before a safe-to-launch result (D-10, D-12, PACK-02)"
    - "GAP-07 remains user-deferred: v1 retains /cert:ignore and accepts HIGH server-impersonation/MITM exposure; this plan does not claim server certificate identity is protected (D-25)"
  artifacts:
    - path: "scripts/launch-touch.sh"
      provides: "Canonical wrapper with bounded calibration validation and unchanged local-touch/mouse-only behavior"
      contains: "FREERDP_TOUCH_LONG_PRESS_MS"
    - path: "scripts/build-release.sh"
      provides: "Signal-safe release publication with EXIT-only cleanup, 128+signal exits, and a cleanup-registered per-run classifier executable"
      contains: "trap cleanup EXIT"
    - path: "scripts/menu"
      provides: "Tracked menu source with startx status propagation and the owner-accepted launch trust behavior preserved"
      contains: "startx_rc"
    - path: "README.md"
      provides: "Operations guide with marked subshell checksum, repeatable exact four-package install, fail-closed repeatable rollback, and accepted-risk disclosure"
      contains: "BEGIN: install-four-package"
    - path: "tests/build_release_signal_check.sh"
      provides: "Executable isolated signal fixture with dynamic compiler-output creation, distinct fixture-local target assertions, unchanged swap-line instrumentation, and one-cleanup assertions"
      contains: "PRE_SWAP_MARKER"
    - path: "tests/wrapper_production_check.sh"
      provides: "Production-wrapper and actual-menu regression through a deployed-style direct xinitrc execution fixture"
      contains: "launch-touch.sh"
    - path: "tests/readme_doc_regression.sh"
      provides: "Exact marked README command-block regression proving repeatable four-package install/rollback and fail-closed incomplete sets"
      contains: "install-four-package"
    - path: "tests/check_x11_session_check.sh"
      provides: "Native-X11 gate regression with Wayland-negative cases"
      contains: "WAYLAND_DISPLAY"
  key_links:
    - from: "scripts/menu"
      to: "scripts/launch-touch.sh"
      via: "the generated private xinitrc execs the canonical wrapper and propagates its status through startx"
      pattern: "launch-touch.sh"
    - from: "tests/build_release_signal_check.sh"
      to: "scripts/build-release.sh"
      via: "an instrumented scratch copy preserves the production swap line while testing TERM before and after publication"
      pattern: "PRE_SWAP_MARKER"
    - from: "README.md"
      to: "scripts/build-release.sh"
      via: "the documented build/install/rollback flow consumes the exact four-package release bundle"
      pattern: "build-release.sh"
    - from: "scripts/menu"
      to: "/usr/local/bin/menu"
      via: "privileged copy followed by byte-for-byte diff and mode verification"
      pattern: "startx_rc"
  prohibitions:
    - requirement_id: CONF-01
      category: safety
      status: unresolved
      statement: "MUST preserve the local-only launch path and keep native multitouch disabled (D-01)"
    - requirement_id: CONF-01
      category: privacy
      status: unresolved
      statement: "MUST NOT store, echo, or log credentials or opaque connection arguments in the menu, wrapper, tests, or README (D-06, D-19)"
    - requirement_id: DIAG-01
      category: safety
      status: unresolved
      statement: "MUST NOT enable diagnostic logging unless FREERDP_TOUCH_DIAG is exactly 1 (D-03)"
    - requirement_id: PACK-01
      category: safety
      status: unresolved
      statement: "MUST NOT create an APT hold or pin the local package above future Debian security releases (D-08)"
    - requirement_id: PACK-02
      category: safety
      status: unresolved
      statement: "MUST NOT document a broad package glob; only the explicit four-package closure is installable (D-10)"
    - requirement_id: CONF-01
      category: accepted-risk
      status: deferred
      statement: "MUST preserve /cert:ignore unchanged and MUST NOT claim GAP-07 closed or server certificate identity verified (D-25)"
---

<objective>
Close the non-source release defects: GAP-05 shell, GAP-06, GAP-08, GAP-09, WR-02, and WR-03. Give every release publisher its own cleanup-registered classifier executable, preserve the production-faithful signal fixture, fix menu status propagation, repair the documented install/rollback flow, and deploy the tracked menu source to `/usr/local/bin/menu`. GAP-07 is explicitly excluded from closure per D-25.

Purpose: Harden the existing launch and release path without changing its user-facing trust behavior. The wrapper keeps one credential-free command path and exact diagnostic tee/status behavior (D-03, D-05, D-06, D-18, D-19, D-20, D-21), validates only the two canonical 600/8 calibration overrides without prompts or extra settings (D-13, D-15, D-16, D-17), and preserves the locked TTY/startx/menu flow (D-22, D-23, D-24). The release/docs path retains fresh pinned build identity, explicit four-package selection, no APT pin, and repeatable exact install/rollback (D-07, D-08, D-09, D-10, D-12). GAP-07 remains unchanged under D-25.
Output: Updated release/wrapper/menu scripts, README corrections, production-faithful regressions, and privileged deployed-menu parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-VERIFICATION.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-REVIEW.md
@scripts/launch-touch.sh
@scripts/build-release.sh
@scripts/check-x11-session.sh
@scripts/menu
</context>

<accepted_risks>
- **AR-04-01 — HIGH, accepted/deferred (D-25):** The current `/cert:ignore` behavior remains unchanged. The owner explicitly accepts that v1 is vulnerable to server impersonation and man-in-the-middle attack.
- **Evidence rule:** GAP-07 is not closed. Neither this plan nor later verification may claim that server certificate identity is protected; reconsideration requires a future owner-requested requirement or phase.
</accepted_risks>

<tasks>

<task type="tracer">
  <name>Task 1: Prove signal-safe release publication and fail-closed native-X11 gating</name>
  <files>scripts/build-release.sh, tests/build_release_signal_check.sh, tests/check_x11_session_check.sh</files>
  <read_first>
    - scripts/build-release.sh — current cleanup/signal traps, Stage 6's shared classifier executable, and the single atomic `mv -Tf "$tmp_link" "${REPO_ROOT}/dist"` publication line
    - tests/check_x11_session_check.sh — existing passing native-X11 cases that currently omit Wayland-negative coverage
    - patches/onemix-touch.patch — fixture input copied into the isolated release-script test
    - 04-VERIFICATION.md GAP-06
    - 04-REVIEW.md BL-06 and WR-03
  </read_first>
  <action>
Fix GAP-06 at the release script's root signal boundary. Register `cleanup` only for EXIT. Register independent INT, HUP, and TERM handlers that exit 130, 129, and 143 respectively without calling cleanup themselves; the resulting EXIT trap must run once and preserve the signal status. Keep the existing publication-aware `readlink -f` ownership test and the atomic swap line unchanged.

At the same cleanup-state seam, initialize `classifier_bin=""` before installing traps. In Stage 6 assign it with `mktemp "${TMPDIR:-/tmp}/freerdp-touch-classifier.XXXXXX"`, compile the standalone classifier to that exact path, execute it, remove it immediately after the `OK` assertion, and clear `classifier_bin`. The EXIT cleanup must remove any nonempty classifier path so compile, execution, or signal failure cannot leak it. Every publisher therefore owns a unique executable and Wave-1 plans share no classifier temp resource.

Create `tests/build_release_signal_check.sh` as a directly executable, isolated scratch-repository regression. Preserve the recently improved fixture exactly:
1. Copy the real release script and patch into a `mktemp -d` tree; never instrument or mutate the live script or live `dist`.
2. Supply deterministic executable shims for every external release stage and set `TMPDIR` to a fixture-owned directory. The `cc` shim must parse arguments until `-o <target>`, fail if absent, require the target to be beneath that fixture TMPDIR, append it to `CC_OUTPUT_LOG`, remove stale output, create that exact target as an executable printing `OK`, and run `chmod +x`. After all publisher cases, require the logged compiler targets to be nonempty and unique. The package shims must create the exact four expected package files and metadata so the unchanged release script reaches publication.
3. Use a Python/stdlib line transformer on the scratch copy. Require the production swap line exactly once, preserve it byte-for-byte, add bounded PRE_SWAP_MARKER and POST_SWAP_MARKER waits immediately around it, and add scratch-only cleanup counting at the start of `cleanup()`.
4. Test prior-bundle pre-swap TERM, newly published post-swap TERM, and no-prior-dist pre-swap TERM. Every case exits 143 and records exactly one cleanup. Pre-swap preserves the old target and checksums; post-swap preserves the new target and checksums; no-prior-dist leaves no published link.
5. Mark the committed test executable and require both `test -x` and direct execution.

Extend `tests/check_x11_session_check.sh` with two negative cases: a matching Xorg process with `XDG_SESSION_TYPE=wayland`, and a matching Xorg process with nonempty `WAYLAND_DISPLAY`. Both must fail while existing native-X11 cases continue to pass (WR-03).
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
bash -n scripts/build-release.sh
bash -n tests/build_release_signal_check.sh
bash -n tests/check_x11_session_check.sh
grep -q 'trap cleanup EXIT' scripts/build-release.sh
if grep -q 'trap cleanup EXIT INT HUP TERM' scripts/build-release.sh; then echo "FAIL: shared signal cleanup trap remains"; exit 1; fi
grep -q "exit 130" scripts/build-release.sh
grep -q "exit 129" scripts/build-release.sh
grep -q "exit 143" scripts/build-release.sh
grep -Fq 'classifier_bin=$(mktemp "${TMPDIR:-/tmp}/freerdp-touch-classifier.XXXXXX")' scripts/build-release.sh
grep -Fq 'rm -f "$classifier_bin"' scripts/build-release.sh
grep -q 'CC_OUTPUT_LOG' tests/build_release_signal_check.sh
grep -q 'sort -u' tests/build_release_signal_check.sh
chmod +x tests/build_release_signal_check.sh
test -x tests/build_release_signal_check.sh
./tests/build_release_signal_check.sh
grep -q 'PRE_SWAP_MARKER' tests/build_release_signal_check.sh
grep -q 'POST_SWAP_MARKER' tests/build_release_signal_check.sh
grep -q 'CLEANUP_COUNTER\|cleanup_count' tests/build_release_signal_check.sh
grep -q -- '-o' tests/build_release_signal_check.sh
grep -q 'chmod +x' tests/build_release_signal_check.sh
grep -q 'dpkg-source' tests/build_release_signal_check.sh
grep -q 'dpkg-buildpackage' tests/build_release_signal_check.sh
grep -q 'dpkg-deb' tests/build_release_signal_check.sh
bash tests/check_x11_session_check.sh
grep -q 'XDG_SESSION_TYPE.*wayland' tests/check_x11_session_check.sh
grep -q 'WAYLAND_DISPLAY.*wayland' tests/check_x11_session_check.sh
echo "SIGNAL-AND-X11-GATE-PASS"</automated>
  </verify>
  <done>
The release script exits 130/129/143 for INT/HUP/TERM, runs cleanup once through EXIT, and owns a unique cleanup-registered classifier executable for every publisher. The directly executable scratch fixture reaches the unchanged swap line with faithful shims, creates each dynamic compiler target beneath fixture TMPDIR, proves targets are distinct, proves one cleanup in all three TERM cases, and never accesses live dist. Native-X11 regression coverage now fails closed for both Wayland indicators.
  </done>
  <acceptance_criteria>
  - scripts/build-release.sh uses EXIT-only cleanup and independent 130/129/143 signal exits
  - scripts/build-release.sh creates the classifier output with `mktemp` under `${TMPDIR:-/tmp}`, registers it in cleanup state before compilation, removes it after execution, and clears the state
  - The atomic publication line and publication-aware cleanup ownership logic remain unchanged
  - tests/build_release_signal_check.sh is executable, passes `test -x`, and succeeds by direct invocation
  - The fixture's cc shim honors each dynamic `-o` path beneath fixture TMPDIR, creates and chmods that exact executable to print OK, records it, and proves publisher targets are distinct
  - PRE_SWAP_MARKER and POST_SWAP_MARKER surround the one unchanged production swap line
  - Pre-swap, post-swap, and no-prior-dist TERM cases each exit 143 with exactly one cleanup and correct bundle state
  - The fixture never reads from, writes to, removes, or replaces live dist
  - tests/check_x11_session_check.sh rejects both Wayland indicators and preserves existing passing cases
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix wrapper calibration, menu status propagation, README operations, and production-path regressions</name>
  <files>scripts/launch-touch.sh, scripts/menu, README.md, tests/wrapper_production_check.sh, tests/readme_doc_regression.sh</files>
  <read_first>
    - scripts/launch-touch.sh — current digits-only checks followed by shell arithmetic, absolute gate/client paths, diagnostic tee, and mouse-only branch
    - scripts/menu — private xinitrc generation, current `/cert:ignore` argument, and the uncaptured startx return path
    - tests/menu_diagnostic_env_check.sh — existing diagnostic propagation coverage that mocks the wrapper
    - README.md — checksum/install sequence and rollback assertion
    - 04-VERIFICATION.md GAP-05, GAP-08, GAP-09
    - 04-REVIEW.md BL-05 shell half, BL-08, BL-09, and WR-02
    - 04-CONTEXT.md D-22 through D-25
  </read_first>
  <action>
Fix GAP-05 shell in `scripts/launch-touch.sh`: after the digits-only check and before arithmetic, reject values longer than 10 digits and reject multi-digit values with a leading zero. Apply the same canonical validation order to long-press and slop. Preserve defaults 600/8, accepted ranges 500-700 and 4-16, mouse-only bypass behavior, exact diagnostic gating, opaque argument forwarding, and installed-client exit status.

Fix `scripts/menu` status handling without changing the locked TTY/startx flow. Capture `startx_rc=$?` immediately after `startx`, remove the private xinitrc, and exit with the captured nonzero status before the existing success-only `read` and loop continuation. Keep the password argument opaque, keep the generated xinitrc executable and private, and preserve the existing `/cert:ignore` argument exactly per D-25.

Create `tests/wrapper_production_check.sh` to close WR-02. Copy the real wrapper and rewrite only its absolute gate and client paths to deterministic mocks; prove those are the only substitutions. Use a startx mock that checks mode 0700 and the shebang, then executes the rendered xinitrc directly. Cover normal, exact diagnostic, and mouse-only modes; valid boundary calibration; overlong and leading-zero rejection; opaque argument preservation; client failure through the diagnostic tee; and the actual copied `scripts/menu` path with successful and failing startx statuses. For the CONF-01 idempotency probe, launch two fresh wrapper processes with identical valid environment values and require byte-identical captured client argv plus equal status; then launch two fresh processes with the same invalid value and require equal rejection status/message with zero client calls both times. Assert the generated normal/menu invocation still carries exactly one `/cert:ignore` argument and make no server-identity protection claim.

Fix GAP-08 in README with stable `# BEGIN: checksum-verify` / `# END: checksum-verify` markers around `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` so the caller's working directory remains the repository root.

Bound the explicit local install transaction with stable `# BEGIN: install-four-package` / `# END: install-four-package` markers. The block names the four bundle files explicitly in dependency order, validates the resolved bundle has exactly one package for each required Package identity before invoking APT, and is safe to execute again when the exact local versions are already installed (D-10, D-12; PACK-02 idempotency).

Fix GAP-09 with stable `# BEGIN: rollback-check` / `# END: rollback-check` markers. Capture `dpkg-query -W` output only after requiring command success, require exactly the four named packages, then distinguish stock from `+onemix1`; query failure or missing/fewer/duplicate/mismatched package rows must report unsafe and must not report `ALL STOCK`. Keep the exact rollback transaction repeatable when the stock versions are already installed.

Add a visible README accepted-risk disclosure per D-25, bounded by `<!-- BEGIN: accepted-cert-risk -->` / `<!-- END: accepted-cert-risk -->`: v1 intentionally retains `/cert:ignore`, GAP-07 is deferred, server certificate identity is not verified, and the owner accepts HIGH server-impersonation/MITM exposure. State that reconsideration requires a future requested requirement or phase.

Create `tests/readme_doc_regression.sh` to extract and execute the exact marked checksum, install-four-package, and rollback blocks in a temporary repository fixture. Prove cwd preservation. Execute the exact install block twice against the same mocked four-package bundle and require the same ordered four package arguments and successful result on both runs. Execute the rollback transaction/assertion twice for an exact stock closure and require the same safe result. On every repetition, test query failure plus missing, fewer, duplicate, extra, mismatched-version, exact four-stock, and exact four-patched branches; no incomplete case may print the safe-to-launch result. Also assert the accepted-risk disclosure contains the D-25 facts and does not claim GAP-07 closure.
  </action>
  <verify>
    <automated>set -euo pipefail; cd /home/hoang/freerdp-touch
bash -n scripts/launch-touch.sh
bash -n scripts/menu
bash -n tests/wrapper_production_check.sh
bash -n tests/readme_doc_regression.sh
bash tests/wrapper_production_check.sh
bash tests/menu_diagnostic_env_check.sh
bash tests/readme_doc_regression.sh
grep -q 'startx_rc' scripts/menu
[ "$(grep -Fo -- '/cert:ignore' scripts/menu | wc -l)" -eq 1 ]
grep -q '# BEGIN: checksum-verify' README.md
grep -q '# END: checksum-verify' README.md
grep -q '(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)' README.md
grep -q '# BEGIN: install-four-package' README.md
grep -q '# END: install-four-package' README.md
grep -q '# BEGIN: rollback-check' README.md
grep -q '# END: rollback-check' README.md
grep -q 'DPKG-QUERY FAILED' README.md
grep -q 'INCOMPLETE CLOSURE' README.md
grep -q '<!-- BEGIN: accepted-cert-risk -->' README.md
grep -q '<!-- END: accepted-cert-risk -->' README.md
grep -qi 'server impersonation\|man-in-the-middle\|MITM' README.md
grep -q '/cert:ignore' README.md
grep -q 'GAP-07' README.md
echo "WRAPPER-MENU-README-PASS"</automated>
  </verify>
  <done>
The wrapper rejects overlong/noncanonical calibration before arithmetic and produces deterministic results across repeated identical valid/invalid inputs; the menu propagates startx failure while preserving the current launch argument; the real wrapper and actual menu execute through a direct xinitrc fixture; README checksum plus repeatable exact four-package install/rollback commands pass exact-text regressions and fail closed on incomplete identities; and the accepted HIGH server-identity risk is disclosed without claiming GAP-07 closure.
  </done>
  <acceptance_criteria>
  - Overlong long-press/slop values and leading-zero multi-digit values fail before the client runs
  - Valid 500, 600, 700 ms and 4, 8, 16 px values remain accepted; mouse-only bypass remains unchanged
  - scripts/menu captures startx status immediately, removes the xinitrc, and propagates failure before success-only interaction
  - scripts/menu retains exactly one `/cert:ignore` occurrence per D-25
  - tests/wrapper_production_check.sh uses the real wrapper with only two dependency-path substitutions and executes rendered xinitrc directly
  - Wrapper/menu regression covers normal, diagnostic, mouse-only, opaque arguments, calibration, and nonzero client/startx status
  - Two fresh wrapper processes with identical valid input produce identical argv/status; repeated identical invalid input produces identical pre-client rejection with zero client calls
  - README checksum verification runs in a subshell and preserves the repository cwd
  - README marks and explicitly validates the exact four-package install transaction; the regression executes it twice with identical ordered package identities
  - README rollback requires an exact four-package query result, remains safe to repeat, and fails closed on query failure or missing/fewer/duplicate/mismatched closure
  - README visibly discloses that GAP-07 is deferred, `/cert:ignore` remains, and certificate identity is not protected (D-25)
  - tests/readme_doc_regression.sh executes exact marked blocks and verifies all rollback branches plus the accepted-risk disclosure
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy tracked scripts/menu to /usr/local/bin/menu and prove parity</name>
  <files>/usr/local/bin/menu</files>
  <read_first>
    - scripts/menu — tracked source of truth updated by Task 2
    - /usr/local/bin/menu — root-owned deployed copy
    - 04-CONTEXT.md D-25 — accepted security exception that must remain unchanged during deployment
  </read_first>
  <precondition>Tasks 1 and 2 pass: scripts/menu contains startx status propagation, retains the current `/cert:ignore` argument, and all wrapper/menu regressions pass.</precondition>
  <action>
Use the required privileged checkpoint because `/usr/local/bin/menu` is root-owned. The human administrator copies the tracked source only after all repository tests pass, then verifies byte-for-byte parity, executable mode, status-propagation marker, and the single retained `/cert:ignore` occurrence. Do not hand-edit the deployed file.
  </action>
  <verify>
    <automated>set -e
diff /home/hoang/freerdp-touch/scripts/menu /usr/local/bin/menu
[ "$(stat -c '%a' /usr/local/bin/menu)" = "755" ]
grep -q 'startx_rc' /usr/local/bin/menu
[ "$(grep -Fo -- '/cert:ignore' /usr/local/bin/menu | wc -l)" -eq 1 ]
echo "DEPLOYMENT-PARITY-PASS"</automated>
  </verify>
  <done>`/usr/local/bin/menu` is mode 0755, byte-for-byte identical to `scripts/menu`, contains startx status propagation, and preserves the single owner-accepted `/cert:ignore` argument per D-25.</done>
  <acceptance_criteria>
  - `sudo cp /home/hoang/freerdp-touch/scripts/menu /usr/local/bin/menu && sudo chmod 755 /usr/local/bin/menu` completes
  - `diff scripts/menu /usr/local/bin/menu` produces no output
  - Deployed mode is exactly 0755
  - Deployed menu contains startx_rc and exactly one `/cert:ignore` occurrence
  - Deployment makes no claim that GAP-07 is closed or server certificate identity is verified (D-25)
  </acceptance_criteria>
  <what-built>
  Tasks 1-2 corrected release interruption, calibration validation, menu status propagation, tests, and documentation. This checkpoint deploys the tested menu source without altering the accepted security exception.
  </what-built>
  <how-to-verify>
  Run `sudo cp /home/hoang/freerdp-touch/scripts/menu /usr/local/bin/menu && sudo chmod 755 /usr/local/bin/menu`. Then run `diff /home/hoang/freerdp-touch/scripts/menu /usr/local/bin/menu`, `stat -c '%a' /usr/local/bin/menu`, `grep -q 'startx_rc' /usr/local/bin/menu`, and confirm the retained launch argument occurs once.
  </how-to-verify>
  <resume-signal>Type "approved" if deployment and parity verification succeed, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell environment -> wrapper | Untrusted calibration values must be rejected before arithmetic or client invocation |
| Signal -> release publisher | An interruption must not become a successful exit or invalidate the published bundle |
| Menu -> RDP server | The retained launch trust behavior crosses this boundary; server identity is not verified |
| README -> operator shell | Copy-pasted checksum/install/rollback commands must preserve cwd and fail closed |
| Repository menu -> privileged path | Root-owned deployment must exactly match the tested tracked source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-G05-01 | Tampering | Overlong/noncanonical calibration value or stateful validation drift | high | mitigate | Bound length before arithmetic, reject leading-zero multi-digit forms, and run repeated fresh-process real-wrapper regressions requiring deterministic argv/status or deterministic pre-client rejection (GAP-05 shell, CONF-01). |
| T-04-G05-02 | Tampering | Interrupted release appears successful, loses a valid bundle, or collides with another Wave-1 classifier executable | high | mitigate | EXIT-only cleanup, 128+signal exits, cleanup-registered per-publisher mktemp classifier paths, a fixture cc shim that honors and distinguishes dynamic outputs, unchanged swap markers, and one cleanup per TERM case (GAP-06). |
| T-04-G05-03 | Spoofing | Server impersonation or MITM while `/cert:ignore` is retained | high | accept | Explicit owner acceptance in D-25. GAP-07 is deferred outside v1; the risk is disclosed and no plan or evidence may claim server certificate identity is protected. |
| T-04-G05-04 | Tampering | README install sequence changes cwd | medium | mitigate | Run checksum verification in a subshell and execute the exact marked block in regression (GAP-08). |
| T-04-G05-05 | Tampering | Install/rollback check reports safe with missing, duplicate, mismatched, or state-dependent package identities | high | mitigate | Execute exact marked install/rollback blocks twice and require the same four identities before stock/local classification on every run (GAP-09, PACK-02). |
| T-04-G05-06 | Information Disclosure | Menu/wrapper exposes password or opaque connection args | high | mitigate | Preserve array/quoted argument boundaries; do not echo the command or credentials; execute through production-path mocks (D-06, D-19). |
| T-04-G05-07 | Tampering | startx failure is swallowed or deployed menu drifts | medium | mitigate | Capture and propagate startx status; deploy only from scripts/menu; require byte parity and mode 0755. |
</threat_model>

<verification>
**Automated/static:**
- Shell syntax passes for the modified scripts and tests
- Real-wrapper regression rejects overlong/leading-zero calibration, accepts all documented boundaries, and proves stateless deterministic results across repeated identical valid and invalid environment input
- build-release uses EXIT-only cleanup, 130/129/143 signal exits, and a per-publisher classifier executable created under `${TMPDIR:-/tmp}` and registered in cleanup state
- executable signal fixture honors each dynamic compiler `-o` target, proves fixture-local targets are distinct, reaches both unchanged swap markers, records one cleanup per pre/post/no-dist TERM case, and never touches live dist
- menu captures and propagates startx status while preserving the current `/cert:ignore` argument exactly once
- wrapper/menu regression executes the rendered xinitrc directly and covers normal, exact diagnostic, mouse-only, opaque args, and failures
- native-X11 gate rejects both Wayland indicators
- README marked checksum, exact four-package install, and rollback blocks pass exact-text regression twice; missing/fewer/duplicate/mismatched closures fail closed on every run
- README discloses D-25 accepted HIGH server-impersonation/MITM risk and does not claim GAP-07 closure
- deployed `/usr/local/bin/menu` is mode 0755 and byte-identical to `scripts/menu`
</verification>

<success_criteria>
1. GAP-05 shell is closed with bounded canonical calibration validation through the real wrapper path, including deterministic repeated identical valid/invalid input (CONF-01).
2. GAP-06 is closed with signal-safe publication, per-publisher mktemp classifier ownership, and the faithful executable fixture's dynamic-output/one-cleanup proof (PACK-01).
3. GAP-08 and GAP-09 are closed by exact-text README regressions for cwd preservation and repeatable exact four-package install/rollback; missing, fewer, duplicate, or mismatched package identities fail closed on every run (PACK-02).
4. WR-02 and WR-03 are closed by production-wrapper/direct-xinitrc and Wayland-negative tests.
5. `scripts/menu` propagates startx status and is deployed byte-for-byte to `/usr/local/bin/menu` before final verification.
6. GAP-07 remains deferred by owner; `/cert:ignore` is expected and no certificate-identity protection claim is made (D-25).
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| `scripts/launch-touch.sh` (updated) | Bash script | repo (committed) | Bounded canonical calibration validation while preserving existing launch modes (GAP-05 shell) |
| `scripts/build-release.sh` (updated) | Bash script | repo (committed) | Signal-safe publication with EXIT-only cleanup, 128+signal exits, and a cleanup-registered unique classifier executable (GAP-06) |
| `scripts/menu` (updated) | menu source | repo (committed) | startx status propagation with current `/cert:ignore` behavior preserved per D-25 |
| `/usr/local/bin/menu` (deployed) | root-owned menu | /usr/local/bin/menu | Mode-0755 byte-identical deployment from scripts/menu |
| `README.md` (updated) | operations guide | repo (committed) | Subshell checksum, marked repeatable exact four-package install/rollback, fail-closed identity checks, and D-25 accepted-risk disclosure |
| `tests/build_release_signal_check.sh` | executable Bash test | repo (committed, mode +x) | Faithful scratch fixture with dynamic fixture-local compiler outputs, distinct-target proof, unchanged swap markers, and one cleanup per TERM case |
| `tests/wrapper_production_check.sh` | Bash test | repo (committed) | Real-wrapper and actual-menu regression through direct xinitrc execution (WR-02) |
| `tests/readme_doc_regression.sh` | Bash test | repo (committed) | Executes checksum/install/rollback blocks twice, rejects incomplete identities, and checks accepted-risk disclosure (GAP-08/09) |
| `tests/check_x11_session_check.sh` (extended) | Bash test | repo (committed) | Wayland-negative rejection cases (WR-03) |