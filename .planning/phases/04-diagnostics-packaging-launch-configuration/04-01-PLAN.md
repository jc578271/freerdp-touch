---
phase: 04-diagnostics-packaging-launch-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
  - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h
  - build/freerdp3-3.15.0+dfsg/channels/rdpei/client/rdpei_main.c
  - patches/onemix-touch.patch
  - scripts/build-release.sh
  - .gitignore
autonomous: false
requirements: [DIAG-01, PACK-01]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "FREERDP_TOUCH_DIAG=1 produces compact WLog_WARN touch records on stderr; unset or any other value produces no new diagnostic output (D-03, D-04, D-05)"
    - "The diagnostic gate adds no per-event behavior change when disabled — no gesture routing, classifier constant, timer, or cleanup path is altered (D-02, D-14)"
    - "A fresh dpkg-source extraction plus the committed quilt patch builds successfully through dpkg-buildpackage and produces the four-package runtime closure at version 3.15.0+dfsg-2.1+deb13u3+onemix1 (D-07, D-10)"
    - "dist is a relative symlink resolving to a bundle directory containing exactly libwinpr3-3, libfreerdp3-3, libfreerdp-client3-3, and freerdp3-x11 .deb files plus SHA256SUMS — no source tree, no glob, no dev package (D-10, D-11)"
    - "Two clean builds from empty staging produce the same four package names, the same +onemix1 version, and valid checksums on each run (reproducibility: identical names/versions/closure, not byte-identical .deb files — Debian metadata may embed timestamps)"
    - "The build script acquires a non-blocking flock -n on a project-local generated lock (failing immediately with a clear message if another publisher holds it), initializes cleanup variables before installing the publication-aware EXIT/INT/HUP/TERM trap (never deletes a bundle dist resolves to, no fragile published flag), builds and validates a unique same-filesystem bundle directory containing exactly the four packages plus SHA256SUMS with basename entries, creates a temporary relative symlink, and atomically swaps it over the dist symlink via mv -Tf (rename(2)), so interruption, parallel invocation, or a failed build cannot corrupt or empty a prior valid dist target — the prior bundle remains reachable until the pointer swap succeeds (D-10)"
    - "No APT hold or pin is created; the +onemix1 local suffix sorts below future Debian security releases (D-08)"
    - "The RDPEI frame record is guarded by a cached private bool in RDPEI_PLUGIN initialized by env != NULL && strcmp(env, \"1\") == 0, not a per-frame getenv call; in xf_touch_force_cancel, gated event=cancel records fire for each active local physical touch (xfc->contacts[] loop) before local state is cleared — the canonical +touch-pinch-wheel-fallback path creates no RDPEI contacts so cctx->contacts[] stays empty; RDPEI per-contact event=cancel records in the cctx->contacts[] cancel loop fire only when actual native contacts exist (D-04)"
    - { statement: "Diagnostic WARN records contain only event IDs, coordinates, state/output labels, and frame count/offset/status — never command arguments or credential material", verification: backstop }
  artifacts:
    - path: "patches/onemix-touch.patch"
      provides: "One quilt patch capturing the complete frozen working-tree source delta plus the +onemix1 changelog entry"
      contains: "diff --git a/client/X11/xf_input.c"
    - path: "scripts/build-release.sh"
      provides: "Repeatable release pipeline: pin-check, fresh extraction, patch apply, classifier check, build, 4-package closure validation, SHA256SUMS"
      contains: "dpkg-buildpackage"
    - path: "dist (relative symlink)"
      provides: "Atomic user-facing pointer to the current validated 4-package bundle directory"
      contains: "mv -Tf"
  key_links:
    - from: "scripts/build-release.sh"
      to: "patches/onemix-touch.patch"
      via: "build script copies the committed patch into the fresh tree's debian/patches/ and appends to series"
      pattern: "onemix-touch.patch"
    - from: "patches/onemix-touch.patch"
      to: "build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c"
      via: "patch carries the diagnostic gate and frozen gesture delta from the working tree"
      pattern: "touchDiagEnabled"
  prohibitions:
    - requirement_id: DIAG-01
      category: safety
      statement: "MUST NOT alter touch/gesture event routing, classifier constants, timers, or cleanup behavior when diagnostics are disabled — the frozen local-only recognizer is the release candidate (D-02, D-14)"
    - requirement_id: PACK-01
      category: safety
      statement: "MUST NOT hold or pin the local package above future Debian security releases — a newer Debian version may replace the touch build (D-08)"
---

<objective>
Add the FREERDP_TOUCH_DIAG diagnostic gate to the frozen working-tree source, crystallize the complete source delta (frozen gestures plus diagnostics) into one committed quilt patch, and prove a reproducible Debian build pipeline that produces the exact four-package runtime closure.

Purpose: This is the tracer plan — it proves the full source-change → patch → build → artifact path end-to-end. Every later Phase 4 artifact depends on this patch existing and building cleanly.
Output: patches/onemix-touch.patch, scripts/build-release.sh, dist symlink to bundle with 4 .deb files + SHA256SUMS, .gitignore updated with dist + .dist-bundle-* + .dist.lock + .dist-symlink-*
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-RESEARCH.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-PATTERNS.md
@scripts/build-baseline.sh
@build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
@build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h
@build/freerdp3-3.15.0+dfsg/channels/rdpei/client/rdpei_main.c
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add FREERDP_TOUCH_DIAG gate to X11 and RDPEI source, build to prove compilation</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c, build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h, build/freerdp3-3.15.0+dfsg/channels/rdpei/client/rdpei_main.c</files>
  <read_first>
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c (working tree — contains frozen local-only gesture state machine; note xf_input_init at line 348, xf_input_touch_remote at line 1558 with existing WLog_DBG at 1572, xf_touch_force_cancel at line 1603)
    - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h (WITH_XI block at lines 303-350 — all gesture state lives here; add touchDiagEnabled adjacent to fallbackActive)
    - build/freerdp3-3.15.0+dfsg/channels/rdpei/client/rdpei_main.c (rdpei_send_touch_event_pdu at line 710 — frame send at line 754; RDPEI_PLUGIN struct at line 71)
    - 04-PATTERNS.md sections "gated local-touch diagnostics" and "conditional RDPEI frame record" — exact insertion seams and line references
    - 04-RESEARCH.md Pattern 2 and Pitfall 1 — WARN-level rationale (WLog defaults to INFO; DBG is invisible without WLOG_LEVEL=DEBUG)
  </read_first>
  <action>
Add the diagnostic gate to the working build tree source. Three files, all inside #ifdef WITH_XI guards where applicable:

1. xfreerdp.h: Add `BOOL touchDiagEnabled;` in the `#if defined(WITH_XI)` block (after `BOOL fallbackActive;` at line 313). This is the cached gate flag — no public API, no rdpSetting (D-03, D-14).

2. xf_input.c — initialization: In `xf_input_init` (line 348), after the existing state initialization block (after `xfc->scrollAccum = 0;` at line 396, before `PubSub_SubscribeTimer`), add one translation-unit-local env check: read `getenv("FREERDP_TOUCH_DIAG")` and set `xfc->touchDiagEnabled = (env != NULL && strcmp(env, "1") == 0)`. Follow the existing local env-access precedent from xf_utils.c (NOLINTNEXTLINE comment for concurrency-mt-unsafe). Only "1" enables; unset, "0", "false", or any other value is off (D-03).

3. xf_input.c — gated WARN records at existing seams (do NOT change routing, thresholds, or output calls — only add observational records guarded by `xfc->touchDiagEnabled`):

**Diagnostic marker (mandatory):** Every newly added diagnostic record's format string MUST begin with the stable prefix `touch-diag:` (for example `"touch-diag: touch ev=%s id=%d x=%d y=%d"`). This marker lets static checks distinguish Phase 4 diagnostics from pre-existing `WLog_WARN`/`WLog_ERROR` calls already in the file. Do NOT gate, wrap, alter, or remove any pre-existing `WLog_WARN` or `WLog_ERROR` record — only add new `touch-diag:` records. Every new guarded record MUST be a single line in the form `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: ...", args)` so the gate flag and the marker are grep-distinguishable on one line.

   - Ingress (xf_input_touch_remote, ~line 1565): Convert the existing `WLog_DBG(TAG, "touch_remote: ...")` to `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: touch ev=%s id=%d x=%d y=%d", evname, ...)`. Keep the same evname/fields.
   - Synthesis output points: Add `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: ...")` immediately beside each existing synthesized button/wheel/key call. Record the emitted action (for example `"touch-diag: synth left-down"`, `"touch-diag: synth wheel-up"`, `"touch-diag: synth ctrl-down"`, `"touch-diag: synth mid-down"`) and coordinates. Do NOT wrap or replace the synthesis calls. Key sites per PATTERNS.md: Ctrl-down (~993), pinch wheel detents (~876), scroll wheel detents (~1272), middle-button (~1079, ~1484), left-button drag/tap/release (~1354, ~1532, ~1545), long-press right-click (~324).
   - State transitions: Add one compact gated WARN at each recognizer transition (pending→pinch, pending→scroll, pending→three-finger, long-press arm/fire/cancel, tap/drag release, quarantine/recovery). Record the new state and touch count (for example `"touch-diag: state pending->pinch contacts=%d"`).
   - xf_touch_force_cancel (~line 1603): Add `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: force_cancel ctrl=%d contacts=%d quarantined=%d", ...)` at the top of the function, recording the cleanup decision. Do NOT add a second teardown state machine. The canonical `+touch-pinch-wheel-fallback` path (without `+multitouch`) creates no RDPEI contacts — `cctx->contacts[i]` all have `id == 0` — but the X11-local `xfc->contacts[]` array (touchContact, MAX_CONTACTS entries) tracks the active local physical fingers for the fallback gesture recognizer. Therefore, BEFORE clearing local gesture state (before the block starting at ~line 1636), add a gated diagnostic loop over `xfc->contacts[]` emitting `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: event=cancel id=%d x=%d y=%d", xfc->contacts[i].id, (int)xfc->contacts[i].pos_x, (int)xfc->contacts[i].pos_y)` for each active local contact where `xfc->contacts[i].id != 0`. This is the per-contact cancel evidence for the canonical local-only path. Additionally, in the existing RDPEI cancel loop (~line 1734) where `cctx->contacts[i].id != 0` and `rdpei->TouchCancel(rdpei, c->id, c->x, c->y, &dummy)` is called, add a compact per-contact record immediately beside that call: `if (xfc->touchDiagEnabled) WLog_WARN(TAG, "touch-diag: event=cancel id=%d x=%d y=%d", c->id, c->x, c->y)`. This RDPEI cancel record fires ONLY when actual native contacts exist (conditional on the `cctx->contacts[i].id != 0` guard already in the loop), which does NOT happen in the canonical local-only path — so it provides evidence only when a native/RDPEI path is actually exercised.

4. rdpei_main.c — cached RDPEI diagnostic bool and gated frame record. Add a private `BOOL touchDiagEnabled;` field to the `RDPEI_PLUGIN` struct (line 71) and initialize it in `init_plugin_cb` (line 1483) with the exact expression `env != NULL && strcmp(env, "1") == 0` where `env = getenv("FREERDP_TOUCH_DIAG")` (with the same NOLINTNEXTLINE(concurrency-mt-unsafe) precedent as xf_utils.c). In `rdpei_send_touch_event_pdu` (line 710), immediately after the real `rdpei_send_pdu` call (line 754) and before `Stream_Free`, add one single-line record guarded by that cached bool: `if (rdpei->touchDiagEnabled) WLog_Print(rdpei->base.log, WLOG_WARN, "touch-diag: rdpei frame contacts=%u offset=%u size=%lu status=%u", frame->contactCount, frame->frameOffset, Stream_Length(s), status)`. The frame record must be guarded by the cached RDPEI bool (not a per-frame getenv call) and must carry the `touch-diag:` marker. Preserve allocation, seal, send, free, and return order exactly (Pitfall 2: do NOT rely on the WITH_DEBUG_RDPEI block at line 624 — Debian builds with WITH_DEBUG_ALL=OFF). Do NOT gate or alter any pre-existing `WLog_Print`/`WLOG_ERROR` record in rdpei_main.c.

All new records use WLog_WARN / WLOG_WARN with the `touch-diag:` prefix, never WLog_DBG (Pitfall 1: WLog defaults to INFO, DBG is invisible). The gate is the ONLY source behavior change — no gesture logic, no classifier constant, no timer, no cleanup change (D-02, D-14). Pre-existing warning/error logs remain untouched and ungated.

After editing, build the working tree: `cd build/freerdp3-3.15.0+dfsg && dpkg-buildpackage -us -uc -b -j"$(nproc)"` to prove compilation succeeds. Also compile the classifier regression check: `cc -DWITH_XI -o /tmp/test_scroll_classifier build/freerdp3-3.15.0+dfsg/client/X11/test_scroll_classifier.c -lm && /tmp/test_scroll_classifier` — must print OK.
  </action>
  <verify>
    <automated>set -o pipefail; cd /home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg && dpkg-buildpackage -us -uc -b -j"$(nproc)" 2>&1 | tail -20 && cc -DWITH_XI -o /tmp/test_scroll_classifier /home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg/client/X11/test_scroll_classifier.c -lm && /tmp/test_scroll_classifier && X11F=/home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c && RDPEIF=/home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg/channels/rdpei/client/rdpei_main.c && grep -q 'touchDiagEnabled' /home/hoang/freerdp-touch/build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h && grep -q 'FREERDP_TOUCH_DIAG' "$X11F" && grep 'FREERDP_TOUCH_DIAG' "$X11F" | grep -q '"1"' && [ "$(grep -c 'touch-diag' "$X11F")" -ge 7 ] && ! grep 'WLog_DBG' "$X11F" | grep -q 'touch-diag' && [ "$(grep 'WLog_WARN' "$X11F" | grep -c 'touch-diag')" -ge 7 ] && grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -qi 'touch.*ev=' && grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -qi 'force_cancel' && grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -qi 'event=cancel' && grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -qi 'synth\|button\|wheel\|ctrl' && grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -qi 'pending\|pinch\|scroll\|long.press\|tap\|drag' && [ "$(grep 'WLog_WARN' "$X11F" | grep 'touch-diag' | grep -cv 'touchDiagEnabled')" -eq 0 ] && grep -q 'touchDiagEnabled' "$RDPEIF" && grep 'touchDiagEnabled' "$RDPEIF" | grep -q 'strcmp.*"1"' && grep -q 'touch-diag' "$RDPEIF" && [ "$(grep 'WLog_Print' "$RDPEIF" | grep 'WLOG_WARN' | grep -c 'touch-diag')" -eq 1 ] && ! grep 'WLOG_DEBUG' "$RDPEIF" | grep -q 'touch-diag' && [ "$(grep 'WLog_Print' "$RDPEIF" | grep 'WLOG_WARN' | grep 'touch-diag' | grep -cv 'touchDiagEnabled')" -eq 0 ] && ! grep -q '+multitouch' "$X11F" && ! grep -qi 'password\|/p:' "$X11F" && ! grep -qi 'password\|/p:' "$RDPEIF"</automated>
  </verify>
  <done>
Working tree builds successfully (exit status preserved via pipefail, not masked by tail). Classifier regression check prints OK. Source contains touchDiagEnabled in xfreerdp.h, FREERDP_TOUCH_DIAG getenv in xf_input_init with strcmp "1" gate (default-off: unset or any other value produces no diagnostic output), gated WLog_WARN records (not WLog_DBG) carrying the `touch-diag:` marker prefix at touch ingress (Begin/Update/End), state transitions (pending/pinch/scroll/long-press/tap/drag), synthesized output (button/wheel/ctrl), force-cancel summary, per-contact event=cancel records for active local physical touches (xfc->contacts[] loop before local state clearing — the canonical local-only path), and conditional per-contact event=cancel records in the RDPEI cancel loop (cctx->contacts[] — fires only when native contacts exist) in xf_input.c (at least 7 touch-diag marked records), and a cached RDPEI touchDiagEnabled bool (initialized by env != NULL && strcmp "1" == 0) guarding a single-line WLOG_WARN frame record carrying the `touch-diag:` marker in rdpei_main.c. Every `touch-diag:` marked WLog_WARN/WLOG_WARN record is gated by a touchDiagEnabled check (zero ungated marked records); pre-existing WLog_WARN/WLog_ERROR/WLOG_ERROR records are NOT gated or altered. No gesture routing, classifier constant, timer, or cleanup path was altered. No +multitouch reintroduced. No password or /p: material in diagnostic format strings.
  </done>
  <acceptance_criteria>
- Working tree compiles via dpkg-buildpackage (exit status preserved via pipefail)
- Classifier regression check prints OK
- xfreerdp.h contains BOOL touchDiagEnabled in the WITH_XI block
- xf_input.c contains FREERDP_TOUCH_DIAG getenv with strcmp "1" gate (default-off)
- xf_input.c contains at least 7 `touch-diag:` marked WLog_WARN sites; zero `touch-diag:` markers appear in any WLog_DBG call (positively proven: grep WLog_DBG piped to grep touch-diag returns nothing)
- All required X11 diagnostic classes appear in gated WLog_WARN calls (not comments, not WLog_DBG): touch event= ingress, force_cancel summary, event=cancel per-contact for active local physical touches (xfc->contacts[] loop before local state clearing — canonical path), event=cancel per-contact in the RDPEI cancel loop (cctx->contacts[] — conditional on native contacts only), synth/button/wheel/ctrl synthesized output, pending/pinch/scroll/long-press/tap/drag state transitions — each scoped to WLog_WARN lines containing the touch-diag marker
- Every `touch-diag:` marked WLog_WARN line in xf_input.c is gated by touchDiagEnabled (scoped grep -cv on marked lines returns 0); pre-existing WLog_WARN lines without the marker are NOT gated or altered
- rdpei_main.c contains a cached BOOL touchDiagEnabled in RDPEI_PLUGIN initialized by env != NULL && strcmp(env, "1") == 0
- rdpei_main.c contains exactly one `touch-diag:` marked WLog_Print(..., WLOG_WARN, ...) frame record guarded by rdpei->touchDiagEnabled; zero `touch-diag:` markers appear in any WLOG_DEBUG call
- Every `touch-diag:` marked WLOG_WARN line in rdpei_main.c is gated by touchDiagEnabled (scoped grep -cv on marked lines returns 0); pre-existing WLOG records are NOT gated or altered
- No +multitouch in xf_input.c
- No password or /p: material in diagnostic format strings in xf_input.c or rdpei_main.c
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Install quilt build prerequisite</name>
  <files></files>
  <read_first>
    - 04-RESEARCH.md "Environment Availability" section — quilt is the sole missing build prerequisite (APT candidate 0.68-1, no fallback allowed)
    - 04-PATTERNS.md "Pattern 1: Freeze the source delta in one quilt patch" — quilt workflow
  </read_first>
  <action>
Install the quilt package, which is the sole missing build prerequisite per 04-RESEARCH.md. This must be completed before Task 3 (crystallize patch and build) which uses quilt to create and apply the project patch. Do not put interactive sudo inside an automatic task.
  </action>
  <instructions>
Run `sudo apt install quilt` in a terminal, then confirm it is available with `quilt --version`. Resume only after `quilt --version` prints a version string.
  </instructions>
  <verification>
Confirm `quilt --version` prints a version string (APT candidate 0.68-1 per 04-RESEARCH.md). This checkpoint gates Task 3 (crystallize patch and build), which calls `quilt new`, `quilt add`, and `quilt refresh`.
  </verification>
  <verify>
    <automated>command -v quilt && quilt --version</automated>
  </verify>
  <acceptance_criteria>
- quilt is installed and `quilt --version` succeeds (prints a version string)
- This checkpoint must pass before Task 3 (crystallize patch) can execute
  </acceptance_criteria>
  <resume-signal>Type "approved" once quilt --version succeeds, or describe the installation failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Crystallize quilt patch, create build-release.sh, build the 4-package closure</name>
  <files>patches/onemix-touch.patch, scripts/build-release.sh, .gitignore</files>
  <read_first>
    - scripts/build-baseline.sh (Phase 1 pipeline — reuse pin-check, dpkg-buildpackage, checksum, and exact-install patterns; keep this script separate per D-09)
    - build/freerdp3-3.15.0+dfsg/debian/patches/series (current Debian patch stack — append after last entry `channels-drive-refine-bounds-checks-CVE-2026-40254.patch`)
    - build/freerdp3-3.15.0+dfsg/debian/changelog (current stock version — prepend +onemix1 entry)
    - build/freerdp3-3.15.0+dfsg/debian/control lines 68-112 (exact-version dependency chain: freerdp3-x11 → libfreerdp-client3-3 → libfreerdp3-3 → libwinpr3-3)
    - 04-PATTERNS.md "Pattern 1: Freeze the source delta in one quilt patch" and "Runtime-bundle rule" sections
    - .gitignore (existing artifact hygiene — append dist/)
  </read_first>
  <precondition>quilt must be installed and `quilt --version` must succeed (Task 2 checkpoint)</precondition>
  <action>
Create the committed quilt patch and the repeatable build pipeline. quilt is already installed (Task 2 checkpoint).

STEP 1 — Crystallize the patch from a fresh extraction:
1. Extract a fresh tree from the pinned .dsc: `dpkg-source -x build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc /tmp/onemix-clean` (this applies Debian's existing patch stack automatically).
2. Produce the project patch by diffing the working tree source against the clean extraction. The patch must include every file listed in 04-PATTERNS.md "Required patch payload": client/X11/xf_input.c, client/X11/xfreerdp.h, channels/rdpei/client/rdpei_main.c, client/X11/xf_input.h, client/X11/xf_event.c, client/X11/xf_client.c, client/common/cmdline.c, client/common/cmdline.h, include/freerdp/settings_types_private.h, libfreerdp/common/settings_getters.c, libfreerdp/common/settings_str.h, client/X11/test_scroll_classifier.c, and debian/changelog.
3. Use quilt to create the patch properly: in the fresh tree, `QUILT_PATCHES=debian/patches quilt new onemix-touch.patch`, then `quilt add` each source file, copy the working-tree version over the clean version, prepend the +onemix1 changelog entry to debian/changelog (version `3.15.0+dfsg-2.1+deb13u3+onemix1`, urgency=medium, "Local OneMix 3 touch gesture patch"), then `quilt refresh`. The resulting `debian/patches/onemix-touch.patch` is the committed patch.
4. Copy it to the repo: `cp /tmp/onemix-clean/debian/patches/onemix-touch.patch patches/onemix-touch.patch`.
5. The patch must NOT include debian/patches/series (the build script owns that append) and must NOT include obj-*, debian/.debhelper, debian/tmp, or binary packages (D-11).

STEP 2 — Create scripts/build-release.sh:
A non-interactive Bash script (`set -eu`) that automates Steps 1-2 plus the build and closure validation. Stages:
1. Pin-check: assert `apt-cache showsrc freerdp3` resolves to `3.15.0+dfsg-2.1+deb13u3` (reuse build-baseline.sh Stage 2 pattern).
2. Fresh extraction: `dpkg-source -x` the pinned .dsc into a temp directory.
3. Patch application: copy `patches/onemix-touch.patch` into the fresh tree's `debian/patches/`, append `onemix-touch.patch` as the last non-comment line in `debian/patches/series`, then `QUILT_PATCHES=debian/patches quilt push -a`.
4. Version assertion: verify `dpkg-parsechangelog -SVersion` in the fresh tree returns exactly `3.15.0+dfsg-2.1+deb13u3+onemix1` (D-07).
5. Classifier check: compile and run `client/X11/test_scroll_classifier.c` to `/tmp` — must print OK (preserve frozen 2.5 dominance ratio per D-02, D-14).
6. Build: `dpkg-buildpackage -us -uc -b -j"$(nproc)"` in the fresh tree.
7. Closure selection: from the build output, select exactly these four same-version packages in dependency order: `libwinpr3-3_*_amd64.deb`, `libfreerdp3-3_*_amd64.deb`, `libfreerdp-client3-3_*_amd64.deb`, `freerdp3-x11_*_amd64.deb` (04-PATTERNS.md corrected four-package closure). For each, validate `dpkg-deb -f` shows Package, Version=`3.15.0+dfsg-2.1+deb13u3+onemix1`, Architecture=amd64. Never use a `freerdp3-*.deb` glob. Never include freerdp3-dev (D-10).
8. Atomic symlink pointer swap (interruption-safe, serialized): do NOT build or copy directly into `dist/`. Acquire a non-blocking `flock -n` on a project-local lock file: `LOCKFILE="$(pwd)/.dist.lock"; exec 9>"$LOCKFILE"; if ! flock -n 9; then echo "ERROR: another publisher holds the dist lock ($LOCKFILE); dist untouched." >&2; exit 1; fi`. If another publisher holds the lock, fail immediately with a clear message and leave `dist` untouched (Ponytail/YAGNI: no waiting, no retry). Initialize cleanup variables BEFORE installing the trap: `bundle_dir=""; tmp_link=""`. Install the cleanup trap immediately after lock acquisition and BEFORE any bundle creation: `trap 'rc=$?; if [ -n "$bundle_dir" ] && [ "$(readlink -f dist 2>/dev/null)" != "$bundle_dir" ]; then rm -rf "$bundle_dir"; fi; rm -f "$tmp_link"; exit $rc' EXIT INT HUP TERM`. This trap is publication-aware: it never deletes a bundle that `dist` currently resolves to (if the swap succeeded, `readlink -f dist` resolves to `$bundle_dir` and the guard skips deletion — no fragile published flag whose update can race with a signal). Build and validate a unique same-filesystem bundle directory: `bundle_dir="$(pwd)/.dist-bundle-$(date -u +%Y%m%dT%H%M%SZ)-$$"; mkdir -p "$bundle_dir"`. Copy the four validated .deb files into `"$bundle_dir"` and generate `"$bundle_dir/SHA256SUMS"` with basenames only (so `sha256sum -c` can run inside the bundle directory): `cd "$bundle_dir" && sha256sum libwinpr3-3_*_amd64.deb libfreerdp3-3_*_amd64.deb libfreerdp-client3-3_*_amd64.deb freerdp3-x11_*_amd64.deb > SHA256SUMS && cd -`. Validate the four packages AND run checksum verification inside the bundle directory: `(cd "$bundle_dir" && sha256sum -c SHA256SUMS)` BEFORE touching `dist/`. Only after bundle validation passes, create a temporary relative symlink: `tmp_link="$(pwd)/.dist-symlink-$$"; ln -s "$(basename "$bundle_dir")" "$tmp_link"`. Then atomically swap: `mv -Tf "$tmp_link" dist` — this is a rename(2), atomic on the same filesystem; the prior `dist` target remains reachable until this single syscall succeeds. After the swap, the trap's publication-aware guard sees `readlink -f dist` resolving to `$bundle_dir` and skips deletion. Clean up old bundles not pointed to by `dist`: if `dist` is a symlink, `current_target="$(readlink dist)"`; for each `.dist-bundle-*` dir not equal to `$current_target`, `rm -rf` it. On ANY failure before the swap (extraction, patch, build, or bundle validation), exit non-zero — the trap cleans up the bundle (since `readlink -f dist` does NOT resolve to `$bundle_dir`) and `dist` still points to the prior valid bundle. Never `rm -rf dist` — `dist` is a symlink, not a directory. Do NOT add any test hook or test-specific env var to the production build script — all interruption and concurrency tests use temporary rewritten copies or external lock holders, never production-only hook variables.
9. Report: print the retained artifact names, versions, and SHA-256 checksums to stderr.

STEP 3 — Add generated paths to .gitignore:
Append `dist`, `.dist-bundle-*`, `.dist.lock`, and `.dist-symlink-*` as new lines at the end of .gitignore, following the existing generated-artifact convention (D-11: commit patch/scripts/docs only, never build output or binary packages). Use `dist` (no trailing slash) so the gitignore pattern matches the symlink, not just a directory.

STEP 4 — Run the build script twice and verify reproducibility:
First, remove the dist symlink if present: `rm -f dist` (removes the symlink only, not the bundle it points to; any orphaned bundle directories are cleaned up by the build script). Run `./scripts/build-release.sh`. Verify dist is a symlink resolving to a bundle with exactly 4 .deb files + SHA256SUMS: `[ -L dist ]` and `[ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ]`. Run `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` to verify checksums (manifest entries are basenames, so verification runs inside the resolved bundle directory). Save the first-run manifest and package list outside dist: `cp "$(readlink -f dist)/SHA256SUMS" /tmp/onemix-first.sha256sums` and `ls "$(readlink -f dist)"/*.deb | xargs -n1 basename | sort > /tmp/onemix-first.list` and `dpkg-deb -f "$(readlink -f dist)/freerdp3-x11_*_amd64.deb" Version > /tmp/onemix-first.ver`. Then remove the dist symlink: `rm -f dist`. Run `./scripts/build-release.sh` a second time. Verify dist is a symlink resolving to a bundle with exactly 4 .deb files + SHA256SUMS and `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes. Compare the second-run package list and version against the first: package filenames must match, version must match, and all four packages must show +onemix1. Reproducibility is defined as two clean successful builds with identical package names, versions, and dependency closure, plus valid checksums on each run — NOT byte-identical .deb files, because Debian metadata may embed timestamps.

STEP 5 — Verify atomic-swap, failure-preservation, interrupted swap, concurrency, and post-swap cleanup safety:
1. Stale-file removal: ensure a valid `dist` symlink exists (from STEP 4). Pre-seed stale package-like files in the prior bundle directory: `touch "$(readlink -f dist)/stale-leftover-1.deb" "$(readlink -f dist)/old-freerdp3-sdl.deb"`. Run `./scripts/build-release.sh`. After success, `dist` is a symlink to a NEW bundle with EXACTLY the four named packages plus SHA256SUMS — the stale files remain in the old bundle (which is cleaned up) and do not appear in the new bundle. Assert: `[ -L dist ]`, exactly 4 `$(readlink -f dist)/*.deb`, `$(readlink -f dist)/SHA256SUMS` exists, `$(readlink -f dist)/stale-leftover-1.deb` and `$(readlink -f dist)/old-freerdp3-sdl.deb` do NOT exist, and `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes.
2. Failure-preservation (pre-swap): record the current valid `dist` target and checksums: `before_target="$(readlink -f dist)"` and `sha256sum "$(readlink -f dist)"/*.deb > /tmp/onemix-dist-before.sha256`. Temporarily break the build by moving the patch aside: `mv patches/onemix-touch.patch patches/onemix-touch.patch.bak`. Run `./scripts/build-release.sh` — it MUST exit non-zero (fails at the `cp patches/onemix-touch.patch` step before any pointer swap). Assert `dist` is unchanged: `[ "$(readlink -f dist)" = "$before_target" ]` and `sha256sum "$(readlink -f dist)"/*.deb > /tmp/onemix-dist-after.sha256` and `diff /tmp/onemix-dist-before.sha256 /tmp/onemix-dist-after.sha256`. Restore the patch: `mv patches/onemix-touch.patch.bak patches/onemix-touch.patch`.
3. Post-publish interruption test (temp-copy injection, NO production test hook): make a temporary rewritten copy of `scripts/build-release.sh` — copy it to a temp file and use sed to insert a deterministic marker plus a blocking wait immediately AFTER the `mv -Tf` dist pointer swap and BEFORE old-bundle cleanup. The injected lines create a marker file (e.g., `touch "$(pwd)/.dist-post-publish-marker"` ) and then `sleep 30` to block indefinitely before cleanup. Run this modified copy in the background. Poll for the marker file (up to 10 seconds). Once the marker appears, the swap has succeeded and the script is blocking before cleanup. Send TERM to the process. Assert: the process exits nonzero or is reported as terminated by the signal; `dist` resolves to the newly published bundle (`[ "$(readlink -f dist)" != "$before_target" ]`); the target bundle directory still exists (`[ -d "$(readlink -f dist)" ]`); `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes; the cleanup did not delete the live bundle (the publication-aware trap saw `dist` pointing at the new bundle and skipped deletion, or the signal arrived before cleanup but the bundle survives because the swap already completed). This test proves that a post-publish interruption cannot corrupt or delete the live bundle — the swap is atomic and the trap is publication-aware. The action and acceptance criteria explicitly require this temp-copy injection and no production-only hook variable.
4. Concurrent publishers (external lock holder, NO production test hook): with a valid `dist` from step 1, hold the lock externally — start a background subshell that acquires `flock -n` on the same lock file the script uses (`$(pwd)/.dist.lock`), touches a marker file, and sleeps: `( exec 9>"$(pwd)/.dist.lock"; flock -n 9 && touch "$(pwd)/.dist-lock-held" && sleep 5 ) & lock_pid=$!`. Poll for the `.dist-lock-held` marker (up to 5 seconds). Once the marker exists, the lock is held. Run the real `./scripts/build-release.sh` — it MUST exit non-zero immediately with the busy message ("another publisher holds the dist lock") and MUST NOT create or modify any bundle or `dist`. Assert it exited non-zero. Wait for the lock-holder to finish (`wait $lock_pid`), then clean up the marker (`rm -f .dist-lock-held`). Assert `dist` is unchanged: still a valid symlink resolving to one complete validated bundle with exactly 4 .deb files + SHA256SUMS, `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes, and no new `.dist-bundle-*` directory was created by the rejected invocation.
5. Post-swap cleanup safety (forced signal/error after swap): ensure a valid `dist` exists from step 1. Record `before_target="$(readlink -f dist)"`. Run a full successful build: `./scripts/build-release.sh`. After the swap succeeds, `dist` now points to a NEW bundle. Verify the publication-aware trap did NOT delete the new bundle: `[ "$(readlink -f dist)" != "$before_target" ]` (new bundle is live) and `[ -d "$(readlink -f dist)" ]` (new bundle directory exists) and exactly 4 `.deb` files + SHA256SUMS remain in the new bundle and `(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)` passes — the cleanup saw `dist` pointing at the new bundle and correctly skipped deletion. The old bundle (`$before_target`) should have been cleaned up by the old-bundle sweep (it is no longer the `dist` target).
  </action>
  <verify>
    <automated>set -e; cd /home/hoang/freerdp-touch
# Build 1: fresh dist symlink (rm -f removes the symlink, not the bundle)
rm -f dist; ./scripts/build-release.sh
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
# Stale files: pre-seed in prior bundle, rebuild, new bundle is clean
touch "$(readlink -f dist)/stale-leftover-1.deb" "$(readlink -f dist)/old-freerdp3-sdl.deb"
./scripts/build-release.sh
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && [ -f "$(readlink -f dist)/SHA256SUMS" ] && [ ! -f "$(readlink -f dist)/stale-leftover-1.deb" ] && [ ! -f "$(readlink -f dist)/old-freerdp3-sdl.deb" ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
# Reproducibility: save manifest, rebuild, compare
cp "$(readlink -f dist)/SHA256SUMS" /tmp/onemix-first.sha256sums
ls "$(readlink -f dist)"/*.deb | xargs -n1 basename | sort > /tmp/onemix-first.list
dpkg-deb -f "$(readlink -f dist)/freerdp3-x11_*_amd64.deb" Version > /tmp/onemix-first.ver
rm -f dist; ./scripts/build-release.sh
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
diff /tmp/onemix-first.list <(ls "$(readlink -f dist)"/*.deb | xargs -n1 basename | sort)
diff /tmp/onemix-first.ver <(dpkg-deb -f "$(readlink -f dist)/freerdp3-x11_*_amd64.deb" Version)
dpkg-deb -f "$(readlink -f dist)/freerdp3-x11_*_amd64.deb" Version | grep -q '+onemix1'
# Failure-preservation: break patch, build fails before swap, dist unchanged
before_target="$(readlink -f dist)"
sha256sum "$(readlink -f dist)"/*.deb > /tmp/onemix-dist-before.sha256
mv patches/onemix-touch.patch patches/onemix-touch.patch.bak
set +e; ./scripts/build-release.sh; rc=$?; set -e
mv patches/onemix-touch.patch.bak patches/onemix-touch.patch
[ "$rc" -ne 0 ]
[ "$(readlink -f dist)" = "$before_target" ]
sha256sum "$(readlink -f dist)"/*.deb > /tmp/onemix-dist-after.sha256
diff /tmp/onemix-dist-before.sha256 /tmp/onemix-dist-after.sha256
# Post-publish interruption test: temp-copy injection with marker + blocking wait AFTER mv -Tf, BEFORE cleanup
# Make a temp copy and inject a marker + sleep after the swap line
test_script=$(mktemp /tmp/build-release-test-XXXXXX.sh)
cp scripts/build-release.sh "$test_script"
# Inject marker + blocking wait immediately after the mv -Tf swap line, before old-bundle cleanup
sed -i '/mv -Tf "$tmp_link" dist/a\touch "$(pwd)/.dist-post-publish-marker"\nsleep 30' "$test_script"
chmod +x "$test_script"
rm -f .dist-post-publish-marker
"$test_script" &
test_pid=$!
# Poll for marker (up to 10 seconds — the build takes time, the marker appears after the swap)
for i in $(seq 1 100); do [ -f .dist-post-publish-marker ] && break; sleep 0.1; done
[ -f .dist-post-publish-marker ]  # marker must appear (swap succeeded)
# Send TERM to the process
kill -TERM "$test_pid" 2>/dev/null
wait "$test_pid" 2>/dev/null; test_rc=$?
[ "$test_rc" -ne 0 ]  # process must exit nonzero or be terminated
# dist resolves to the newly published bundle (not the prior one)
[ "$(readlink -f dist)" != "$before_target" ]
# Target bundle still exists
[ -d "$(readlink -f dist)" ]
# Checksums pass on the live bundle
(cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
# Cleanup did not delete the live bundle
[ -d "$(readlink -f dist)" ]
rm -f .dist-post-publish-marker "$test_script"
# Concurrent publishers: external lock holder (NO production test hook)
( exec 9>"$(pwd)/.dist.lock"; flock -n 9 && touch "$(pwd)/.dist-lock-held" && sleep 5 ) &
lock_pid=$!
for i in $(seq 1 50); do [ -f .dist-lock-held ] && break; sleep 0.1; done
[ -f .dist-lock-held ]  # lock is held
set +e; ./scripts/build-release.sh; rc2=$?; set -e
[ "$rc2" -ne 0 ]  # second publisher must fail immediately (non-blocking flock -n)
wait $lock_pid 2>/dev/null || true
rm -f .dist-lock-held
[ -L dist ] && [ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
# Post-swap cleanup safety: full build, then verify new bundle NOT deleted by trap
before_swap="$(readlink -f dist)"
./scripts/build-release.sh
[ "$(readlink -f dist)" != "$before_swap" ]  # dist points to new bundle
[ -d "$(readlink -f dist)" ]  # new bundle directory still exists
[ "$(ls "$(readlink -f dist)"/*.deb | wc -l)" -eq 4 ] && (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS)
[ ! -d "$before_swap" ]  # old bundle cleaned up by sweep
# gitignore: dist symlink + generated bundle/lock/symlink paths
grep -qFx 'dist' .gitignore
grep -qFx '.dist-bundle-*' .gitignore
grep -qFx '.dist.lock' .gitignore
grep -qFx '.dist-symlink-*' .gitignore</automated>
  </verify>
  <done>
patches/onemix-touch.patch exists and contains the complete source delta (xf_input.c, xfreerdp.h, rdpei_main.c, and all other payload files) plus the +onemix1 changelog entry. scripts/build-release.sh runs end-to-end and produces dist as a relative symlink resolving to a bundle directory with exactly 4 .deb files (libwinpr3-3, libfreerdp3-3, libfreerdp-client3-3, freerdp3-x11) all at version 3.15.0+dfsg-2.1+deb13u3+onemix1, plus SHA256SUMS with basename entries. The build script acquires a non-blocking flock -n on a project-local lock (failing immediately with a clear message if another publisher holds it), initializes cleanup variables before the trap, installs a publication-aware EXIT/INT/HUP/TERM trap that never deletes a bundle dist resolves to (no fragile published flag), builds and validates a unique same-filesystem bundle directory, runs checksum verification inside the bundle directory, creates a temp relative symlink, and atomically swaps it over the dist symlink via mv -Tf; the prior dist target remains reachable until the pointer swap succeeds. Checksums verify on each run via (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS). A second clean build produces the same package names and version (reproducibility: identical names/versions/closure, not byte-identical .deb files). Pre-seeding stale package-like files in the prior bundle followed by a successful build leaves the new bundle with exactly the four named packages plus SHA256SUMS (stale files do not leak into the new bundle). A failed build (patch moved aside) exits non-zero and leaves the prior valid dist unchanged (same target and checksums). A post-publish interruption test (temp-copy injection with no production test hook) proves that sending TERM after the swap succeeds and before cleanup leaves the newly published bundle intact: dist resolves to the new bundle, the target bundle exists, checksums pass, and the publication-aware trap did not delete the live bundle. Concurrent publishers are tested with an external lock holder (no production test hook): a background subshell acquires flock on the same lock file, and the real script must exit non-zero immediately with the busy message; after the lock-holder finishes, dist resolves to one complete validated bundle. A forced post-swap cleanup safety test proves the publication-aware trap sees dist pointing at the new bundle and does not delete it. .gitignore contains dist, .dist-bundle-*, .dist.lock, and .dist-symlink-*. The patch does NOT include debian/patches/series, obj-*, or binary packages. No apt hold or pin is created (D-08).
  </done>
  <acceptance_criteria>
- patches/onemix-touch.patch exists and contains diff --git for xf_input.c, xfreerdp.h, rdpei_main.c, and all required payload files plus the +onemix1 changelog entry
- scripts/build-release.sh runs end-to-end (set -eu, fresh extraction, non-blocking flock -n on project-local lock, cleanup variables initialized before trap, unique bundle directory on same filesystem, no interactive sudo)
- build-release.sh builds/validates in a bundle directory and atomically swaps a temp relative symlink over the dist symlink via mv -Tf only after bundle validation passes; on failure or signal before the swap, the publication-aware trap cleans up the bundle (never deleting one dist resolves to) and dist is untouched with the prior bundle remaining reachable
- dist is a relative symlink ([ -L dist ]) resolving to a bundle directory with exactly 4 .deb files: libwinpr3-3, libfreerdp3-3, libfreerdp-client3-3, freerdp3-x11 (no glob, no dev package)
- All 4 packages show version 3.15.0+dfsg-2.1+deb13u3+onemix1 via dpkg-deb -f
- (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS) passes on each run (manifest entries are basenames, verification runs inside the resolved bundle directory)
- SHA256SUMS contains exactly four basename entries (no path prefixes)
- A second clean build produces the same package names and version
- Pre-seeding stale package-like files in the prior bundle, then running a successful build, leaves the new bundle with exactly the four named packages plus SHA256SUMS (stale files do not leak into the new bundle)
- A failed build (patch moved aside so it exits non-zero before the swap) leaves the prior valid dist unchanged (same symlink target and checksums)
- A post-publish interruption test uses a temp-copy injection (NO production test hook variable): a temporary rewritten copy of scripts/build-release.sh inserts a deterministic marker plus a blocking wait immediately AFTER the mv -Tf dist pointer swap and BEFORE old-bundle cleanup; running that copy, waiting for the marker, and sending TERM proves: the process exits nonzero/terminated, dist resolves to the newly published bundle, the target bundle still exists, (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS) passes, and cleanup did not delete the live bundle
- No production test hook or test-specific env var (FREERDP_TOUCH_FAIL_SWAP, FREERDP_TOUCH_TEST_HOLD_LOCK, or similar) is present in scripts/build-release.sh — the action and acceptance criteria explicitly require the temp-copy injection for the post-publish interruption test and no production-only hook variable
- Concurrent publishers are tested with an external lock holder (NO production test hook): a background subshell acquires flock -n on the same lock file, the real script must exit non-zero immediately with the busy message before publication; after the lock-holder finishes, dist resolves to one complete validated bundle; dist always resolves to a complete valid 4+1 bundle after concurrent runs
- A forced post-swap cleanup safety test proves the publication-aware trap sees dist pointing at the new bundle and does not delete it (new bundle directory exists with 4 .deb + SHA256SUMS, old bundle cleaned up)
- .gitignore contains dist, .dist-bundle-*, .dist.lock, and .dist-symlink-*
- The patch does NOT include debian/patches/series, obj-*, or binary packages
- No apt hold or pin is created (D-08)
  </acceptance_criteria>
  <reversibility rating="reversible">The patch file and build script are committed artifacts; the dist/ output is gitignored and disposable. Rolling back means deleting dist/ and reverting the commit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → quilt patch | The patch crystallizes a gitignored working tree; a stale or dirty diff could carry unintended changes |
| Fresh extraction → build output | The build must start from the pinned clean source, not a stale tree |
| Build output → dist/ | Only the explicit 4-package closure may enter dist/; a glob would include unrelated packages |
| dist/ → installed system | The user installs from dist/; checksums must verify before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Diagnostic WLog_WARN records | high | mitigate | Records contain only event IDs, coordinates, state labels, and frame count/offset/status — never command arguments, /p: values, or credential material (D-06). Static grep verification: no `arg` or `password` or `/p:` in diagnostic format strings. |
| T-04-02 | Tampering | quilt patch crystallization | medium | mitigate | Patch is produced from a fresh dpkg-source extraction diffed against the working tree; no obj-*, debian/.debhelper, or binary files in the patch (D-11). Build script asserts post-patch changelog version. |
| T-04-03 | Tampering | Package closure selection | high | mitigate | Explicit 4-package list with dpkg-deb -f metadata validation (Package, Version, Architecture); never a freerdp3-*.deb glob. Four-package closure (libwinpr3-3, libfreerdp3-3, libfreerdp-client3-3, freerdp3-x11) per PATTERNS.md dependency-chain analysis (D-10). Build validates in a unique same-filesystem bundle directory and atomically swaps a temp relative symlink over the dist symlink via mv -Tf only after validation, serialized by flock, so a failed build, stale pre-existing bundle files, or a concurrent publisher cannot corrupt the released bundle. |
| T-04-04 | Denial of Service | Local package blocking security updates | medium | mitigate | No apt hold/pin created; +onemix1 suffix sorts below future Debian versions per dpkg --compare-versions (D-08). Documentation in Plan 03 explains detection and rebuild. |
| T-04-05 | Tampering | dpkg-buildpackage / quilt installs | high | mitigate | quilt is a standard Debian tool from the configured trixie APT source (Package Legitimacy Audit: no npm/PyPI/crates package applicable). No third-party build tool substituted. |
</threat_model>

<verification>
**Automated/static (can run in any session):**
- Working tree compiles via dpkg-buildpackage (exit status preserved via pipefail)
- Classifier regression check prints OK
- Static predicates prove all DIAG-01 record classes: touchDiagEnabled flag (X11), FREERDP_TOUCH_DIAG gate with strcmp "1" (default-off), `touch-diag:` marked WLog_WARN at ingress/transition/synthesis/force-cancel-summary/per-contact-cancel seams (>= 7 marked records), cached RDPEI touchDiagEnabled bool in RDPEI_PLUGIN (env != NULL && strcmp "1" == 0) guarding a `touch-diag:` marked WLOG_WARN frame record, every `touch-diag:` marked record gated by touchDiagEnabled (zero ungated marked records; pre-existing WLog_WARN/WLOG records not gated or altered), no +multitouch, no password or /p: in diagnostic format strings
- build-release.sh produces dist as a relative symlink to a bundle with exactly 4 .deb files + SHA256SUMS, atomically swapped via mv -Tf after non-blocking flock -n serialized validation; cleanup variables initialized before trap; publication-aware trap never deletes a bundle dist resolves to
- (cd "$(readlink -f dist)" && sha256sum -c SHA256SUMS) passes on each run (manifest entries are basenames, verification runs inside the resolved bundle directory)
- SHA256SUMS contains exactly four basename entries (no path prefixes)
- Pre-seeding stale files in the prior bundle followed by a successful build leaves the new bundle with exactly the four named packages plus SHA256SUMS
- A failed build (patch moved aside) leaves the prior valid dist symlink target unchanged
- A post-publish interruption test (temp-copy injection, no production test hook) sends TERM after the swap succeeds and before cleanup: process exits nonzero/terminated, dist resolves to the newly published bundle, target bundle exists, checksums pass, cleanup did not delete the live bundle
- No production test hook or test-specific env var is present in scripts/build-release.sh
- Concurrent publishers are tested with an external lock holder (no production test hook): a background subshell acquires flock on the same lock file, the real script exits non-zero immediately with the busy message; after the lock-holder finishes, dist resolves to one complete validated bundle
- A forced post-swap cleanup safety test proves the publication-aware trap sees dist pointing at the new bundle and does not delete it
- Two clean builds produce identical package names and version (reproducibility: names/versions/closure, not byte-identical .deb files)
- dpkg-deb -f shows +onemix1 version on all 4 packages
- .gitignore contains dist, .dist-bundle-*, .dist.lock, and .dist-symlink-*
- grep confirms no +multitouch in patch or build script (D-01)

**Native-X11/on-device (deferred to Plan 03 verification):**
- FREERDP_TOUCH_DIAG=1 produces visible touch records on stderr during a live session
- Unset FREERDP_TOUCH_DIAG produces no diagnostic output during a live session
</verification>

<success_criteria>
1. Diagnostic gate compiles, is cached at init, and uses WLog_WARN (not DBG) at all specified seams — DIAG-01 source-level evidence complete.
2. One committed quilt patch captures the frozen gesture delta + diagnostic gate + +onemix1 changelog — PACK-01 patch artifact complete.
3. build-release.sh reproducibly produces the 4-package closure with checksums — PACK-01 build artifact complete.
4. No gesture behavior, classifier constant, timer, or cleanup path was altered — D-02, D-14 preserved.
5. No +multitouch reintroduced, no apt hold/pin, no credential in diagnostic output — D-01, D-08, D-06 preserved.
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| `patches/onemix-touch.patch` | quilt patch | repo (committed) | Complete frozen source delta + diagnostic gate + changelog entry |
| `scripts/build-release.sh` | Bash script | repo (committed) | Repeatable release build pipeline |
| `dist` | relative symlink | repo (gitignored) | Points to the current validated bundle directory |
| `.dist-bundle-*` | bundle directory | repo (gitignored) | Contains the 4 .deb files + SHA256SUMS for one build |
| `dist/SHA256SUMS` | checksum manifest | bundle (gitignored) | Verification manifest for install |
| `BOOL touchDiagEnabled` | C struct field | xfreerdp.h WITH_XI block | Cached diagnostic gate flag |
| `FREERDP_TOUCH_DIAG` | environment variable | process env | Opt-in diagnostic gating (1=on, unset/other=off) |
| `dist` + `.dist-bundle-*` + `.dist.lock` + `.dist-symlink-*` gitignore entries | .gitignore lines | .gitignore | Artifact hygiene for symlink, bundles, lock, and temp symlinks |