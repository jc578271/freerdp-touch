---
phase: 04-diagnostics-packaging-launch-configuration
plan: 07
type: execute
wave: 2
depends_on: [04-04]
files_modified:
  - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
  - build/freerdp3-3.15.0+dfsg/client/X11/CMakeLists.txt
  - build/freerdp3-3.15.0+dfsg/client/X11/test/CMakeLists.txt
  - build/freerdp3-3.15.0+dfsg/client/X11/test/TestXfInputDispatcher.c
  - tests/check_third_finger_owner.py
  - patches/onemix-touch.patch
autonomous: true
requirements: [DIAG-01, CONF-01]
gap_closure: true
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After an interrupted local gesture, an old touch Update and each non-final old End stay quarantined; a repeated cancellation preserves the remaining old IDs; only the final old End reopens input for a fresh touch."
    - "Opt-in cancellation diagnostics use the adjusted-coordinate diagContacts lifecycle store, emit one cancel record per active local contact and one local-only summary with native_count=0, then retire that store."
    - "Third-finger cancellation retains every nonzero supplemental touch ID, and a pre-claim three-finger lift clears all pending three-finger fields before later gestures begin."
    - "The repaired quilt patch applies on a newly extracted Debian source tree whose real X11 dispatcher regression and source ownership checker both pass."
  artifacts:
    - "build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c"
    - "build/freerdp3-3.15.0+dfsg/client/X11/test/TestXfInputDispatcher.c"
    - "tests/check_third_finger_owner.py"
    - "patches/onemix-touch.patch"
  key_links:
    - "xf_input_handle_event reaches the actual xf_input_handle_event_remote XI2 dispatcher, which delegates quarantine changes to xf_quarantine_update."
    - "xf_input_touch_remote adjusts coordinates before updating diagContacts; xf_touch_cancel_lifecycle feeds that store through xf_force_cancel_emit_sequence once into the existing WLog path."
    - "The CTest target invokes the production dispatcher with synthetic XGenericEventCookie fixtures and linker-wrapped XGetEventData/XFreeEventData, not a copied state model."
    - "The refreshed patch is applied through the Debian quilt stack in a new extraction before the same dispatcher CTest is run."
---

<objective>
Close only verification Gaps 2 and 3: make cancellation quarantine ownership correct through the real XI2 dispatcher and make local-only cancellation diagnostics truthful, then crystallize those exact source changes into the shipping quilt patch.

Purpose: Restore interruption safety and diagnostic evidence for the already frozen local-only launch behavior without broadening Phase 4 release, launcher, package, credential, log-publication, or certificate work. This preserves D-01 and D-02 while implementing D-03 through D-06; D-25 remains unchanged.

Output: A production-source repair, one CTest dispatcher regression, a strengthened scoped source invariant checker, and a cleanly applicable `patches/onemix-touch.patch`.

Decision coverage: D-01: preserve disabled native multitouch; D-02: preserve the frozen local recognizer; D-03: retain the exact diagnostic gate; D-04: retain compact lifecycle diagnostics; D-05: use the existing stderr/WLog route; D-06: keep diagnostic records credential-free; D-07: preserve package version policy; D-08: preserve security-update policy; D-09: preserve release-script policy; D-10: preserve exact-package policy; D-11: preserve tracked-artifact policy; D-12: preserve install/rollback policy; D-13: preserve calibration surface; D-14: preserve classifier constants; D-15: preserve CLI mapping; D-16: preserve calibration validation; D-17: preserve launch defaults; D-18: preserve canonical wrapper; D-19: preserve wrapper credential boundary; D-20: preserve mouse-only behavior; D-21: preserve diagnostic wrapper path; D-22: preserve TTY/startx flow; D-23: preserve menu/xinit behavior; D-24: preserve invocation contract; D-25: preserve accepted certificate exception.

## Artifacts this phase produces

| Category | This plan adds |
| --- | --- |
| X11 cancellation and diagnostic lifecycle | Repaired `xf_input.c` behavior and an actual-dispatcher CTest target. |
| Source invariant evidence | A strengthened `tests/check_third_finger_owner.py`. |
| Shipping source delta | A refreshed `patches/onemix-touch.patch` that contains the repair and CTest source. |
| Debian packages or release assembly | None. No `.deb`, release script, release workspace, or publication-path work is added. |
| Launcher, menu, documentation, calibration, or credential transport | None. Existing artifacts remain untouched. |
| Diagnostic log files or publication hygiene artifacts | None. The test captures WLog records in memory only. |
| Server-certificate behavior | None. D-25 and the existing `/cert:ignore` behavior remain unchanged. |

## Source audit

SOURCE | ID | Feature or constraint | Coverage | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Verified Debian patch, opt-in diagnostics, and local-only preset | 04-01–04-06 historical coverage plus 04-07 lifecycle repair | COVERED | This gap invocation repairs only the failed diagnostic/interruption slice; historical package and launch artifacts are not reopened.
REQ | DIAG-01 | Opt-in truthful touch cancellation diagnostics while normal runs remain quiet | 04-07 Task 1 and Task 2 | COVERED | Dispatcher CTest captures the real WLog cancellation batch and fresh-patch test reruns it.
REQ | CONF-01 | Local-only preset remains usable after interruption | 04-07 Task 1 | COVERED | Recovery test proves a fresh local touch enters after the final old End; launch-wrapper work is historical and not modified.
REQ | PACK-01 | Reproducible installable Debian package from the quilt patch | 04-01, 04-05, 04-06 historical coverage | HISTORICAL / OPERATOR-EXCLUDED | Task 2 proves patch application and source synchronization only; no package assembly is claimed.
REQ | PACK-02 | Exact install and rollback | 04-03, 04-05, 04-06 historical coverage | HISTORICAL / OPERATOR-EXCLUDED | No install or rollback action belongs to this targeted gap plan.
RESEARCH | Local-only X11 architecture | Keep native touch forwarding disabled and reuse the existing fallback recognizer | 04-07 Task 1 | COVERED | The regression explicitly enables the local fallback setting and keeps native multitouch off.
RESEARCH | Existing diagnostic path | Reuse WLog/stderr and the cached diagnostic gate; add no logging framework | 04-07 Task 1 | COVERED | The test uses the existing WLog callback appender solely to observe production records.
RESEARCH | Quilt source-of-truth pattern | One project patch must apply after Debian's quilt stack | 04-07 Task 2 | COVERED | A newly extracted source tree receives the refreshed patch through quilt before testing.
RESEARCH | No new dependency or daemon | Use current C, CMake, XInput2, WLog, Debian, and quilt facilities | 04-07 Tasks 1–2 | COVERED | No package-manager install task or new runtime dependency is introduced.
CONTEXT | D-01 | Native multitouch stays disabled | 04-07 Task 1 | COVERED | The fixture uses `FreeRDP_TouchPinchWheelFallback` with `FreeRDP_MultiTouchInput` false.
CONTEXT | D-02 | Preserve frozen one-, two-, and three-finger local gesture behavior | 04-07 Task 1 | COVERED | The repair changes shared interruption ownership only and adds direct dispatcher regressions for its affected branches.
CONTEXT | D-03 | Exact `FREERDP_TOUCH_DIAG=1` gate | 04-07 Task 1 | COVERED | Existing gate remains the sole environment lookup; cancellation records remain behind it.
CONTEXT | D-04 | Compact trace includes touch cancellation and cleanup evidence | 04-07 Task 1 | COVERED | Captured production records prove final-coordinate cancels and one local-only summary.
CONTEXT | D-05 | Existing stderr/WLog route | 04-07 Task 1 | COVERED | Production callback emits through current WLog code; test capture does not add a runtime logger.
CONTEXT | D-06 | Do not emit commands, passwords, or stored credentials | 04-07 Task 1 | COVERED | New record fields are only touch ID, coordinates, counts, and recovery state.
CONTEXT | D-07 | Local package-version policy | Historical Phase 4 plans | OPERATOR-EXCLUDED | Preserved; no changelog or package-version change.
CONTEXT | D-08 | Debian security-update policy | Historical Phase 4 plans | OPERATOR-EXCLUDED | Preserved; no package-policy change.
CONTEXT | D-09 | Release-build script policy | Historical Phase 4 plans | OPERATOR-EXCLUDED | No release-script edit or release execution is planned.
CONTEXT | D-10 | Exact package-set policy | Historical Phase 4 plans | OPERATOR-EXCLUDED | No package-set derivation or artifact publication is planned.
CONTEXT | D-11 | Commit only source artifacts and documentation | Historical Phase 4 plans | OPERATOR-EXCLUDED | This plan updates the tracked source patch and test source only.
CONTEXT | D-12 | Exact install and rollback policy | Historical Phase 4 plans | OPERATOR-EXCLUDED | No package transaction is planned.
CONTEXT | D-13 | Two-value calibration surface | Historical Phase 4 plans | OPERATOR-EXCLUDED | No calibration edit.
CONTEXT | D-14 | Fixed classifier constants | 04-07 Task 1 | COVERED | No classifier threshold, ratio, or wheel-step modification.
CONTEXT | D-15 | Existing calibration CLI mapping | Historical Phase 4 plans | OPERATOR-EXCLUDED | No wrapper or parser edit.
CONTEXT | D-16 | Calibration validation | Historical Phase 4 plans | OPERATOR-EXCLUDED | No wrapper validation edit.
CONTEXT | D-17 | Non-interactive defaults | Historical Phase 4 plans | OPERATOR-EXCLUDED | No launch-default edit.
CONTEXT | D-18 | Canonical credential-free wrapper | Historical Phase 4 plans | OPERATOR-EXCLUDED | No wrapper or menu edit.
CONTEXT | D-19 | Wrapper credential boundary | Historical Phase 4 plans | OPERATOR-EXCLUDED | No argument-transport change.
CONTEXT | D-20 | Mouse-only escape hatch | Historical Phase 4 plans | OPERATOR-EXCLUDED | The recovery regression protects the underlying local mode only.
CONTEXT | D-21 | One diagnostic wrapper path | Historical Phase 4 plans | OPERATOR-EXCLUDED | No wrapper/log-file behavior change.
CONTEXT | D-22 | TTY/startx operating flow | Historical Phase 4 plans | OPERATOR-EXCLUDED | No session-launch change.
CONTEXT | D-23 | Menu delegation and private xinit hygiene | Historical Phase 4 plans | OPERATOR-EXCLUDED | No menu, xinit, or credential handling change.
CONTEXT | D-24 | Locked three-mode invocation contract | Historical Phase 4 plans | OPERATOR-EXCLUDED | No launcher invocation change.
CONTEXT | D-25 | Accepted certificate exception | Deferred by owner | EXCLUDED | Preserve `/cert:ignore`; do not claim server-identity protection.
</objective>

<execution_context>
@/home/hoang/.claude/gsd-core/workflows/execute-plan.md
@/home/hoang/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-VERIFICATION.md
@.planning/phases/04-diagnostics-packaging-launch-configuration/04-04-SUMMARY.md
@build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c
@build/freerdp3-3.15.0+dfsg/client/X11/xf_touch_internal.h
@build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h
@build/freerdp3-3.15.0+dfsg/client/X11/CMakeLists.txt
@build/freerdp3-3.15.0+dfsg/client/common/test/CMakeLists.txt
@tests/check_third_finger_owner.py
@patches/onemix-touch.patch
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exercise the real XI2 dispatcher, then repair quarantine and canonical local cancellation diagnostics</name>
  <files>build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c, build/freerdp3-3.15.0+dfsg/client/X11/CMakeLists.txt, build/freerdp3-3.15.0+dfsg/client/X11/test/CMakeLists.txt, build/freerdp3-3.15.0+dfsg/client/X11/test/TestXfInputDispatcher.c, tests/check_third_finger_owner.py</files>
  <precondition>The Phase 1 Debian build environment can configure this source tree with CMake, CTest, XInput2 headers, the existing FreeRDP targets, and the existing WLog implementation.</precondition>
  <read_first>
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c — read `xf_input_touch_remote`, `xf_touch_cancel_lifecycle`, each fallback TouchEnd branch, and `xf_input_handle_event_remote` before changing ownership.
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_touch_internal.h — reuse `xf_quarantine_update`, `xf_diag_contact_begin/update/end`, and `xf_force_cancel_emit_sequence`; do not introduce a parallel lifecycle helper.
    - build/freerdp3-3.15.0+dfsg/client/X11/xfreerdp.h — confirm the complete quarantine, diagnostic, pinch, midpoint, and three-finger state fields.
    - build/freerdp3-3.15.0+dfsg/client/X11/CMakeLists.txt and build/freerdp3-3.15.0+dfsg/client/common/test/CMakeLists.txt — follow the existing CTest target and output-directory conventions.
    - build/freerdp3-3.15.0+dfsg/winpr/libwinpr/utils/test/TestWLogCallback.c — reuse the existing WLog callback-appender pattern for in-memory assertions.
    - tests/check_third_finger_owner.py and .planning/phases/04-diagnostics-packaging-launch-configuration/04-VERIFICATION.md — preserve existing checker coverage while closing only Gaps 2 and 3.
  </read_first>
  <behavior>
    - RED: `TestXfInputDispatcher` constructs synthetic XI2 touch cookies, invokes public `xf_input_handle_event`, and fails against the current dispatcher on the sequence Begin(A), Begin(B), force-cancel, Update(A), End(A), force-cancel, End(B), Begin(C).
    - The sequence proves Update(A) is consumed while two old IDs remain, End(A) leaves B quarantined with the recovery gate armed, the repeated force-cancel preserves B, End(B) clears the gate, and Begin(C) reaches real ingress to create exactly one diagnostic contact.
    - The WLog callback sees one cancellation record for each active local diagnostic contact at its adjusted final coordinates and exactly one initial cancellation summary containing `native_count=0`.
    - A third-finger abort path with supplemental IDs shaped as A, B, zero, C retains A, B, and C in the quarantine set.
    - A pre-claim three-finger lift clears `threeFingerPending`, both pinch IDs and coordinates, the third-finger ID and coordinates, and the associated midpoint fields before subsequent input.
  </behavior>
  <action>
    Start with the CTest regression. Add the standard BUILD_TESTING-gated X11 test subdirectory and a `TestXfInputDispatcher` executable following FreeRDP's existing CTest pattern. Link it to the actual X11 client target and use linker wraps for `XGetEventData` and `XFreeEventData` so synthetic `XGenericEventCookie` and `XIDeviceEvent` fixtures can drive public `xf_input_handle_event` without a display server. Do not copy the dispatcher, call `xf_quarantine_update` directly from the test, or add a test-only production dispatch seam.

    In that fixture, enable `FreeRDP_TouchPinchWheelFallback` and leave `FreeRDP_MultiTouchInput` false per D-01. Configure the existing WLog callback appender to capture formatted text in memory. Prove the exact cancellation sequence and the two additional supplemental-ID and pre-claim-lift scenarios from `<behavior>`. Keep assertions at the production boundary: inspect `xfContext` state, diagnostic-store count/content, and emitted WLog text, not a duplicated helper model. This is the tracer for D-01, D-02, D-03, D-04, D-05, and CONF-01.

    After the regression is red, make the smallest shared-owner repair in `xf_input.c`. Every runtime change to the quarantine array, count, and recovery gate must pass through `xf_quarantine_update`; replace the direct gesture-lift assignments in pinch cleanup, pending/scroll/middle-drag lift branches, the cancellation owner, and the recovery dispatcher. Keep pre-existing initialization in `xf_input_init` as initialization only. Do not clear the accumulated quarantine before an add-set update, and remove the dispatcher-level gate disarm so the helper derives it from the remaining count.

    In the third-finger abort route, compact nonzero supplemental IDs into a contiguous array before passing its count to `xf_touch_cancel_lifecycle`, preserving a later touch ID when an earlier optional slot is zero. In the pre-claim three-finger TouchEnd route, save the remaining IDs, update quarantine through the shared helper, then clear the whole pending-three-finger state described in `<behavior>`. Preserve recognizer decisions, timing, classifier constants, local mouse/keyboard synthesis, and the no-native-forwarding behavior exactly per D-02 and D-14.

    Make `diagContacts` the sole local diagnostic lifecycle source. Move its Begin/Update/End maintenance to the adjusted coordinate frame that feeds the fallback recognizer, snapshot and emit it through exactly one production call to `xf_force_cancel_emit_sequence` in `xf_touch_cancel_lifecycle`, and translate that callback through the existing WLog path. Report the canonical local-only diagnostic summary with native count zero, retire all diagnostic contacts immediately after cancellation emission, and retire the relevant diagnostic contact when a quarantined End is consumed during recovery. Native-contact cleanup may remain for defensive lifecycle cleanup, but it must not supply local cancellation diagnostic coordinates or counts. Keep records behind the existing exact diagnostic gate and limit new fields to touch identifiers, coordinates, lifecycle counts, and recovery information per D-03 through D-06.

    Strengthen `tests/check_third_finger_owner.py` with brace-scoped checks that allow quarantine initialization only in `xf_input_init`, require one production helper call from `xf_touch_cancel_lifecycle`, require the shared quarantine updater at all runtime mutation sites, prove the third-finger route uses a compacted supplemental set, and reject diagnostic cancellation evidence sourced from the noncanonical contact arrays. Retain its existing gate and format checks.
  </action>
  <verify>
    <automated>cmake -S build/freerdp3-3.15.0+dfsg -B build/freerdp3-3.15.0+dfsg/obj-x86_64-linux-gnu -DBUILD_TESTING=ON &amp;&amp; cmake --build build/freerdp3-3.15.0+dfsg/obj-x86_64-linux-gnu --target TestXfInputDispatcher &amp;&amp; ctest --test-dir build/freerdp3-3.15.0+dfsg/obj-x86_64-linux-gnu --output-on-failure -R '^TestXfInputDispatcher$' &amp;&amp; python3 tests/check_third_finger_owner.py build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c</automated>
  </verify>
  <acceptance_criteria>The actual public XI2 input entry point passes all three dispatcher scenarios: it preserves old quarantine through a repeated cancel, permits a fresh Begin only after the final old End, retains every compacted supplemental ID, clears all pending-three-finger fields on its pre-claim lift, and emits final-coordinate local diagnostics from `diagContacts` with one `native_count=0` summary.</acceptance_criteria>
  <done>The production dispatcher and its canonical local diagnostic lifecycle are covered by a passing CTest target and a passing function-scoped source invariant checker.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the quilt patch and prove the fresh source executes the dispatcher regression</name>
  <files>patches/onemix-touch.patch</files>
  <precondition>Task 1 is green, `build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc` exists, and the Phase 1 environment provides `dpkg-source`, `quilt`, CMake, CTest, and the already installed Debian build dependencies.</precondition>
  <read_first>
    - patches/onemix-touch.patch — preserve its project-patch format and all pre-existing source deltas.
    - scripts/build-release.sh — use its Debian extraction and full quilt-stack application contract as reference only; do not edit or execute release-assembly behavior for this targeted repair.
    - build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c, build/freerdp3-3.15.0+dfsg/client/X11/CMakeLists.txt, and build/freerdp3-3.15.0+dfsg/client/X11/test/ — these are the exact source artifacts that must be represented in the refreshed patch.
    - tests/check_third_finger_owner.py and the Task 1 CTest target — both must run against the freshly patched extraction, not only the working build tree.
    - .planning/phases/04-diagnostics-packaging-launch-configuration/04-CONTEXT.md — preserve D-07 through D-24 artifacts and D-25 certificate behavior without changing their scope.
  </read_first>
  <action>
    Refresh only `patches/onemix-touch.patch` from the completed Task 1 source delta. In a private fresh Debian-source workspace, extract the pinned `.dsc`, copy the current project patch into that extraction's temporary Debian patch stack, append it there only, and apply the complete stack with quilt. Overlay the repaired X11 source and new CTest source from the build tree, add new test files to quilt, refresh the project patch, and copy the refreshed patch back to the repository. Do not alter the repository's Debian series, release script, package identity, package-selection logic, launcher/menu/docs, calibration handling, credential transport, diagnostic log publication, or certificate behavior; this preserves D-07 through D-25 and the operator's explicit exclusions.

    Create a second brand-new extraction from the same `.dsc`, apply the refreshed project patch through the complete quilt stack, and configure that extracted tree with BUILD_TESTING enabled in an isolated build directory. Build and run `TestXfInputDispatcher` with CTest, then run the repository source checker with the fresh `client/X11/xf_input.c` path. Compare the patched extraction's `xf_input.c`, parent CMake file, and X11 test directory to the repaired build-tree counterparts so source/patch drift fails the task. Use a private temporary workspace for these proofs; this is test isolation, not release assembly or publication work.
  </action>
  <verify>
    <automated>In a new `dpkg-source -x build/freerdp3_3.15.0+dfsg-2.1+deb13u3.dsc` workspace: add the refreshed patch to the temporary `debian/patches/series`, run `QUILT_PATCHES=debian/patches quilt push -a`, configure with `cmake -DBUILD_TESTING=ON`, build `TestXfInputDispatcher`, run `ctest --output-on-failure -R '^TestXfInputDispatcher$'`, run `python3 /home/hoang/freerdp-touch/tests/check_third_finger_owner.py client/X11/xf_input.c`, and use `cmp` plus `diff -ruN` to prove the repaired build-tree X11 source/CMake/test artifacts match the fresh patched extraction.</automated>
  </verify>
  <acceptance_criteria>`patches/onemix-touch.patch` applies after the Debian quilt stack in a newly extracted source tree; that tree contains the repaired dispatcher and CTest source byte-for-byte, passes the real dispatcher regression and scoped source checker, and no excluded release, launcher, credential, log-publication, or certificate artifact is modified.</acceptance_criteria>
  <done>The shipping patch truthfully contains and proves the Gap 2 and Gap 3 repair on a fresh Debian source extraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| XI2 event cookie to local gesture state | Timing-sensitive, device-originated touch IDs and event order update local quarantine and recognizer state. |
| Local diagnostic state to WLog stderr | Diagnostic records expose local touch metadata only when the explicit diagnostic gate is enabled. |
| Repaired build tree to Debian quilt extraction | The working source delta becomes a shipping patch applied to a clean source package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07-01 | D/T | `xf_input_handle_event_remote` recovery gate and quarantine set | high | mitigate | Route runtime state transitions through `xf_quarantine_update`; assert old Update, partial End, repeated cancel, final End, and fresh Begin through the real dispatcher. |
| T-04-07-02 | I | `xf_touch_cancel_lifecycle` and `diagContacts` | high | mitigate | Use the canonical adjusted-coordinate store, invoke the existing ordered emission helper once in production, retire contacts at cancellation/recovery, and capture real WLog records in CTest. |
| T-04-07-03 | I | Diagnostic WLog records | medium | mitigate | Preserve D-03 through D-06: exact opt-in gate, current stderr/WLog path, and compact touch-only fields with no command or credential data. |
| T-04-07-04 | T | `patches/onemix-touch.patch` | medium | mitigate | Apply the refreshed patch through the full Debian quilt stack in a new extraction, compare source artifacts, and rerun the dispatcher regression there. |
| T-04-07-SC | T | npm/pip/cargo installs | low | accept | No package-manager installation is in scope; the plan uses the existing Debian build environment only. |
</threat_model>

<verification>
1. Configure the current source tree with BUILD_TESTING enabled, build `TestXfInputDispatcher`, and run its focused CTest case.
2. Run `python3 tests/check_third_finger_owner.py build/freerdp3-3.15.0+dfsg/client/X11/xf_input.c`.
3. In a new Debian-source extraction, apply the refreshed patch through quilt, rerun the focused CTest and checker, and compare the affected source artifacts to the repaired build tree.
4. Confirm the working tree changes for this plan are only the stated X11 source/test/checker/patch artifacts; no release, launcher, package, credential, diagnostic-log, or certificate artifact is added.
</verification>

<success_criteria>
- The real XI2 dispatcher maintains quarantine until the final old touch ends, including across a repeated cancellation, and accepts a fresh local Begin afterward.
- Cancellation diagnostics are sourced from adjusted `diagContacts`, show final local positions, report `native_count=0`, and retire diagnostic contacts during cancellation/recovery.
- Third-finger and pre-claim lift paths preserve all relevant touch IDs and clear stale pending-three-finger state.
- The fresh quilt-applied source tree passes the same direct dispatcher regression and source ownership checks as the working build tree.
</success_criteria>

<output>
Create `.planning/phases/04-diagnostics-packaging-launch-configuration/04-07-SUMMARY.md` when done.
</output>
